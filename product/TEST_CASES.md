**Missing Issue Key → 422 |**
| 20 | TC-008 | Missing Status → 422 |
| 21 | TC-005 | No file field → 400 |
| 22 | TC-011 | Rate limit 21st request → 429 |
| 23 | TC-013 | Import log written correctly |
| 24 | TC-088 | Health endpoint → correct JSON |
| 25 | TC-089 | Logs endpoint → JSON array |

### Tier 3 — E2E / UI Tests (run on PR to main, < 5 minutes total)

| Order | Test Case | What It Guards |
|---|---|---|
| 26 | TC-031 | Health score gauge renders after upload |
| 27 | TC-032 | KPI grid has 6 cards |
| 28 | TC-042 | DeliveryCircle segments sum to totalIssues |
| 29 | TC-046 | High Risk filter opens flow panel with critical filter |
| 30 | TC-054 | Pagination shows 100 rows, Show More loads 100 more |
| 31 | TC-055 | Reset clears all 11 filters |
| 32 | TC-076 | Quick Overview opens Manager Report modal |
| 33 | TC-078 | Snapshot grid shows 8 cells |
| 34 | TC-080 | Risk Details link closes modal and filters flow table |
| 35 | TC-062 | SectionNav has exactly 14 dots |
| 36 | TC-073 | Dark mode toggle switches class |
| 37 | TC-074 | No horizontal scroll at 375 px |

### Tier 4 — Performance Tests (run nightly)

| Order | Test Case | What It Guards |
|---|---|---|
| 38 | TC-014 | 5,000-row file processes under 5 seconds |

---

## 5. Test Data Requirements

All fixture files below should be created before test execution begins and stored in `backend/tests/fixtures/`.

### 5.1 Minimal Valid Files

| File | Description | Rows | Columns |
|---|---|---|---|
| `valid_minimal.csv` | All four required fields only; 3 rows; 1 done, 2 open | 3 | 4 |
| `valid_full.xlsx` | All required + recommended optional fields; 50 rows; 20 done | 50 | 25 |
| `header_only.csv` | Required column headers only; zero data rows | 0 | 4 |
| `minimal_required_only.csv` | Four required fields, ten rows, all open | 10 | 4 |

### 5.2 Health Classification Fixtures

| File | Description |
|---|---|
| `blocked_items.csv` | 10 rows; PROJ-1 and PROJ-2 have `Blocked Flag: true`, Status `In Progress` |
| `all_done.csv` | 10 rows; all `Status: Done`, all have `Done Date` 3 days after `Created Date` |
| `score_excellent.xlsx` | 20 rows; all done, cycle time ≤ 3 days, no orphans, sprint present |
| `all_critical.csv` | 10 rows; all `Status: In Progress`, all `Blocked Flag: true`, no Epic Link |

### 5.3 Filter and Flow Table Fixtures

| File | Description |
|---|---|
| `e2e_standard.xlsx` | 100 rows; 50 done; mix of statuses, assignees, sprints, labels, epics |
| `large_200.csv` | 210 rows; all required fields; uniform mix of statuses |
| `large_5000.xlsx` | 5,000 rows; all required + optional fields; ~15 MB |
| `orphan_items.csv` | 10 rows; PROJ-1–PROJ-4 have no Epic Link or Parent Key |
| `capacity_imbalance.csv` | 20 rows; "Jordan Lee" assigned 8 of 20 issues (40%) |
| `attention_test.csv` | 20 rows; 3 blocked, 3 overdue, 3 orphans |

### 5.4 Date and Parsing Fixtures

| File | Description |
|---|---|
| `alias_created.csv` | Uses `created` (lowercase) as header for the date field |
| `epic_link_alias.csv` | Uses `Custom Field (Epic Link)` header |
| `bom_header.csv` | UTF-8 BOM prepended to first header byte |
| `multi_quarter.csv` | 30 rows; `Done Date` spanning Q1 2026, Q2 2026, and no-date issues |
| `long_summary.csv` | 3 rows; PROJ-1 Summary is 1,000 characters |
| `sparse_issue.csv` | 5 rows; only required fields populated; all others empty string |

### 5.5 Link and Relations Fixtures

| File | Description |
|---|---|
| `with_links.csv` | 3 rows; columns `Inward issue link (Blocks)` and `Outward issue link (Blocks)` present with values |
| `critical_epic.csv` | 15 rows; 10 issues under EPIC-1, 8 of those blocked and critical |

### 5.6 Story Points and Prediction Fixtures

| File | Description |
|---|---|
| `story_points.csv` | 10 rows; 6 done × 10 pts = 60 pts done; 4 open × 10 pts = 40 pts remaining |
| `prediction_test.csv` | 50 rows; 20 done in last 30 days with `Done Date`; all have `Created Date` 30 days ago |

### 5.7 Security and Edge-Case Fixtures

| File | Description |
|---|---|
| `report.pdf` | Any valid PDF under 1 MB; used to test unsupported type rejection |
| `rate_limit_test.csv` | Minimal valid 3-row CSV; used for rate-limit loop test |
| `missing_key.csv` | Three rows; `Issue Key` column absent |
| `missing_status.csv` | Three rows; `Status` column absent |
| `random_spreadsheet.csv` | Product/price spreadsheet with no Jira columns |
| `large_file.xlsx` | 20 MB + 1 byte; generated programmatically with padding |

### 5.8 Fixture Generation Notes

The `large_5000.xlsx` and `large_file.xlsx` fixtures should be generated by a Node.js script (`backend/tests/fixtures/generate-fixtures.js`) using the `xlsx` library to avoid committing large binary files to source control. All other fixtures are small enough to commit directly. The generation script should be idempotent (checking for existing files before writing) and should be run once as part of the test setup step.

---

*End of Test Cases Document — Delivery Clarity v4.0 — 2026-06-03*

---

## New Test Cases — v1.1 Routing (TC-101–TC-110)

### TC-101: Upload redirects to /summary

| Field | Value |
|---|---|
| **ID** | TC-101 |
| **Title** | Successful upload navigates to /summary |
| **Type** | E2E |
| **Priority** | P1 Critical |
| **FR** | FR-200 |

**Preconditions:** App loaded at /, valid Jira CSV ready
**Steps:** 1. Upload a valid Jira CSV file 2. Wait for upload to complete
**Expected Result:** Browser URL changes to /summary; SummaryPage is visible
**Pass Criteria:** URL === '/summary' AND health score gauge visible
**Status:** Not Run

---

### TC-102: Summary page health score colour band

| Field | Value |
|---|---|
| **ID** | TC-102 |
| **Title** | Health score 84 → "good" band (teal circle) |
| **Type** | UI |
| **Priority** | P1 Critical |
| **FR** | FR-201 |

**Preconditions:** Uploaded file producing healthScore=84
**Steps:** 1. Upload file 2. Land on /summary 3. Inspect health score circle CSS class
**Expected Result:** .summary-score-circle has class score-good; circle is teal
**Pass Criteria:** score-good class present
**Status:** Not Run

---

### TC-103: Summary page shows all 6 KPI cards

| Field | Value |
|---|---|
| **ID** | TC-103 |
| **Title** | Summary page renders 6 KPI cards |
| **Type** | UI |
| **Priority** | P1 Critical |
| **FR** | FR-201 |

**Preconditions:** Data with story points > 0
**Steps:** 1. Upload file 2. Land on /summary 3. Count KPI cards
**Expected Result:** 6 KpiCard components visible (Completion, Health Alerts, Active Work, Lead Time, Cycle Time, Story Points)
**Pass Criteria:** 6 cards rendered with non-empty values
**Status:** Not Run

---

### TC-104: Attention cards shown when blockers exist

| Field | Value |
|---|---|
| **ID** | TC-104 |
| **Title** | Summary shows blocker card when blocked items exist |
| **Type** | UI |
| **Priority** | P2 High |
| **FR** | FR-201 |

**Preconditions:** Uploaded file with at least 1 critical blocked item
**Steps:** 1. Upload file 2. Land on /summary 3. Check attention section
**Expected Result:** .summary-attention-card.card-blockers visible with count > 0
**Pass Criteria:** Blockers card shown with correct count
**Status:** Not Run

---

### TC-105: No attention section when all items healthy

| Field | Value |
|---|---|
| **ID** | TC-105 |
| **Title** | Attention section hidden when no blockers/overdue/orphans |
| **Type** | UI |
| **Priority** | P2 High |
| **FR** | FR-201 |

**Preconditions:** All items are healthy, none overdue, none orphaned
**Steps:** 1. Upload file 2. Land on /summary 3. Check for attention section
**Expected Result:** No .summary-attention-grid rendered
**Pass Criteria:** Attention section absent from DOM
**Status:** Not Run

---

### TC-106: View Full Report button navigates to /dashboard

| Field | Value |
|---|---|
| **ID** | TC-106 |
| **Title** | "View Full Report →" navigates to /dashboard |
| **Type** | E2E |
| **Priority** | P1 Critical |
| **FR** | FR-202 |

**Preconditions:** User is on /summary
**Steps:** 1. Click "View Full Report →" button
**Expected Result:** URL changes to /dashboard; DashboardPage renders
**Pass Criteria:** URL === '/dashboard' AND DashboardPage h2 visible
**Status:** Not Run

---

### TC-107: Back to Overview button navigates to /summary

| Field | Value |
|---|---|
| **ID** | TC-107 |
| **Title** | "← Back to Overview" navigates from /dashboard to /summary |
| **Type** | E2E |
| **Priority** | P2 High |
| **FR** | FR-205 |

**Preconditions:** User is on /dashboard
**Steps:** 1. Click "← Back to Overview" 
**Expected Result:** URL changes to /summary; SummaryPage renders
**Pass Criteria:** URL === '/summary'
**Status:** Not Run

---

### TC-108: Help button navigates to /help route

| Field | Value |
|---|---|
| **ID** | TC-108 |
| **Title** | Help button opens /help as a page (not modal) |
| **Type** | E2E |
| **Priority** | P1 Critical |
| **FR** | FR-204 |

**Preconditions:** User is on any page
**Steps:** 1. Click "Help" button in AppHeader
**Expected Result:** URL changes to /help?section=welcome; HelpGuide renders as page (no backdrop)
**Pass Criteria:** URL includes '/help'; .help-overlay class absent; .help-panel-page class present
**Status:** Not Run

---

### TC-109: Context ? button navigates to correct help section

| Field | Value |
|---|---|
| **ID** | TC-109 |
| **Title** | ? button on KPIs section opens /help?section=kpis |
| **Type** | E2E |
| **Priority** | P2 High |
| **FR** | FR-204 |

**Preconditions:** User is on /dashboard, Overview/KPI section visible
**Steps:** 1. Click ? button next to KPI section 2. Check URL and active tab
**Expected Result:** URL = /help?section=kpis; KPIs tab is active in help guide
**Pass Criteria:** URL contains 'section=kpis'; KPIs tab has 'active' class
**Status:** Not Run

---

### TC-110: Direct URL to /summary without data → redirect to /

| Field | Value |
|---|---|
| **ID** | TC-110 |
| **Title** | Unauthenticated direct URL access redirects to upload |
| **Type** | E2E |
| **Priority** | P1 Critical |
| **FR** | FR-206 |

**Preconditions:** New browser session; no file uploaded; navigate directly to /summary
**Steps:** 1. Open /summary in browser with no prior upload
**Expected Result:** URL changes to /; UploadPage renders
**Pass Criteria:** URL === '/' AND UploadPage visible
**Status:** Not Run

---

## v3.0 Test Cases — Throughput, Explorer, Auth, Excel

### F1 — Throughput Formula Tests (implemented in src/__tests__/throughput.test.ts)

| ID | Test | Formula | Expected | Status |
|----|------|---------|----------|--------|
| TC-T-01 | 10 committed, 8 done | completionPct = done/committed×100 | 80%, Partially Met | ✅ Pass |
| TC-T-02 | 3 done by mid, 10 committed | midSprintPct = midDone/committed×100 | 30% | ✅ Pass |
| TC-T-03 | Sprints [8,10,12] done | averageThroughput = sum/count | 10 | ✅ Pass |
| TC-T-04 | 9/10 done | completionPct ≥ 90% | goalOutcome = Met | ✅ Pass |
| TC-T-05 | 5/10 done, past end | completionPct < 60%, past sprint end | Missed | ✅ Pass |
| TC-T-06 | 2 added-after-start | addedScopeCount | 2 | ✅ Pass |
| TC-T-07 | 1/10 done by midpoint | midSprintPct < 20% | End-Loaded Sprint | ✅ Pass |
| TC-T-08 | 6/10 done by midpoint | midSprintPct ≥ 50% | Healthy Early Progress | ✅ Pass |
| TC-T-09 | 3 added on 11 total | added > 20% committed | Scope Instability | ✅ Pass |
| TC-T-10 | 2 blocked items | blockedCount ≥ 2 | Blocked Sprint | ✅ Pass |
| TC-T-11 | `calculateDashboardMetrics(issues).throughput` | shape conforms to `ThroughputMetrics` (FR-215: `sprint`/`kanban`/`midSprint` bundle, `src/types/throughput.ts`) | `sprint.sprints[]`, `kanban.periods[]`, `midSprint[]` all present with documented field types | ✅ Pass |

**Related:** UC-043, SCN-012, UJ-010, FR-207–FR-215 (F1-07 `src/types/throughput.ts` and F1-08 `DashboardMetrics.throughput` are the data-contract layer that `TC-T-11` anchors and that `UC-043`/`SCN-012`/`UJ-010` consume through the Throughput & Delivery Analytics panels)

### F2 — Work Item Explorer Tests (implemented in src/__tests__/relationExplorer.test.ts)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-E-01 | Story focus → parent Epic + 3 children | All 5 nodes in graph | ✅ Pass |
| TC-E-02 | Epic focus → child Story | Story in graph | ✅ Pass |
| TC-E-03 | Focus node marked correctly | isFocusNode = true | ✅ Pass |
| TC-E-04 | Non-existent key | buildRelationGraph returns null | ✅ Pass |
| TC-E-05 | Completion stats | completedCount ≥ 2, completionPct > 0 | ✅ Pass |
| TC-E-06 | Orphan detection | orphanKeys contains PROJ-99 | ✅ Pass |
| TC-E-07 | Focus node depth | depth = 0 | ✅ Pass |
| TC-E-08 | Ancestor/child depth | parent depth=-1, child depth=1 | ✅ Pass |
| TC-E-09 | Multi-level ancestor chain (Epic → Initiative/grandparent) | Graph includes the grandparent, not just the immediate parent | ✅ Pass — added 2026-06-22, `JIRA-11` |
| TC-E-10 | Ancestor depths beyond one level | Closest ancestor = depth -1, each level up counts down further (-2, -3, ...) | ✅ Pass — added 2026-06-22, `JIRA-11` |
| TC-E-11 | Ancestor-chain edge (root → closest parent) | Edge present connecting every adjacent pair in the chain | ✅ Pass — added 2026-06-22, `JIRA-11` |
| TC-E-12 | "Product"/"Project" issue types (hierarchy levels above Epic) | Resolve to their own `Product`/`Project` node type, not `Unknown` | ✅ Pass — added 2026-06-22, `JIRA-12` |
| TC-E-13 | A root container (no parent of its own, but has children) | `isOrphan` is `false` — having no parent is correct for a root, not a data-quality issue | ✅ Pass — added 2026-06-22, `JIRA-12` |

**Related:** UC-046, UC-088, SCN-013, SCN-044, UJ-011 — covers F2-01–F2-08 (hierarchy reconstruction, orphan detection, relation graph builder, React Flow visual graph, node styles per type, orphan visual treatment, RelationLegend/RelationInsightPanel/RelationStatsCards/RelationDetailsTable, RelationCharts). `TC-E-09/10/11` close a gap where `buildRelationGraph()` only showed one level up by deliberate earlier design — see `JIRA-11` in `TODO-List.md` Section 19a. `TC-E-12/13` close a phantom-hierarchy-cycle regression introduced by enabling that ancestor chain — see `JIRA-12`.

**Live (non-mocked) verification, JIRA-11:** re-synced the real "Agile Jordan" Jira connection and confirmed the Explore page renders the full ancestor chain for a real Epic (`AJ-28` → parent `AJ-27` → grandparent `AJ-26`), which previously showed only the focus node and its children.

**Live (non-mocked) verification, JIRA-12:** re-synced again after the cycle fix and confirmed `AJ-26`/`AJ-27` show their correct types with no stray "Orphan"/"Most work" badges, and Key Metrics reports "Orphans: 0". **Separately discovered but explicitly not fixed in this pass** — the relation graph's connecting lines never render any visible path at all, reproduced even on the unrelated, already-correct `AJ-28` children graph in both dev and production builds — see `JIRA-13` in `TODO-List.md` Section 19a.

### Hierarchy reconstruction cycle-safety (implemented in src/__tests__/hierarchyService.test.ts)

*(Added 2026-06-22, `JIRA-12`.)*

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-HIER-01 | A root container (e.g. "Product") shares its key prefix with an unrelated Epic | No phantom epic link is created — `map.epic.has(rootKey)` is `false` | ✅ Automated — "TC-HIER-01" |
| TC-HIER-02 | Ancestor chain for the middle node of a Product→Project→Epic chain | Terminates at the true root (`['Product']`), with no cycle back to the starting node | ✅ Automated — "TC-HIER-02" |
| TC-HIER-03 | Ancestor chain for the Epic (bottom of the chain) | Includes both the Project and the Product, in order | ✅ Automated — "TC-HIER-03" |
| TC-HIER-04 | A root container that already has children | Never flagged as an orphan | ✅ Automated — "TC-HIER-04" |
| TC-HIER-05 | Children map after reconstruction | Reflects only the real parent-key links; never the inferred phantom one | ✅ Automated — "TC-HIER-05" |

### F3 — Authentication Tests (automated)

*(Renumbered and automated 2026-06-08 to close TRACE-02 / Gaps Summary COVER-11 — see TODO-List.md Section 8. This table previously listed nine "Not Run" manual placeholders TC-A-01–TC-A-09. Investigation found that `auth.test.ts` had independently grown its own `TC-A-01`–`TC-A-09` test IDs for different scenarios — a numeric-ID collision parallel to the FR/UC collisions resolved in TRACE-01 cluster #6. TC-A-01/02/08/09 below are close matches to `auth.test.ts`'s tests of the same ID and are cross-referenced as such. TC-A-03/04/05/06/07's old intents collided with `auth.test.ts` tests that cover genuinely different scenarios (disabled-account login-blocking, non-existent-user lookup, admin-role session assignment respectively) — rather than overwrite those passing tests' IDs, the five colliding rows below were renumbered to the free range TC-A-10–TC-A-14 and closed with new automated tests.)*

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-A-01 | Login with correct credentials sets session fields (`isLoggedIn`, `userId`, `email`, `name`) | `verifyPassword` returns true, `isActive` true, session saved with `isLoggedIn=true` | ✅ Automated — `auth.test.ts` (`TC-A-01`) |
| TC-A-02 | Login with wrong password is rejected | `verifyPassword('Wrong@999', hash)` returns false | ✅ Automated — `auth.test.ts` (`TC-A-02`) |
| TC-A-08 | Password is stored hashed, not in plaintext | bcrypt hash (`$2…` prefix), salted (same password → different hashes), ≥ 12 cost-factor rounds | ✅ Automated — `auth.test.ts` (`TC-A-08a`–`TC-A-08c`) |
| TC-A-09 | Login rate limiting blocks brute-force attempts | 6th attempt from the same IP within 1 minute is blocked; limiter resets independently per IP | ✅ Automated — `auth.test.ts` (`TC-A-09`, `TC-A-09b`) |
| TC-A-10 | Access a protected route (e.g. `/dashboard`) without a session | Middleware responds 307 and redirects to `/login?redirect=<path>` | ✅ Automated — `middleware.test.ts` (`TC-A-10`) *(renumbered from old TC-A-03 — see collision note above)* |
| TC-A-11 | Admin/manager/c-level roles can view all users' import logs (not just their own) | `canViewAllImportData()` returns true for `admin`, `manager`, and `c_level` | ✅ Automated — `roles.test.ts` ("role-scoped import visibility allows admin, manager, and c-level to view all imports") *(renumbered from old TC-A-04 — see collision note above; cross-ref UC-084)* |
| TC-A-12 | Non-admin visiting an admin-only route is blocked / redirected to their role's fallback | `canAccessRoute('scrum_master', '/admin/settings')` returns false; middleware enforces `fallbackRouteForRole()` | ✅ Automated — `roles.test.ts` ("role route matrix blocks routes outside assigned role scope") *(renumbered from old TC-A-05 — see collision note above)* |
| TC-A-13 | Logout clears the session and records an audit event | `POST /api/auth/logout` writes an `auditEvent` (`eventType: 'logout'`) for a logged-in user, calls `session.destroy()`, returns `{ ok: true }`; an already-anonymous request skips the audit write but still destroys the session | ✅ Automated — `logout.test.ts` (`TC-A-13a`, `TC-A-13b`) *(renumbered from old TC-A-06 — see collision note above)* |
| TC-A-14 | Uploading a file while logged in persists an `ImportLog` row tagged with the session's `userId`; an anonymous upload does not write to the DB | `prisma.importLog.create` called with `{ userId: <session.userId>, fileName, … }` when logged in; not called at all when `session.isLoggedIn` is false | ✅ Automated — `uploadUserId.test.ts` (`TC-A-14a`, `TC-A-14b`) *(renumbered from old TC-A-07 — see collision note above)* |

### F4 — Excel Export Tests (automated — `excelExport.test.ts`, `excelExportSheets.test.ts`)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-X-01 | Workbook sheet count and names | Exactly 17 sheets, named/ordered `01 Executive Summary` … `17 Raw Data Reference` | ✅ Pass |
| TC-X-02 | Executive Summary | Health score, completion rate, total issues all present in the sheet | ✅ Pass |
| TC-X-03 | Recommendations sheet | At least 1 critical rec when blocked items exist; every rec has evidence/impact/owner/action; header row present | ✅ Pass |
| TC-X-04 | All sheets frozen header / auto-filter | `!autofilter` set on every sheet with tabular data | ✅ Pass |
| TC-X-05 | Metric Dictionary | Sheet exists with content and defines Health Score, Lead/Cycle Time, Flow Efficiency, Orphan Issue | ✅ Pass |
| TC-X-06 | No HTML/JSON in cells | No `<tag>`, `className`, `style={{`, or `[object Object]` in any sheet | ✅ Pass |
| TC-X-07 | Executive narrative | Non-empty readable paragraph mentioning total-issue count and completion rate | ✅ Pass |
| TC-X-08 | Raw Data Reference sheet | Header row contains Issue Key, Assignee, Health columns | ✅ Pass |
| TC-X-09a | Risks & Blockers sheet (F4-05) — sort & actions | Rows ordered critical → warning → good; each row carries a risk-tier suggested action (Escalate / Review in next standup / Monitor) | ✅ Pass |
| TC-X-09b | Risks & Blockers sheet — clean bill of health | Shows "No risk items detected — delivery health looks good." when nothing qualifies | ✅ Pass |
| TC-X-10a | Orphan & Data Quality sheet (F4-05) — summary counts | Orphan / missing-SP / unassigned / no-sprint counts and percentages match the dataset | ✅ Pass |
| TC-X-10b | Orphan & Data Quality sheet — orphan detail | Each orphan listed with key/summary/type/status/assignee/age and "Epic Link and Parent Key" missing-note; non-orphans excluded | ✅ Pass |
| TC-X-10c | Orphan & Data Quality sheet — complete hierarchy | Shows "No orphan items detected — hierarchy is complete." when there are no orphans | ✅ Pass |
| TC-X-11a | Cycle & Lead Time sheet (F4-06) — percentiles | Average, Median (P50), P75, P85, P95, and sample size match the percentile formula for a known 1–10 day sample | ✅ Pass |
| TC-X-11b | Cycle & Lead Time sheet — slowest items | "TOP 20 SLOWEST ITEMS" ranks the highest lead-time item first | ✅ Pass |
| TC-X-12 | Release Readiness sheet (F4-05) — Go/Conditional/No-Go | Groups by Fix Version and assigns Go (100%, 0 blocked/bugs), Conditional Go (≥80%, ≤1 blocked), and No-Go (<80%) correctly | ✅ Pass |
| TC-X-13 | Export trigger — `exportToExcel` (F4-08) | Dashboard/summary "Export → Excel" control downloads the 17-sheet workbook as `delivery-clarity-report.xlsx` by default | ✅ Pass |
| TC-X-13b | Export trigger — custom filename | A caller-supplied filename is honored by the download | ✅ Pass |
| TC-X-14 | HTML export branding (UX-05 / FR-300) — `buildReportHtml` | Output markup contains the lightning-bolt brand-mark SVG, "Delivery Clarity" eyebrow, `<title>Delivery Clarity — Report</title>`, "Delivery Report" heading, and footer attribution "Ali Abu Ras · ali.aburas@deliveryclarity.app · Delivery Clarity v4.1" | ✅ Automated — `exportUtilsHtml.test.ts` "TC-X-14" |

**Related:** UC-049, UC-076, UC-089, SCN-016, SCN-035, SCN-045, UJ-013, UJ-029, UJ-031, FR-236, FR-310, FR-311, FR-300, BR-105 *(FR-310/FR-311 renumbered 2026-06-08 from colliding `FR-242`/`FR-243` — see TODO-List.md Section 12 Gaps Summary item 6)* — `excelExport.test.ts` (TC-X-01–08), `excelExportSheets.test.ts` (TC-X-09–13b), and `exportUtilsHtml.test.ts` (TC-X-14, the HTML report's branding markup extracted to the pure `buildReportHtml()` builder in `src/lib/exportUtils.ts` so the redesigned header/footer brand treatment is independently testable without a DOM)

---

## 9.18 — Work Item Explorer Risk-Path Highlight (TC-RP-01 to TC-RP-08)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-RP-01 | Blocked task → parent story marked isOnRiskPath=true | isOnRiskPath=true on both | ✅ Pass |
| TC-RP-02 | Healthy task → isOnRiskPath=false | isOnRiskPath=false | ✅ Pass |
| TC-RP-03 | Done critical items excluded from risk path | No risk path nodes | ✅ Pass |
| TC-RP-04 | Edge between blocked task and parent marked isOnRiskPath | isOnRiskPath=true on edge | ✅ Pass |
| TC-RP-05 | Edge to healthy task NOT marked as risk path | isOnRiskPath falsy | ✅ Pass |
| TC-RP-06 | All healthy nodes → no risk paths | riskNodes.length === 0 | ✅ Pass |
| TC-RP-07 | Two blocked tasks → both mark parent as risk path | story.isOnRiskPath=true | ✅ Pass |
| TC-RP-08 | Every node has isOnRiskPath defined | typeof isOnRiskPath === 'boolean' | ✅ Pass |

**Related:** UC-088 (Main Flow steps 1–2), FR-225B, SCN-026, SCN-044, UJ-028 — `riskPath.test.ts`

---

## 9.19 — Work Item Explorer Largest Unfinished Branch (TC-LB-01 to TC-LB-08)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-LB-01 | All done children → largestUnfinishedBranch null or openCount=0 | Null or 0 | ✅ Pass |
| TC-LB-02 | Single open task identified as branch root | branch.rootKey = task key | ✅ Pass |
| TC-LB-03 | Done nodes never marked isLargestBranch=true | false | ✅ Pass |
| TC-LB-04 | Open task in identified branch is marked isLargestBranch | true | ✅ Pass |
| TC-LB-05 | Multiple open branches → one is identified | branch not null | ✅ Pass |
| TC-LB-06 | Insight appears when branch has open items | insights array has entry | ✅ Pass |
| TC-LB-07 | largestUnfinishedBranch has correct shape | rootKey, rootLabel, openCount, totalCount, completionPct present | ✅ Pass |
| TC-LB-08 | All nodes have isLargestBranch as boolean | typeof === 'boolean' | ✅ Pass |

**Related:** UC-088 (Main Flow steps 3–4), FR-225C, SCN-026, SCN-044, UJ-028 — `largestBranch.test.ts`

---

## 9.20 — Work Item Explorer Blocked Branch Filter (TC-BF-01 to TC-BF-08)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-BF-01 | Filter returns only risk-path and blocked nodes | All filtered have isOnRiskPath or isBlocked | ✅ Pass |
| TC-BF-02 | Healthy nodes excluded when filter active | Healthy task not in filtered result | ✅ Pass |
| TC-BF-03 | Blocked node always included | blocked task in filtered result | ✅ Pass |
| TC-BF-04 | Risk-path ancestor included | Parent story in filtered result | ✅ Pass |
| TC-BF-05 | No blocked nodes → filter returns empty | filtered.length === 0 | ✅ Pass |
| TC-BF-06 | blockedCount computed correctly | count ≥ 1 | ✅ Pass |
| TC-BF-07 | riskPathCount reflects isOnRiskPath | filtered.length ≥ riskPathCount | ✅ Pass |
| TC-BF-08 | isBlocked node always passes filter | filtered.some(n => n.isBlocked) | ✅ Pass |

**Related:** UC-088 (Main Flow steps 5–6, Alt Flow A), FR-225D, SCN-044, UJ-028 — `blockedBranchFilter.test.ts`

---

## 9.21 — Release Readiness Checklist (TC-RR-01 to TC-RR-10)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-RR-01 | No Fix Version → hasVersionData=false | false | ✅ Pass |
| TC-RR-02 | All done, no blockers → Go | verdict=Go | ✅ Pass |
| TC-RR-03 | Blocker present → No-Go | verdict=No-Go | ✅ Pass |
| TC-RR-04 | Completion < 70% → No-Go | verdict=No-Go | ✅ Pass |
| TC-RR-05 | 70–89% + open bug → Conditional Go | verdict=Conditional Go | ✅ Pass |
| TC-RR-06 | Multiple versions create separate results | 2 releases | ✅ Pass |
| TC-RR-07 | completionPct correct | 50% | ✅ Pass |
| TC-RR-08 | Checklist has 7 items | length=7 | ✅ Pass |
| TC-RR-09 | Blocking check fails correctly | blocking=true, passed=false | ✅ Pass |
| TC-RR-10 | Sort order: No-Go → Conditional → Go | Correct order | ✅ Pass |

---

## 9.22 — Database Backup and Restore (TC-BK-01 to TC-BK-08)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-BK-01 | Missing manifest → failure | success=false | ✅ Pass |
| TC-BK-02 | Manifest with no version → failure | success=false | ✅ Pass |
| TC-BK-03 | Valid manifest, empty files → success, nothing restored | restored=[] | ✅ Pass |
| TC-BK-04 | Unknown file rejected (security) | skipped, not written | ✅ Pass |
| TC-BK-05 | Allowed filename processed | not skipped | ✅ Pass |
| TC-BK-06 | Backup bundle has correct manifest shape | fields present | ✅ Pass |
| TC-BK-07 | Unknown files in skipped list | skipped contains filename | ✅ Pass |
| TC-BK-08 | Base64 round-trip correct | decoded === original | ✅ Pass |

---

## 9.23 — Production Security Checklist (TC-SC2-01 to TC-SC2-08)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-SC2-01 | runSecurityChecks returns report with checks | Array with > 5 checks | ✅ Pass |
| TC-SC2-02 | Every check has required fields | id, label, status, severity, category, isAuto | ✅ Pass |
| TC-SC2-03 | overallScore is 0–100 | 0 ≤ score ≤ 100 | ✅ Pass |
| TC-SC2-04 | Count fields consistent | pass+fail+warn+manual = total | ✅ Pass |
| TC-SC2-05 | isProductionReady=false when criticalFails > 0 | false | ✅ Pass |
| TC-SC2-06 | Manual checks have isAuto=false | false | ✅ Pass |
| TC-SC2-07 | Auto checks have isAuto=true | true | ✅ Pass |
| TC-SC2-08 | Default SESSION_SECRET → fail status | status=fail | ✅ Pass |

---

## v4.0 Test Cases (2026-06-03)

> **Current test count:** 469 automated tests across 48 test suites (verified 2026-06-07 via `npm test`). The tests below reflect the current state of `src/__tests__/`.

### Automated Test Suites (469 tests, 48 suites)

| Suite file | Tests | Coverage |
|---|---|---|
| `throughput.test.ts` | 11 | Sprint throughput engine, mid-sprint patterns, goal outcomes, FR-215 `ThroughputMetrics` shape contract |
| `relationExplorer.test.ts` | 8 | Hierarchy reconstruction, orphan classification, graph builder |
| `auth.test.ts` | 17 | Login, logout, inactive register, session, rate limit, password hash |
| `adminUsers.test.ts` | 5 | Admin user API access control, user creation with first-login password change, role/status updates, delete-user self-protection, cloud sync/push |
| `profileImage.test.ts` | 2 | S3-backed profile image upload validation, avatar URL update, and cloud sync |
| `roles.test.ts` | 7 | Supported roles, labels, import visibility scope, dashboard default view mapping, role view locking, route matrix/fallbacks including change-password access |
| `excelExport.test.ts` | 15 | 17-sheet workbook structure, recommendation engine, formula correctness |
| `dataQuality.test.ts` | 12 | Data Quality Score, field checks, band classification |
| `metricConfidence.test.ts` | 14 | Confidence levels per KPI, missing-field detection |
| `missingFieldImpact.test.ts` | 12 | Per-field impact descriptors, affected sections |
| `dataRetention.test.ts` | 10 | Retention period enforcement, auto-delete logic |
| `deleteHistory.test.ts` | 10 | Import log and snapshot deletion, ownership checks |
| `onboarding.test.ts` | 10 | Onboarding step tracking, completion detection |
| `dashboardView.test.ts` | 11 | Role-based view section visibility and assigned-role view locking |
| `customerView.test.ts` | 8 | Customer view data shape, no technical fields exposed |
| `snapshots.test.ts` | 8 | Save, list, load, delete, max-20 limit |
| `snapshotComparison.test.ts` | 8 | Delta computation, direction indicators, same-data detection |
| `trends.test.ts` | 10 | Trend data shape, 30-upload limit, metric extraction |
| `whatChanged.test.ts` | 10 | Delta vs previous upload, narrative generation |
| `thresholds.test.ts` | 10 | Threshold read/write, cache invalidation, apply to metrics |
| `orphanRules.test.ts` | 11 | Orphan rule evaluation, exempt types, sub-task flag |
| `mutedRecommendations.test.ts` | 10 | Mute/snooze persistence, expiry, restore |
| `riskPath.test.ts` | 8 | Risk-path walk, edge marking, done-node exclusion |
| `largestBranch.test.ts` | 8 | Largest unfinished branch detection, completion % |
| `blockedBranchFilter.test.ts` | 8 | Blocked branch filter, graph dimming |
| `releaseReadiness.test.ts` | 10 | Go/Conditional Go/No-Go, 7-item checklist |
| `backup.test.ts` | 8 | Backup archive creation, restore with .bak safety copy |
| `cloudRestoreHardening.test.ts` | 4 | Latest metrics first-run response, backup inclusion, pending-push restore guard, localStorage fallback |
| `storageSettingsPersistence.test.ts` | 3 | Redacted cloud settings preserve saved provider credentials; explicit replacement still works |
| `securityCheck.test.ts` | 8 | Automated checks, score, production-ready flag |
| `columnMapping.test.ts` | 10 | Column preview, mapped/aliased/unrecognised classification |
| `metrics.test.ts` | ~530 | Core metrics calculation, health scoring, prediction |
| `explorerExport.test.ts` | 11 | Explorer Excel workbook structure, sheet content, CSV correctness |
| `releaseConfidenceTrend.test.ts` | 10 | Release confidence score formula, band classification, edge cases |
| `teamHealth.test.ts` | 10 | Team health score formula, band, blocked/critical detection, sorting, edge cases |
| `portfolioHealth.test.ts` | 10 | Portfolio score formula, epic/project mapping, band thresholds, edge cases |
| `executivePdf.test.ts` | 8 | HTML structure, health/score/epic/assignee content, print CSS, XSS escaping |
| `recOwners.test.ts` | 8 | Owner get/set/clear/overwrite, whitespace trimming, empty-string removal, clearAll |
| `diagnostics.test.ts` | 8 | Ops score formula, env penalty weights, import penalty cap, success rate, uptime format |
| `productTour.test.ts` | 8 | Step structure, first/last step IDs, dismiss/complete/reset state, targetId validation |
| `themeCustomizer.test.ts` | 8 | Preset counts, hex/shadow formats, load/save/reset, invalid-value fallback, defaults |
| `layoutBuilder.test.ts` | 9 | Default layout, load/save/reset, moveUp/moveDown, toggleVisibility, getHiddenKeys, getOrderedVisibleSections |
| `chartCustomizer.test.ts` | 9 | Registry count, defaults, load/save/reset, moveUp/moveDown, toggleVisible, setSpan, isDefault detection |
| `cloudStorage.test.ts` | 8 | Settings defaults/persist/merge, PROVIDER_INFO, installCmds, LocalProvider.test() |

### 9.1 — Data Quality Score Tests (TC-DQ-01 to TC-DQ-12)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-DQ-01 | All 10 fields present and populated → Excellent | score ≥ 90, band = Excellent | ✅ Pass |
| TC-DQ-02 | Missing Done Date and In Progress Date → reduced score | score < 75 | ✅ Pass |
| TC-DQ-03 | All fields absent → Critical band | score < 25, band = Critical | ✅ Pass |
| TC-DQ-04 | Score normalises to 0–100 range | 0 ≤ score ≤ 100 | ✅ Pass |
| TC-DQ-05 | Field breakdown includes all 10 fields | fieldBreakdown.length === 10 | ✅ Pass |
| TC-DQ-06 | Fill rate computed correctly per field | fillRate = present/total | ✅ Pass |
| TC-DQ-07 | Summary message is non-empty string | typeof summary === 'string' | ✅ Pass |
| TC-DQ-08 | Band = Good at 80% score | band = 'Good' | ✅ Pass |
| TC-DQ-09 | Band = Fair at 60% score | band = 'Fair' | ✅ Pass |
| TC-DQ-10 | Band = Poor at 35% score | band = 'Poor' | ✅ Pass |
| TC-DQ-11 | Empty dataset → score = 0, band = Critical | score = 0 | ✅ Pass |
| TC-DQ-12 | Partial fill (50% of issues have Sprint) → correct fillRate | fillRate = 0.50 | ✅ Pass |

### 9.2 — Metric Confidence Score Tests (TC-MC-01 to TC-MC-14)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-MC-01 | All required fields present → High confidence | level = 'High' | ✅ Pass |
| TC-MC-02 | Missing In Progress Date → Low CycleTime confidence | CycleTime.level = 'Low' | ✅ Pass |
| TC-MC-03 | Missing Sprint → N/A SprintThroughput | SprintThroughput.level = 'N/A' | ✅ Pass |
| TC-MC-04 | Confidence map covers all major KPIs | keys.includes('SprintThroughput', 'CycleTime', ...) | ✅ Pass |
| TC-MC-05 | missingFields list populated correctly | missingFields contains field names | ✅ Pass |
| TC-MC-06 | reason is non-empty string | reason.length > 0 | ✅ Pass |
| TC-MC-07 | Unreliable when critical fields absent | level = 'Unreliable' | ✅ Pass |
| TC-MC-08 | Medium when 70–99% required fields present | level = 'Medium' | ✅ Pass |
| TC-MC-09 | N/A when metric is inapplicable | level = 'N/A' | ✅ Pass |
| TC-MC-10 | Sample size too small → Low | level = 'Low' | ✅ Pass |
| TC-MC-11 | Full dataset → High for most KPIs | most levels = 'High' | ✅ Pass |
| TC-MC-12 | LeadTime requires Created Date | missing Created Date → Low | ✅ Pass |
| TC-MC-13 | KanbanFlow requires In Progress Date | missing → Low | ✅ Pass |
| TC-MC-14 | Output structure has level, reason, missingFields | shape correct | ✅ Pass |

### 9.3 — Missing-Column Impact Tests (TC-MI-01 to TC-MI-12)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-MI-01 | Missing Sprint → Sprint Throughput impacted | impacts includes 'SprintThroughput' | ✅ Pass |
| TC-MI-02 | Missing Done Date → Lead/Cycle Time impacted | impacts include timing metrics | ✅ Pass |
| TC-MI-03 | Missing Story Points → point metrics degraded | impacts include 'StoryPoints' | ✅ Pass |
| TC-MI-04 | Impact descriptor has what/gain/section fields | shape correct | ✅ Pass |
| TC-MI-05 | No missing fields → empty impacts array | impacts.length === 0 | ✅ Pass |
| TC-MI-06 | Missing Epic Link → orphan ratio increases | noted in impacts | ✅ Pass |
| TC-MI-07 | Missing Assignee → capacity chart impacted | impacts include 'Capacity' | ✅ Pass |
| TC-MI-08 | Missing In Progress Date → Kanban flow degraded | impacts include 'KanbanFlow' | ✅ Pass |
| TC-MI-09 | Each impact has a plain-English description | description.length > 0 | ✅ Pass |
| TC-MI-10 | 53 optional fields covered in impact map | map has entries for all optional fields | ✅ Pass |
| TC-MI-11 | Missing Due Date → Overdue detection affected | noted in impacts | ✅ Pass |
| TC-MI-12 | Missing Labels → label analytics impacted | impacts include 'Labels' | ✅ Pass |

### Planned Test Cases — P1 UX Features (TC-CLD, TC-SD, TC-DS)

> These are planned test cases for features not yet implemented (P1.2 Clear Local Data, P1.3 Dashboard Section Switcher).

#### Clear Local Data (TC-CLD-01 to TC-CLD-05) — Planned

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-CLD-01 | clearDeliveryData() removes all dc_ prefixed localStorage keys | no dc_ keys remain | ❌ Planned |
| TC-CLD-02 | clearDeliveryData() removes all dc_ prefixed sessionStorage keys | no dc_ keys remain | ❌ Planned |
| TC-CLD-03 | clearDeliveryData() does not remove non-dc_ keys | other keys unaffected | ❌ Planned |
| TC-CLD-04 | After clear, hasMetrics() returns false | false | ❌ Planned |
| TC-CLD-05 | Confirmation modal shown before clearing | modal visible before action | ❌ Planned |

#### Upload Page Stored-Data Detection (TC-SD-01 to TC-SD-04) — Planned

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-SD-01 | hasMetrics() returns true when dc_metrics exists | true | ❌ Planned |
| TC-SD-02 | Upload page shows detection notice when stored data found | notice rendered | ❌ Planned |
| TC-SD-03 | Upload page shows no notice when no stored data | no notice rendered | ❌ Planned |
| TC-SD-04 | Clear button on Upload page triggers confirmation modal | modal opens | ❌ Planned |

#### Dashboard Section Switcher (TC-DS-01 to TC-DS-08) — Planned

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-DS-01 | Default view mode is 'overview' | viewMode = 'overview' | ❌ Planned |
| TC-DS-02 | Clicking section button sets activeSection and viewMode to 'single' | state updated | ❌ Planned |
| TC-DS-03 | Full View button sets viewMode to 'full' | viewMode = 'full' | ❌ Planned |
| TC-DS-04 | Non-active sections have hidden CSS class in single mode | hidden sections not visible | ❌ Planned |
| TC-DS-05 | Active section button has highlighted style | active class applied | ❌ Planned |
| TC-DS-06 | scrollIntoView called on section click | scroll invoked | ❌ Planned |
| TC-DS-07 | Reduced-motion: no transition classes applied | transition-none applied | ❌ Planned |
| TC-DS-08 | Role-based view selection still works with section switcher | views remain functional | ❌ Planned |


---

## New Test Cases — v4.1 UX Design System (TC-CLD, TC-DS, TC-GH, TC-EV)

### TC-CLD — Clear Local Data (10 passing)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-CLD-01 | hasLocalData returns false when no DC data | false | ✅ Pass |
| TC-CLD-02 | hasLocalData returns true for dc_metrics_v2 | true | ✅ Pass |
| TC-CLD-03 | hasLocalData returns true for any DC key | true | ✅ Pass |
| TC-CLD-04 | hasLocalData detects dc_col_order_* prefix | true | ✅ Pass |
| TC-CLD-05 | hasLocalData ignores non-DC keys | false | ✅ Pass |
| TC-CLD-06 | clearLocalData removes all fixed DC keys | keys absent | ✅ Pass |
| TC-CLD-07 | clearLocalData removes dc_col_order_* keys | dynamic keys absent | ✅ Pass |
| TC-CLD-08 | clearLocalData preserves non-DC keys | unrelated keys remain | ✅ Pass |
| TC-CLD-09 | hasLocalData returns false after clearLocalData | false | ✅ Pass |
| TC-CLD-10 | clearLocalData clears sessionStorage DC keys | session keys absent | ✅ Pass |

### TC-DS — Dashboard Section Switcher (10 passing)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-DS-01 | Full mode respects expandedSections | only expanded sections visible | ✅ Pass |
| TC-DS-02 | Overview mode shows only overview/attention/recommendations | 3 sections visible | ✅ Pass |
| TC-DS-03 | Single-section mode shows only targeted section | 1 section visible | ✅ Pass |
| TC-DS-04 | Role-hidden sections remain hidden in all modes | hidden sections not rendered | ✅ Pass |
| TC-DS-05 | sectionHeaderVisible hides role-hidden sections | header not rendered | ✅ Pass |
| TC-DS-06 | sectionHeaderVisible hides off-mode sections | non-active headers hidden | ✅ Pass |
| TC-DS-07 | Overview mode ignores expandedSections | always visible | ✅ Pass |
| TC-DS-08 | DASHBOARD_SECTIONS has exactly 14 entries | length = 14 | ✅ Pass |
| TC-DS-09 | OVERVIEW_KEYS contains overview/attention/recommendations | size = 3 | ✅ Pass |
| TC-DS-10 | All sections have unique keys and valid sectionIds | no duplicates | ✅ Pass |

### TC-GH — Glossary/Help Sticky Nav (Planned)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-GH-01 | Sticky nav bar present on /glossary | nav#glossary-nav visible | ❌ Planned |
| TC-GH-02 | Clicking section tab scrolls to that section | section in view | ❌ Planned |
| TC-GH-03 | IntersectionObserver highlights active tab | active tab blue | ❌ Planned |
| TC-GH-04 | Back to Top button scrolls to top | window.scrollY = 0 | ❌ Planned |
| TC-GH-05 | Help search bar in sticky nav filters sections | non-matching sections hidden | ❌ Planned |

### TC-EV — Executive View Access Control (Planned)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-EV-01 | Executive view hides filter row | filter row not in DOM | ❌ Planned |
| TC-EV-02 | Executive view hides Show filters button | button not rendered | ❌ Planned |
| TC-EV-03 | Executive view hides flow health panel | panel not in DOM | ❌ Planned |
| TC-EV-04 | KPI cards have no onClick in Executive view | no cursor-pointer | ❌ Planned |
| TC-EV-05 | Full view shows filter row and flow panel | both visible | ❌ Planned |

| TC-RH-01 | getRecHistory returns [] initially | empty array | ✅ Pass |
| TC-RH-02 | saveRecSnapshot saves first entry | 1 entry, correct data | ✅ Pass |
| TC-RH-03 | Duplicate recs do not create new snapshot | still 1 entry | ✅ Pass |
| TC-RH-04 | Changed recs create new snapshot | 2 entries | ✅ Pass |
| TC-RH-05 | History capped at 10 entries | length ≤ 10 | ✅ Pass |
| TC-RH-06 | getNewTitles identifies newly appeared recs | correct set | ✅ Pass |
| TC-RH-07 | getResolvedRecs identifies fixed recs | correct list | ✅ Pass |
| TC-RH-08 | clearRecHistory empties all entries | length = 0 | ✅ Pass |

---

## 9.29 — Work Item Explorer Export (TC-EX-01 to TC-EX-08)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-EX-01 | Workbook has exactly 5 sheets | SheetNames.length === 5 | ✅ Pass |
| TC-EX-01b | Sheet names match expected names | 01 Summary … 05 Insights | ✅ Pass |
| TC-EX-02 | Summary sheet contains focus key and delivery confidence | PROJ-1, 65%, 40% | ✅ Pass |
| TC-EX-02b | Summary sheet contains largest branch info | PROJ-2, 33% | ✅ Pass |
| TC-EX-03 | All Issues sheet has 4 data rows (3 nodes + 1 orphan) | rows.length = 5 (header + 4) | ✅ Pass |
| TC-EX-03b | All Issues sheet has required columns | Key, Health, On Risk Path, Orphan | ✅ Pass |
| TC-EX-04 | Risk Items sheet contains only blocked/critical/risk-path nodes | PROJ-3 in; PROJ-2 and PROJ-4 out | ✅ Pass |
| TC-EX-05 | Orphans sheet contains only orphan nodes | PROJ-4 in; PROJ-1/2/3 out | ✅ Pass |
| TC-EX-06 | Insights sheet contains insight text | both insight strings present | ✅ Pass |
| TC-EX-07 | Empty risk/orphan/insights shows placeholder row | no crash, placeholder text | ✅ Pass |
| TC-EX-08 | No HTML tags or \`[object Object]\` in any cell | clean text values only | ✅ Pass |

---

## 9.30 — Release Confidence Trend (TC-RC-01 to TC-RC-10)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-RC-01 | Perfect delivery (100% done, 0 blockers) → score = 100 | score = 100 | ✅ Pass |
| TC-RC-02 | Zero completion → score = 45 (non-completion points still awarded) | score = 45 | ✅ Pass |
| TC-RC-03 | Blocked issues reduce score proportionally | withBlockers < noBlockers | ✅ Pass |
| TC-RC-04 | All issues blocked → full 25-pt penalty vs none blocked | diff = 25 | ✅ Pass |
| TC-RC-05 | Each open defect costs 2 pts; 4+ defects caps at 8-pt loss | diff 1 defect = 2, diff 4 defects = 8 | ✅ Pass |
| TC-RC-06 | Score clamped to 0–100 under any input | 0 ≤ score ≤ 100 | ✅ Pass |
| TC-RC-07 | Band: ≥80 = High, ≥60 = Medium, ≥40 = Low, <40 = Critical | all thresholds correct | ✅ Pass |
| TC-RC-08 | totalIssues = 0 does not throw (division guard) | no exception | ✅ Pass |
| TC-RC-09 | Typical mid-sprint scenario scores 50–80 | 50 ≤ score ≤ 80 | ✅ Pass |
| TC-RC-10 | Improving uploads produce monotonically increasing scores | sprint1 < sprint2 < sprint3 | ✅ Pass |

---

## 9.31 — Team-Level Health Comparison (TC-TH-01 to TC-TH-10)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-TH-01 | Fully done team → score = 100, band = Healthy | score = 100 | ✅ Pass |
| TC-TH-02 | All critical items → Critical band | score < 40, band = Critical | ✅ Pass |
| TC-TH-03 | Blocked reason counted in blockedCount | blockedCount = 2 | ✅ Pass |
| TC-TH-04 | Done-status items excluded from critical/blocked counts | both = 0 | ✅ Pass |
| TC-TH-05 | Results sorted by healthScore descending | Alice first, Bob last | ✅ Pass |
| TC-TH-06 | avgOpenAgeDays = mean of open-item ageDays | avg = 15 | ✅ Pass |
| TC-TH-07 | Done items excluded from avgOpenAgeDays | avg = 10 (not 100) | ✅ Pass |
| TC-TH-08 | Empty capacity → empty result array | [] | ✅ Pass |
| TC-TH-09 | healthScore clamped 0–100 under worst inputs | 0 ≤ score ≤ 100 | ✅ Pass |
| TC-TH-10 | teamBandColor and teamBandBg return correct hex values | all correct | ✅ Pass |

---

## 9.32 — Cross-Team Portfolio Summary (TC-PF-01 to TC-PF-10)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-PF-01 | portfolioScore is between 0 and 100 | 0 ≤ score ≤ 100 | ✅ Pass |
| TC-PF-02 | Epics mapped with correct fields and health | Epic Alpha=warning, Beta=critical, Gamma=good | ✅ Pass |
| TC-PF-03 | Projects mapped with correct health bands | PROJ-A=good (80%), PROJ-B=warning (50%) | ✅ Pass |
| TC-PF-04 | "No date" quarter excluded from quarters array | quarters.length = 1 | ✅ Pass |
| TC-PF-05 | atRiskEpics counts epics with critical > 0 | atRiskEpics = 1 | ✅ Pass |
| TC-PF-06 | healthyProjects counts projects with completionRate ≥ 70 | healthyProjects = 1, atRiskProjects = 1 | ✅ Pass |
| TC-PF-07 | insights array has at least one string entry | length ≥ 1 | ✅ Pass |
| TC-PF-08 | Perfect delivery → score = 100, band = Excellent | score = 100, band = Excellent | ✅ Pass |
| TC-PF-09 | Empty epics/projects fall back to overall completionRate | no throw, valid score | ✅ Pass |
| TC-PF-10 | portfolioBand thresholds and portfolioBandColor values correct | all correct | ✅ Pass |

---

## 9.33 — Executive One-Page PDF Export (TC-EP-01 to TC-EP-08)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-EP-01 | buildExecutivePdfHtml returns valid HTML string | typeof html = string, contains DOCTYPE + /html | ✅ Pass |
| TC-EP-02 | HTML contains health score (72) and band label (Moderate) | both present | ✅ Pass |
| TC-EP-03 | HTML contains completion rate (70%) | present | ✅ Pass |
| TC-EP-04 | HTML contains epic names (Payments v2, Auth Revamp) | both present | ✅ Pass |
| TC-EP-05 | HTML contains assignee names (Alice, Bob) | both present | ✅ Pass |
| TC-EP-06 | HTML contains insight text | both insight strings present | ✅ Pass |
| TC-EP-07 | HTML contains @media print, @page, print-color-adjust | all present | ✅ Pass |
| TC-EP-08 | XSS: \<script\> in epic name is escaped to &lt;script&gt; | no raw \<script\> tag in output | ✅ Pass |

---

## 9.34 — Action-Owner Assignment (TC-AO-01 to TC-AO-08)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-AO-01 | getRecOwner returns '' when no owner set | '' | ✅ Pass |
| TC-AO-02 | setRecOwner saves owner, getRecOwner retrieves it | 'Alice' | ✅ Pass |
| TC-AO-03 | setRecOwner trims leading/trailing whitespace | '  Bob  ' → 'Bob' | ✅ Pass |
| TC-AO-04 | setRecOwner with empty string removes the entry | '' → getRecOwner returns '' | ✅ Pass |
| TC-AO-05 | clearRecOwner removes one key, leaves others | key-1 cleared, key-2 intact | ✅ Pass |
| TC-AO-06 | getAllRecOwners returns all assigned owners as a map | 2 entries, correct values | ✅ Pass |
| TC-AO-07 | clearAllRecOwners wipes all stored owners | all empty after clear | ✅ Pass |
| TC-AO-08 | setRecOwner overwrites previous owner | 'Alice' → 'Carol' | ✅ Pass |

---

## 9.35 — Deployment Guide (TC-DG-01 to TC-DG-05, Manual)

> These are manual verification steps — no automated tests for deployment docs.

| ID | Check | Expected | Status |
|----|-------|----------|--------|
| TC-DG-01 | `docker compose up -d --build` completes without error | Container healthy after 60s | Manual |
| TC-DG-02 | App accessible at `http://localhost:3000` after Docker start | Login page loads | Manual |
| TC-DG-03 | Upload a 5 MB Jira CSV via nginx proxy with `client_max_body_size 25M` | Upload succeeds, no 413 | Manual |
| TC-DG-04 | SSL certificate issued via Certbot — site loads on HTTPS | Valid cert, no browser warning | Manual |
| TC-DG-05 | PM2 restarts app after `pm2 kill` on VPS deployment | App auto-restarts | Manual |

---

## 9.36 — System Diagnostics (TC-SD-01 to TC-SD-10)

> Note: `TC-SD-01`–`04` in this section share IDs with an unrelated, separately-tracked "Upload Page Stored-Data Detection" block earlier in this document (§ "TC-SD-01 to TC-SD-04 — Planned"). Pre-existing ID collision, not introduced by this pass — out of scope here; flagged for a future `TRACE` cleanup pass.

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-SD-01 | All env checks pass + no failures → ops score = 100 | score = 100 | ✅ Pass |
| TC-SD-02 | Missing SESSION_SECRET reduces score by 30 | delta = 30 | ✅ Pass |
| TC-SD-03 | Non-production NODE_ENV reduces score by 10 | delta = 10 | ✅ Pass |
| TC-SD-04 | Open registration reduces score by 10 | delta = 10 | ✅ Pass |
| TC-SD-05 | Failed imports penalise score (1 each, max 10) | 1 fail=−1, 20 fails=−10 | ✅ Pass |
| TC-SD-06 | Ops score clamped at 0 under worst inputs | score ≥ 0 | ✅ Pass |
| TC-SD-07 | Success rate = successful/total × 100, 0/0=0 | all correct | ✅ Pass |
| TC-SD-08 | Uptime: 30s→"30s", 90s→"1m", 3661s→"1h 1m", 90000s→"1d 1h" | all correct | ✅ Pass |
| TC-SD-09 | Latest-metrics age: null when no snapshot, else minutes since `savedAt` (30m, 1440m for 1 day) | all correct | ✅ Pass |
| TC-SD-10 | Cloud backup freshness picks newest by `lastModified`, not list order; empty list → null | newest key selected, `null` for `[]` | ✅ Pass |

---

## 9.39 — Product Tour (TC-PT-01 to TC-PT-08)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-PT-01 | TOUR_STEPS has ≥ 8 steps, each with id + title + description | all well-formed | ✅ Pass |
| TC-PT-02 | First step = welcome, last step = done | correct | ✅ Pass |
| TC-PT-03 | isTourDismissed returns false initially | false | ✅ Pass |
| TC-PT-04 | dismissTour sets dismissed flag | isTourDismissed = true | ✅ Pass |
| TC-PT-05 | isTourCompleted returns false initially | false | ✅ Pass |
| TC-PT-06 | completeTour sets both completed and dismissed | both = true | ✅ Pass |
| TC-PT-07 | resetTour clears both flags | both = false | ✅ Pass |
| TC-PT-08 | targetId values match known dashboard section IDs | all known | ✅ Pass |

---

## 9.40 — Advanced Theme Customization (TC-TC-01 to TC-TC-08)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-TC-01 | ACCENT_PRESETS has 7 entries, each with valid hex/hover/shadow | all valid | ✅ Pass |
| TC-TC-02 | RADIUS_PRESETS has sharp/default/rounded with md+lg values | all valid | ✅ Pass |
| TC-TC-03 | FONT_SIZE_PRESETS has sm/md/lg with px string values | all valid | ✅ Pass |
| TC-TC-04 | loadThemeCustom returns DEFAULT_THEME when storage empty | blue/default/md | ✅ Pass |
| TC-TC-05 | saveThemeCustom persists; loadThemeCustom retrieves purple/rounded/lg | correct | ✅ Pass |
| TC-TC-06 | resetThemeCustom clears saved settings → defaults returned | defaults | ✅ Pass |
| TC-TC-07 | loadThemeCustom falls back to defaults for invalid stored values | defaults | ✅ Pass |
| TC-TC-08 | DEFAULT_THEME: accent=blue, radius=default, fontSize=md; blue hex=#2563eb | correct | ✅ Pass |

---

## 9.41 — Custom Dashboard Layout Builder (TC-LB2-01 to TC-LB2-08)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-LB2-01 | getDefaultLayout returns all 14 sections all visible | length=14, all visible | ✅ Pass |
| TC-LB2-02 | getLayoutPrefs returns defaults when storage empty | defaults | ✅ Pass |
| TC-LB2-03 | saveLayoutPrefs persists; getLayoutPrefs retrieves custom value | visible[0]=false | ✅ Pass |
| TC-LB2-04 | resetLayoutPrefs clears to all-visible defaults | visible[2]=true | ✅ Pass |
| TC-LB2-05 | moveUp swaps entry with the one above; first entry is no-op | swapped correctly | ✅ Pass |
| TC-LB2-06 | moveDown swaps entry with the one below | swapped correctly | ✅ Pass |
| TC-LB2-07 | toggleVisibility flips visible flag for one key only | key[3]=false, others true | ✅ Pass |
| TC-LB2-08 | getHiddenKeys returns keys with visible=false; getOrderedVisibleSections excludes them | kanban hidden, length=13 | ✅ Pass |

---

## 9.42 — Advanced Chart Customization (TC-CC-01 to TC-CC-08)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-CC-01 | CHART_REGISTRY has 11 entries, each with id/label/icon/defaultSpan (1/2/3) | all valid | ✅ Pass |
| TC-CC-02 | getDefaultChartPrefs returns 11 prefs all visible with correct spans | all visible, spans match registry | ✅ Pass |
| TC-CC-03 | getChartPrefs returns defaults when storage empty | isDefault = true | ✅ Pass |
| TC-CC-04 | saveChartPrefs persists; getChartPrefs retrieves visible=false, span=3 | correct values | ✅ Pass |
| TC-CC-05 | resetChartPrefs clears to defaults | isDefault = true | ✅ Pass |
| TC-CC-06 | chartMoveUp swaps with one above; no-op at index 0 | swapped correctly | ✅ Pass |
| TC-CC-06b | chartMoveDown swaps with one below | swapped correctly | ✅ Pass |
| TC-CC-07 | chartToggleVisible flips one entry only; chartSetSpan changes span for one | correct | ✅ Pass |
| TC-CC-08 | isDefaultChartPrefs returns false when visibility or span changed | detected correctly | ✅ Pass |

---

## P3-01 — Cloud Storage Integration (TC-CS-01 to TC-CS-08)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-CS-01 | readStorageSettings returns defaults when no file | active=local, all providers present | ✅ Pass |
| TC-CS-02 | writeStorageSettings persists; readStorageSettings retrieves S3 bucket+region | correct values | ✅ Pass |
| TC-CS-03 | PROVIDER_INFO has local/s3/azure/gcp entries, each with label/icon/description | all valid | ✅ Pass |
| TC-CS-04 | s3/azure/gcp have installCmd; local has empty installCmd | correct per provider | ✅ Pass |
| TC-CS-05 | Partial S3 config saved; merged with defaults on read | bucket=test-bucket preserved | ✅ Pass |
| TC-CS-06 | StorageSettings has active, local, s3, azure, gcp fields | all present | ✅ Pass |
| TC-CS-07 | readStorageSettings merges partial saved with DEFAULTS | azure.containerName preserved | ✅ Pass |
| TC-CS-08 | LocalStorageProvider.test() returns { ok: true }; type='local' | correct | ✅ Pass |
| TC-CS-09 | `/api/metrics/latest` returns HTTP 200 with `available:false` when `latest-metrics.json` is absent | no console 404; browser can fall back | ✅ Automated — `cloudRestoreHardening.test.ts` |
| TC-CS-10 | Backup bundles include `data/latest-metrics.json` when present | latest dashboard payload included in next cloud backup | ✅ Automated — `cloudRestoreHardening.test.ts` |
| TC-CS-11 | `syncFromCloud()` does not pull over pending local changes | `pendingPush=true` returns local fallback result | ✅ Automated — `cloudRestoreHardening.test.ts` |
| TC-CS-12 | `loadMetricsWithSource()` falls back to `dc_metrics_v2` when bucket/server metrics are unavailable | source metadata saved to `dc_metrics_source_v1` | ✅ Automated — `cloudRestoreHardening.test.ts` |
| TC-CS-13 | Admin storage save receives redacted/blank S3 credential fields | saved S3 access key and secret are preserved; `hasCredentials` is not persisted | ✅ Automated — `storageSettingsPersistence.test.ts` |
| TC-CS-14 | Admin storage save receives explicit replacement S3 credentials | saved S3 credentials are replaced intentionally | ✅ Automated — `storageSettingsPersistence.test.ts` |
| TC-CS-15 | Admin storage save receives redacted/blank Azure/GCP secret fields | saved connection string and service-account values are preserved | ✅ Automated — `storageSettingsPersistence.test.ts` |

---

## v4.3 — System Error Observability (TC-SE-01 to TC-SE-05)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-SE-01 | `GET /api/admin/system-errors` with admin session returns paginated list | `{ errors: [], total: 0 }` when no errors logged | ✅ Manual |
| TC-SE-02 | `logSystemError()` writes a row; re-reading `SystemErrorLog` returns it with correct errorCode | row present, errorCode matches | ✅ Manual |
| TC-SE-03 | `safeAuditEvent()` called with deleted userId (simulated P2003) creates record with `userId: null` and logs to `SystemErrorLog` | AuditEvent.userId = null; SystemErrorLog.resolution = 'auto-fixed' | ✅ Manual |
| TC-SE-04 | `POST /api/admin/system-errors?action=retry` with a valid id re-runs payload; resolution updated to 'retried' | response `{ result: '...' }`; retryCount incremented | ✅ Manual |
| TC-SE-05 | `PATCH /api/admin/system-errors` with `{ all: true }` sets resolvedAt on all unresolved entries | all resolvedAt not null | ✅ Manual |

---

## v4.3 — Bulk User Operations (TC-BU-01 to TC-BU-04)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-BU-01 | Admin selects all users via header checkbox; own row excluded from selection | all other rows checked; own row unchecked | ✅ Manual |
| TC-BU-02 | Bulk delete with 2 users selected — confirm dialog shown, then DELETE called for each | users removed from list; pending UserAddRequests for those emails cancelled | ✅ Manual |
| TC-BU-03 | Bulk role change to `manager` with 3 users selected | all 3 rows updated to role = 'manager' | ✅ Manual |
| TC-BU-04 | Bulk delete attempted without confirmation (dialog cancelled) | no DELETE request sent; users remain in list | ✅ Manual |

---

## v4.3 — Ghost Session Protection (TC-GS-01 to TC-GS-03)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-GS-01 | `POST /api/user-add-requests` with session belonging to deleted user | HTTP 401 "Your account no longer exists" | ✅ Manual |
| TC-GS-02 | Admin deletes user who has a `pending` UserAddRequest | UserAddRequest.status = 'cancelled' before user row deleted | ✅ Manual |
| TC-GS-03 | `withDbRetry()` called with a P2003 error function | no retry attempted; error logged to SystemErrorLog immediately | ✅ Manual |

---

## 9.43 — Admin User Management, Member Directory, Forced Password Change (TC-AU, TC-MD, TC-PW)

*(Added 2026-06-07 to close TRACE-01 traceability gaps for F3-14, F3-15, F3-16 — see TODO-List.md Section 12 and UC-084/UC-085/UC-086. All 14 previously-❌-Not-Run rows below were automated on 2026-06-07 in `adminUsers.test.ts`, `members.test.ts`, `middleware.test.ts`, and `changePassword.test.ts`.)*

### Admin User Management (implemented in `src/__tests__/adminUsers.test.ts`)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-AU-01 | Admin users API blocks non-admin users | `GET/POST/PATCH/DELETE /api/admin/users` return 403 for `session.role !== 'admin'` | ✅ Automated — `adminUsers.test.ts` |
| TC-AU-02 | Admin users API creates a role-assigned user without returning password hash | 201, `user.role` matches selection, `passwordHash` absent from response (`safeUser()`) | ✅ Automated — `adminUsers.test.ts` |
| TC-AU-03 | Admin users API updates role and active state | `PATCH` returns 200 with updated `role`/`isActive`; `admin_user_update` audit event written | ✅ Automated — `adminUsers.test.ts` |
| TC-AU-04 | Admin users API blocks deleting the signed-in admin | `DELETE` with `id === session.userId` returns 400 "You cannot delete your own account."; no deletion | ✅ Automated — `adminUsers.test.ts` |
| TC-AU-05 | Admin users API deletes another user and syncs cloud data | `DELETE` returns 200 with `deletedUserId`; `admin_user_delete` audit event written; `pushUsersToCloudIfConfigured()` invoked | ✅ Automated — `adminUsers.test.ts` |
| TC-AU-06 | Admin users API blocks disabling the signed-in admin | `PATCH` with `id === session.userId` and `isActive: false` returns 400; account remains active | ✅ Automated — `adminUsers.test.ts` ("admin users API blocks disabling the signed-in admin") |
| TC-AU-07 | Admin users API rejects duplicate email on create | `POST` with an existing email returns 409 "An account with this email already exists"; no user created | ✅ Automated — `adminUsers.test.ts` ("admin users API rejects duplicate email on create") |

### Member Directory (route-access in `src/__tests__/roles.test.ts`; route + search/contact-email logic in `src/__tests__/members.test.ts`)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-MD-01 | `canAccessRoute('scrum_master', '/members')` | `true` | ✅ Automated — `roles.test.ts` |
| TC-MD-02 | `canAccessRoute('product_owner', '/members')` | `true` | ✅ Automated — `roles.test.ts` |
| TC-MD-03 | `canAccessRoute('c_level', '/members')` | `true` | ✅ Automated — `roles.test.ts` |
| TC-MD-04 | `canAccessRoute('admin', '/members')` | `true` | ✅ Automated — `roles.test.ts` |
| TC-MD-05 | `GET /api/members` returns only active users sorted by name | `isActive: false` accounts excluded; alphabetical order | ✅ Automated — `members.test.ts` ("TC-MD-05: members API queries only active users ordered by name ascending") |
| TC-MD-06 | Member search filters across name/email/position/roleLabel/bio | Matching narrows the visible set, case-insensitive | ✅ Automated — `members.test.ts` ("TC-MD-06: search query matches across name, email, position, role label, and bio") via `matchesMemberQuery()` extracted to `src/lib/members.ts` |
| TC-MD-07 | Member detail popup falls back to account email when `contactEmail` is empty | Popup displays `email` via `contactEmailFor()` | ✅ Automated — `members.test.ts` ("TC-MD-07: contactEmailFor falls back to account email when contactEmail is empty") via `src/lib/members.ts` |
| TC-MD-08 | Anonymous user visiting `/members` is redirected to `/login` | `GET /api/members` 401 → client `router.replace('/login')` | ✅ Automated — `members.test.ts` ("TC-MD-08: members API rejects anonymous requests with 401 (drives client redirect to /login)"); the route-level 401 contract that the page's redirect depends on is asserted directly — full client-side render/redirect would additionally need React component-test infrastructure (`@testing-library/react` + jsdom), which this project does not currently use |

### Forced First-Login Password Change (route-access in `roles.test.ts`; strength/hash logic in `auth.test.ts`; redirect in `middleware.test.ts`; route flow in `changePassword.test.ts`)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-PW-01 | `canAccessRoute('scrum_master', '/change-password')` | `true` | ✅ Automated — `roles.test.ts` |
| TC-PW-02 | `canAccessRoute('product_owner', '/change-password')` | `true` | ✅ Automated — `roles.test.ts` |
| TC-PW-03 | `canAccessRoute('manager', '/change-password')` | `true` | ✅ Automated — `roles.test.ts` |
| TC-PW-04 | `canAccessRoute('c_level', '/change-password')` | `true` | ✅ Automated — `roles.test.ts` |
| TC-PW-05 | `validatePasswordStrength` rejects short/no-uppercase/no-number passwords and accepts valid ones | matches `TC-A-00a`–`TC-A-00d` | ✅ Automated — `auth.test.ts` (cross-ref `TC-A-00a`–`TC-A-00d`) |
| TC-PW-06 | `hashPassword` produces a salted bcrypt hash at ≥ 12 rounds | matches `TC-A-08a`–`TC-A-08c` | ✅ Automated — `auth.test.ts` (cross-ref `TC-A-08a`–`TC-A-08c`) |
| TC-PW-07 | Middleware redirects every protected route except `/change-password` to `/change-password` while `mustChangePassword = true` | redirect on `/dashboard`, `/admin/*`, etc.; no redirect loop on `/change-password` itself | ✅ Automated — `middleware.test.ts` (TC-PW-07a/b/c: redirect-on-protected-routes, no-loop-on-/change-password, no-redirect-when-flag-false) |
| TC-PW-08 | `POST /api/auth/change-password` rejects a new password identical to the current/temporary password | 400 "New password must be different from the temporary password" | ✅ Automated — `changePassword.test.ts` ("TC-PW-08: change-password rejects a new password identical to the temporary password") |
| TC-PW-09 | `POST /api/auth/change-password` succeeds: clears `mustChangePassword`, writes `password_change` AuditEvent, updates session | 200 `{ ok: true }`; `user.mustChangePassword = false`; audit event recorded; `session.mustChangePassword = false` | ✅ Automated — `changePassword.test.ts` ("TC-PW-09: change-password succeeds — clears mustChangePassword, writes audit event, updates session") |
| TC-PW-10 | `POST /api/auth/change-password` rejects an incorrect current/temporary password | 401; `mustChangePassword` remains `true` | ✅ Automated — `changePassword.test.ts` ("TC-PW-10: change-password rejects an incorrect current/temporary password and leaves mustChangePassword set") |

---

## 9.44 — Flat Admin Settings UI Redesign (TC-AC)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-AC-01 | `/admin/settings` loads the flat admin console with sticky sidebar, top context bar, and page status area | sidebar visible while scrolling, current tab name and status badge displayed | ✅ Automated — `adminSettingsConsole.test.ts` "TC-AC-01" suite asserts `activeTabMeta` resolves each tab to its label/description for the context bar, falls back to the first tab for an unknown id, and `ADMIN_TABS` lists all 9 sidebar entries (`users`, `requests`, `config`, `retention`, `thresholds`, `orphan`, `backup`, `cloud`, `browser`) with a label and icon — updated 2026-06-16 to include `config` tab added in v4.9.0 |
| TC-AC-02 | Switching tabs in `/admin/settings` updates the main panel in place while preserving the flat console layout | selected tab content changes without a full layout reload | ✅ Automated — `adminSettingsConsole.test.ts` "TC-AC-02" suite asserts `activeTabMeta` resolves a distinct tab per selection and `buildSettingsStats` returns tab-specific cards (and an empty array for unknown tabs) for all 9 tabs, proving the panel content is driven purely by the selected `Tab` while `ADMIN_TABS`/layout stay constant |
| TC-AC-03 | Users tab presents a table-first workflow with inline role/status editing and contextual summary cards | table-first layout and summary cards are visible; inline edits save on blur | ✅ Automated — `adminSettingsConsole.test.ts` "TC-AC-03" suite asserts `buildSettingsStats('users', …)` produces correct total/active/admin summary cards (including the "No users yet" zero-division guard), `roleOptionsFor` returns the right assignable-role set for plain vs. elevated users, and `matchesUserFilter` narrows the inline-editable table by name/email search and role filter |

---

## 9.45 — Work Item Explorer Field-Format Compatibility (TC-FF-01 to TC-FF-06)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-FF-01 | `buildRelationGraph` resolves issueKey/type/summary from raw-only JiraIssue fields (`Issue Key`/`Issue Type`/`Summary`, no FlowItem fields present) | focus node found with correct issueKey, type, summary | ✅ Automated — `fieldFormatCompat.test.ts` "TC-FF-01" |
| TC-FF-02 | `buildRelationGraph` resolves issueKey/type/summary from FlowItem-only fields (`key`/`type`/`summary`, no raw Jira fields present) | focus node found with correct issueKey, type, summary | ✅ Automated — `fieldFormatCompat.test.ts` "TC-FF-02" |
| TC-FF-03 | Raw-only `Status: Done` (no `status` field) resolves `isDone`/`health` | isDone=true, health='done' | ✅ Automated — `fieldFormatCompat.test.ts` "TC-FF-03" |
| TC-FF-04 | Raw-only `Blocked Flag: true` (no `isBlocked` field) resolves `isBlocked`/`health` | isBlocked=true, health='critical' | ✅ Automated — `fieldFormatCompat.test.ts` "TC-FF-04" |
| TC-FF-05 | Raw-only `Parent Key`/`Epic Link` fields reconstruct the same hierarchy depth/parentKey as FlowItem `parent`/`epic` fields | focus depth=0, parent depth=-1, child depth=1, parentKey resolved | ✅ Automated — `fieldFormatCompat.test.ts` "TC-FF-05" |
| TC-FF-06 | A dataset mixing raw-only and FlowItem-only issues builds one consistent graph; risk-path computation marks both a raw-only blocked node and its FlowItem-only ancestor | all 4 nodes present; blocked/done flags correct; both nodes marked isOnRiskPath=true | ✅ Automated — `fieldFormatCompat.test.ts` "TC-FF-06" |

**Related:** UC-046, UC-088 (Alt Flow B), FR-225A, F2-09 — `fieldFormatCompat.test.ts` (6 tests; logic lives in `relationExplorer.service.ts`'s field accessors `f()`/`getIssueKey()`/`isDone()`/`isBlocked()` and `hierarchy.service.ts`'s `getKey()`/`getEpicLink()`/`getParentKey()`, already pure and directly testable through `buildRelationGraph()` — no extraction required).

---

## 9.46 — Dashboard Section Status Chips (TC-CH-01 to TC-CH-03)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-CH-01 | Each of the 5 chip severity tiers (`good`/`warning`/`critical`/`info`/`neutral`) maps to a distinct, defined `CHIP_CLS` style class, and `chipClass(type)` returns exactly that class | 5 distinct classes; `chipClass(t) === CHIP_CLS[t]` for every tier | ✅ Automated — `dashboardChips.test.ts` "TC-CH-01" |
| TC-CH-02 | `chipClass(undefined)` (a chip with no `type` given) falls back to the `neutral` style | returns `CHIP_CLS.neutral` | ✅ Automated — `dashboardChips.test.ts` "TC-CH-02" |
| TC-CH-03 | `mostSevereChipType` reduces a mixed list of chips to the single highest-severity tier present (critical > warning > info > good > neutral), and returns `neutral` for an empty list or a chip with no type | picks `critical` over `warning`/`info`/`good`; picks `warning` once `critical` is removed; returns `neutral` for `[{label:'no data'}]` and `[]` | ✅ Automated — `dashboardChips.test.ts` "TC-CH-03" |

**Related:** UC-090, SCN-046, UJ-030, FR-308, BR-112 — `dashboardChips.test.ts` (3 tests; `Chip`/`CHIP_CLS`/`CollapsibleTrigger`'s severity-to-style mapping extracted from `app/dashboard/page.tsx` to the pure module `src/lib/dashboardChips.ts` as `chipClass()`/`mostSevereChipType()`, mirroring the `adminConsole.ts`/`members.ts` extraction pattern so the cross-cutting chip convention spanning ~16 dashboard sections is independently unit-testable).

---

## 9.47 — Multi-File Upload Merge & Deduplication (TC-UM-01 to TC-UM-06)

*(Added 2026-06-08 to close TRACE-02 / Gaps Summary COVER-06 — `mergeIssueArrays()` backed a live route and UI control with zero FR/UC/TC anchor and zero test coverage.)*

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-UM-01 | Issues with distinct `Issue Key`s across merged files are all kept; `mergeStats` reports zero duplicates | `merged.length === 2`; `stats === { fileCount: 2, totalBeforeMerge: 2, duplicatesRemoved: 0, uniqueIssues: 2 }` | ✅ Automated — `mergeIssues.test.ts` "TC-UM-01" |
| TC-UM-02 | The same `Issue Key` appearing in two files is deduplicated into a single merged record | `merged.length === 1`; `stats.duplicatesRemoved === 1`; `stats.uniqueIssues === 1` | ✅ Automated — `mergeIssues.test.ts` "TC-UM-02" |
| TC-UM-03 | When merging a duplicate, a non-empty field value (string or otherwise) wins over an empty string, `null`, or `undefined` from the other file | merged record keeps `Assignee: 'Jane Doe'` and `Reporter: 'John Roe'` over the empty/null values | ✅ Automated — `mergeIssues.test.ts` "TC-UM-03" |
| TC-UM-04 | When merging a duplicate where both files have a non-empty string for the same field, the longer (more detailed) string wins, regardless of file order | `Summary` resolves to the longer string in both `[A,B]` and `[B,A]` merge order | ✅ Automated — `mergeIssues.test.ts` "TC-UM-04" |
| TC-UM-05 | Rows with a missing, blank, or whitespace-only `Issue Key` are excluded from the merged result (cannot be deduplicated without a key) | only the row with a real key survives; `stats.totalBeforeMerge === 4`, `stats.uniqueIssues === 1` | ✅ Automated — `mergeIssues.test.ts` "TC-UM-05" |
| TC-UM-06 | Merging three files reports correct `fileCount`, `totalBeforeMerge`, `duplicatesRemoved`, and `uniqueIssues` when duplicates span more than two files | `merged.length === 4`; `stats === { fileCount: 3, totalBeforeMerge: 6, duplicatesRemoved: 2, uniqueIssues: 4 }` | ✅ Automated — `mergeIssues.test.ts` "TC-UM-06" |

**Related:** UC-094, FR-312, FR-001 — `mergeIssues.test.ts` (6 tests covering `mergeIssueArrays()` in `src/lib/mergeIssues.ts`, the pure dedup-and-merge function behind `POST /api/upload/merge` and the landing page's "Combine multiple exports" control).

---

## 9.48 — Snapshot Load Error States: Not Authenticated, Not Found, Access Denied (TC-SN-09 to TC-SN-11)

*(Added 2026-06-08 to close TRACE-02 / Gaps Summary COVER-12 — `GET /api/snapshots/:id` guards three distinct error responses (401/404/403) that had no direct route-level test; `snapshots.test.ts`'s TC-SN-01–08 cover the `deleteDashboardSnapshot` service layer only, not the GET handler's load-failure paths.)*

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-SN-09 | Loading any snapshot while unauthenticated | `GET /api/snapshots/:id` returns `401 { error: 'Not authenticated.' }` before any DB lookup | ✅ Automated — `snapshotLoadErrors.test.ts` "TC-SN-09" |
| TC-SN-10 | Loading a snapshot ID that does not exist in the DB | `GET /api/snapshots/:id` returns `404 { error: 'Snapshot not found.' }` | ✅ Automated — `snapshotLoadErrors.test.ts` "TC-SN-10" |
| TC-SN-11 | Loading another user's snapshot is denied for normal roles but permitted for admins | non-owner non-admin gets `403 { error: 'Access denied.' }`; admin loading the same snapshot ID gets `200` with the snapshot payload | ✅ Automated — `snapshotLoadErrors.test.ts` "TC-SN-11" |

**Related:** UC-053 (Save and Load Dashboard Snapshot), FR-001 — `snapshotLoadErrors.test.ts` (3 tests covering the `GET` handler in `app/api/snapshots/[id]/route.ts`); cross-ref `snapshots.test.ts` TC-SN-01–08 (service-layer save/list/delete/limit coverage) and TC-SN-02 (cross-user delete denial, the DELETE-side analogue of TC-SN-11).

---

## 9.49 — Backend Integration Gateway Foundation (TC-GW-01 to TC-GW-21 + TC-GW-05b + TC-GW-15b)

*(Added 2026-06-09 to close NEXT-03 / GW-01–GW-25 documentation impact matrix — `src/server/gateway/` module suite ships 23 tests in `src/__tests__/gateway.test.ts`; all tests are ✅ Automated and passing. Suite: 550/61. Server-only module — no UI routes, no user-facing flows, no schema changes; FR-313 in SRS §4 and §8.1 note added.)*

### Endpoint Policy & SSRF Protection (`endpointPolicy.ts`)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-GW-01 | Host not on allowlist is blocked | `validateEndpoint` returns `{ allowed: false, reason: … }` naming the host | ✅ Automated — `gateway.test.ts` "TC-GW-01" |
| TC-GW-02 | HTTP protocol blocked in production; HTTPS allowed | `http://` URL returns `allowed: false` when `NODE_ENV=production`; `https://` allowed | ✅ Automated — `gateway.test.ts` "TC-GW-02" |
| TC-GW-03 | Private/RFC-1918 IP blocked in production | `192.168.x.x`, `10.x.x.x`, `172.16–31.x.x` all return `allowed: false` | ✅ Automated — `gateway.test.ts` "TC-GW-03" |
| TC-GW-04 | Path/query traversal pattern blocked | URLs containing `../`, `..%2F`, `%2e%2e` return `allowed: false` | ✅ Automated — `gateway.test.ts` "TC-GW-04" |
| TC-GW-05 | Configured endpoint (example.com) is allowed | `validateEndpoint` returns `{ allowed: true }` for a whitelisted host over HTTPS | ✅ Automated — `gateway.test.ts` "TC-GW-05" |
| TC-GW-05b | Localhost allowed in development when `allowLocalhost: true` | Returns `{ allowed: true }` when `NODE_ENV=development` and the provider config sets `allowLocalhost: true` | ✅ Automated — `gateway.test.ts` "TC-GW-05b" |

### Retry Policy (`retryPolicy.ts`)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-GW-06 | Retryable vs non-retryable HTTP status codes | `isRetryable(500/502/503/504/429/408)` → `true`; `isRetryable(400/401/403/404/409/422)` → `false` | ✅ Automated — `gateway.test.ts` "TC-GW-06" |
| TC-GW-07 | Exponential backoff grows with retry count | `computeBackoffDelay(0)` < `computeBackoffDelay(1)` < `computeBackoffDelay(2)` | ✅ Automated — `gateway.test.ts` "TC-GW-07" |

### Gateway Logger & Redaction (`gatewayLogger.ts`)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-GW-10 | Secrets redacted in log records | `redact()` masks values keyed `token`/`key`/`secret`/`password`/`cookie`/`Authorization`/`Basic`/`Bearer`/`connString` with `[REDACTED]` | ✅ Automated — `gateway.test.ts` "TC-GW-10" |
| TC-GW-11 | Non-secret fields pass through redaction unchanged | Plain fields (e.g., `url`, `operation`) are unmodified by `redact()` | ✅ Automated — `gateway.test.ts` "TC-GW-11" |
| TC-GW-12 | JSONL audit record written with all observability fields | `logGatewayCall()` appends a JSONL record containing `requestId`, `provider`, `operation`, `startedAt`, `durationMs`, `status`, `retryCount`, `errorCategory` | ✅ Automated — `gateway.test.ts` "TC-GW-12" |

### Provider Registry (`providerRegistry.ts`)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-GW-13 | Default blueprint provider returned when no config file exists | `getProviderConfig('jira')` returns the built-in `JIRA_BLUEPRINT` defaults | ✅ Automated — `gateway.test.ts` "TC-GW-13" |
| TC-GW-14 | Config-file overrides merge over default blueprint | A custom `gateway-providers.json` entry overrides only the specified fields; unspecified fields retain blueprint defaults | ✅ Automated — `gateway.test.ts` "TC-GW-14" |
| TC-GW-15 | Unknown provider returns a safe anonymous config | `getProviderConfig('unknown-provider')` returns a minimal safe default rather than throwing | ✅ Automated — `gateway.test.ts` "TC-GW-15" |
| TC-GW-15b | `writeProviderConfigFile()` persists config to disk; subsequent `getProviderConfig` reads it back | Written JSON roundtrips correctly through `readProviderConfigFile` | ✅ Automated — `gateway.test.ts` "TC-GW-15b" |

### External Gateway (End-to-End Flow — `externalGateway.ts`)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-GW-16 | Successful fetch returns `GatewayResult` with `ok: true` | `callExternal()` resolves with `{ ok: true, data, status: 200, retryCount: 0 }` | ✅ Automated — `gateway.test.ts` "TC-GW-16" |
| TC-GW-17 | Policy-blocked URL returns `GatewayResult` with `ok: false` and `errorCategory: 'policy'` | Disallowed host → `{ ok: false, errorCategory: 'policy' }` without any network call | ✅ Automated — `gateway.test.ts` "TC-GW-17" |
| TC-GW-18 | Non-OK HTTP response returns typed error result | `fetch` returning 404 → `{ ok: false, status: 404, errorCategory: 'http_error' }` | ✅ Automated — `gateway.test.ts` "TC-GW-18" |
| TC-GW-19 | Retryable error retries up to max and returns final failure | `fetch` always returning 503 → result has `retryCount: 2` (max retries exhausted), `ok: false` | ✅ Automated — `gateway.test.ts` "TC-GW-19" |
| TC-GW-20 | Timeout aborts request and sets `errorCategory: 'timeout'` | Simulated `AbortError` → `{ ok: false, errorCategory: 'timeout' }` | ✅ Automated — `gateway.test.ts` "TC-GW-20" |
| TC-GW-21 | Non-retryable error (401) fails immediately without retries | 401 response → `{ ok: false, retryCount: 0, errorCategory: 'http_error' }` | ✅ Automated — `gateway.test.ts` "TC-GW-21" |

**Related:** FR-313 (SRS §4 — Backend Integration Gateway) — `gateway.test.ts` (23 tests across `endpointPolicy.ts`, `retryPolicy.ts`, `gatewayLogger.ts`, `providerRegistry.ts`, and `externalGateway.ts`; all server-only modules in `src/server/gateway/`). No UC/SCN/UJ anchors required — this is a server-only infrastructure module with no user-facing flows (server team/admin concern only; future gateway-usage use cases will be added per COVER-17 when external-call features are implemented).

---

## 9.50 — User Add-Member Request Workflow (TC-REQ-01 to TC-REQ-14, TC-REQ-18 to TC-REQ-20b)

*(Added 2026-06-09 to close USERREQ-07–14, USERREQ-28 from TODO-List.md Section 15. Prisma models: `UserAddRequest` + `Notification` (FR-314/FR-315). API routes: `POST /api/user-add-requests` (FR-316), `GET /api/user-add-requests/mine` (FR-317), `GET /api/admin/user-add-requests` (FR-318), `PATCH /api/admin/user-add-requests/[id]/accept` (FR-319), `PATCH /api/admin/user-add-requests/[id]/reject` (FR-319). Tests in `src/__tests__/userAddRequests.test.ts`.)*

### POST /api/user-add-requests (requester routes)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-REQ-01 | Authenticated user submits a valid add-member request | HTTP 201 `{ ok: true, request: { status: "pending", requestedEmail } }`; `prisma.userAddRequest.create` called with `status: "pending"` and correct `requestedByUserId` | ✅ Automated — `userAddRequests.test.ts` "TC-REQ-01" |
| TC-REQ-02 | Unauthenticated request to `POST /api/user-add-requests` | HTTP 401 | ✅ Automated — `userAddRequests.test.ts` "TC-REQ-02" |
| TC-REQ-03 | Email already has an existing user account | HTTP 409 "An account with this email already exists."; `userAddRequest.create` not called | ✅ Automated — `userAddRequests.test.ts` "TC-REQ-03" |
| TC-REQ-04 | A pending request for the same email already exists | HTTP 409 "A pending request for this email already exists."; `userAddRequest.create` not called | ✅ Automated — `userAddRequests.test.ts` "TC-REQ-04" |
| TC-REQ-05 | Required fields missing from request body | HTTP 400 error mentioning "required" | ✅ Automated — `userAddRequests.test.ts` "TC-REQ-05" |
| TC-REQ-18 | Same requester submits an 11th request within a 10-minute window (rate limit, USERREQ-25) | First 10 calls return HTTP 201; the 11th returns HTTP 429 with an error mentioning "too many requests" | ✅ Automated — `userAddRequests.test.ts` "TC-REQ-18" (added 2026-06-20; numbered 18 because `TC-REQ-17` was already in use by the §9.53 Generate-button manual test) |
| TC-REQ-19 | Submit with `requestedEmail: "not-an-email"` | HTTP 400 "Please enter a valid email address."; `userAddRequest.create` not called | ✅ Automated — `userAddRequests.test.ts` "TC-REQ-19" (added 2026-06-27 — closed `TODO-List.md` `TEST-REQ-03`; found a real gap, the email-format check existed only client-side in `RequestAddMemberModal.tsx`) |
| TC-REQ-20 | Submit `requestedRole: "admin"` with a reason under 20 characters | HTTP 400 "Admin and C-level requests require a detailed justification (at least 20 characters)."; `userAddRequest.create` not called | ✅ Automated — `userAddRequests.test.ts` "TC-REQ-20" (added 2026-06-27 — closed `TODO-List.md` `TEST-REQ-11`; found a real gap, the high-privilege reason-length rule existed only client-side) |
| TC-REQ-20b | Submit `requestedRole: "admin"` with a reason ≥20 characters | HTTP 201 success | ✅ Automated — `userAddRequests.test.ts` "TC-REQ-20b" |

### GET /api/user-add-requests/mine

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-REQ-06 | Authenticated user fetches their own requests | HTTP 200 `{ requests: [...] }` filtered to `requestedByUserId: session.userId` | ✅ Automated — `userAddRequests.test.ts` "TC-REQ-06" |
| TC-REQ-07 | Unauthenticated request to `GET /api/user-add-requests/mine` | HTTP 401 | ✅ Automated — `userAddRequests.test.ts` "TC-REQ-07" |

### GET /api/admin/user-add-requests

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-REQ-08 | Admin lists all requests; each includes `requestedBy` user info | HTTP 200 `{ requests }` with `requestedBy.email` populated | ✅ Automated — `userAddRequests.test.ts` "TC-REQ-08" |
| TC-REQ-09 | Non-admin call to `GET /api/admin/user-add-requests` | HTTP 403 "Admin access required." | ✅ Automated — `userAddRequests.test.ts` "TC-REQ-09" |

### PATCH /api/admin/user-add-requests/[id]/accept

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-REQ-10 | Admin accepts a pending request: creates user, marks request accepted, notifies requester, audits | HTTP 200 `{ ok: true, createdUser: { mustChangePassword: true } }`; `user.create` called with `mustChangePassword: true`; `userAddRequest.update` called with `status: "accepted"`; `notification.create` called; `auditEvent.create` called | ✅ Automated — `userAddRequests.test.ts` "TC-REQ-10" |
| TC-REQ-11 | Accept a request ID that does not exist | HTTP 404 | ✅ Automated — `userAddRequests.test.ts` "TC-REQ-11" |
| TC-REQ-12 | Accept a request that is already accepted | HTTP 409 matching "already accepted" | ✅ Automated — `userAddRequests.test.ts` "TC-REQ-12" |

### PATCH /api/admin/user-add-requests/[id]/reject

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-REQ-13 | Admin rejects a pending request with a decision note: marks rejected, notifies requester, audits | HTTP 200 `{ ok: true }`; `userAddRequest.update` with `status: "rejected"`; `notification.create` called; `auditEvent.create` called | ✅ Automated — `userAddRequests.test.ts` "TC-REQ-13" |
| TC-REQ-14 | Reject a request that is already rejected | HTTP 409 matching "already rejected" | ✅ Automated — `userAddRequests.test.ts` "TC-REQ-14" |

**Related:** UC-095, UC-096, FR-314–FR-319, FR-235B, FR-235D — `userAddRequests.test.ts` (18 tests in this section covering the full USERREQ-07/09/10–14/25 surface: Prisma model structure via mocked `prisma.userAddRequest`/`prisma.notification`, both requester routes, all three admin routes, all key guard conditions — duplicate email, duplicate pending request, already-decided status, missing required fields, invalid email format, high-privilege reason length, unauthenticated access, non-admin access — and the submission rate limiter).

---

## 9.51 — In-App Notification API (TC-NOTIF-01 to TC-NOTIF-05)

### GET /api/notifications

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-NOTIF-01 | Authenticated user calls GET; prisma returns one notification | HTTP 200 `{ notifications: [{ id: "notif-1", ... }] }`; `findMany` called with `where: { recipientUserId: "user-1" }` | ✅ Automated — `notifications.test.ts` "TC-NOTIF-01" |
| TC-NOTIF-02 | Unauthenticated request to GET /api/notifications | HTTP 401 "Not authenticated." | ✅ Automated — `notifications.test.ts` "TC-NOTIF-02" |

### PATCH /api/notifications/[id]/read

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-NOTIF-03 | Authenticated owner marks notification as read | HTTP 200 `{ ok: true }`; `notification.update` called with `where: { id: "notif-1" }` and `data: { readAt: <Date> }` | ✅ Automated — `notifications.test.ts` "TC-NOTIF-03" |
| TC-NOTIF-04 | Unauthenticated request to PATCH /api/notifications/[id]/read | HTTP 401 "Not authenticated." | ✅ Automated — `notifications.test.ts` "TC-NOTIF-04" |
| TC-NOTIF-05 | Authenticated user attempts to mark another user's notification as read | HTTP 404 "Not found." (recipientUserId mismatch) | ✅ Automated — `notifications.test.ts` "TC-NOTIF-05" |

**Related:** UC-099, FR-322, FR-315 — `notifications.test.ts` (5 tests covering auth guards, data retrieval, mark-read ownership validation)

---

## 9.52 — Admin Accept: Temp Password Validation (TC-REQ-15 to TC-REQ-16)

### PATCH /api/admin/user-add-requests/[id]/accept — password guards (updated FR-319)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-REQ-15 | Accept called with empty body (no `tempPassword`) | HTTP 400 "A temporary password is required." | ✅ Automated — `userAddRequests.test.ts` "TC-REQ-15" |
| TC-REQ-16 | Accept called with `tempPassword: "weak"`; `validatePasswordStrength` mock returns an error | HTTP 400 matching "8 characters" | ✅ Automated — `userAddRequests.test.ts` "TC-REQ-16" |

**Related:** UC-098, FR-319, FR-321 — added to `userAddRequests.test.ts`; suite for that file now 16 tests (TC-REQ-01–16)

---

## 9.53 — Welcome Email + Generate Password + Notification Navigation (TC-EMAIL-01–03, TC-REQ-17, TC-NOTIF-06–07)

### Email delivery, password generation, and notification click routing (FR-319, FR-321, FR-323, FR-325)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-EMAIL-01 | `sendEmail()` called when `SMTP_HOST`, `SMTP_USER`, `SMTP_PASS` all set; nodemailer `sendMail` resolves | Returns `true`; `sendMail` called once with correct `to`, `subject`, `text`, `html` fields containing the temp password and login URL | ⬜ Manual — verify in server logs: "✅ Welcome email sent to …" badge visible in admin UI after accept |
| TC-EMAIL-02 | `sendEmail()` called with `SMTP_HOST` missing from env | Returns `false` without throwing; `console.warn` logged with "SMTP not configured"; accept HTTP 200 still returned | ⬜ Manual — clear `SMTP_HOST`, accept a request, verify ⚠️ "Email not sent — SMTP not configured" badge shown |
| TC-EMAIL-03 | `sendEmail()` called with valid SMTP config but `sendMail` rejects (e.g. auth failure) | `catch` swallows the error; `emailSent` set to `false`; accept HTTP 200 still returned; `console.warn` logged with error message | ⬜ Manual — set wrong `SMTP_PASS`, accept a request, verify ⚠️ badge shown and no 500 |
| TC-REQ-17 | Admin clicks "Generate" button in `UserAddRequestsPanel` temp-password field | Password field fills with a 14-character string; field becomes visible (eye open); value satisfies `validatePasswordStrength` (≥8 chars, ≥1 uppercase, ≥1 digit) | ⬜ Manual — click Generate, visually confirm format, then click Accept |
| TC-NOTIF-06 | Non-admin user clicks a `user_add_request_accepted` notification | Notification marked read; dropdown closes; browser navigates to `/members` | ⬜ Manual — accept a request as admin, log in as requester, click notification, verify Members page loads |
| TC-NOTIF-07 | Admin navigates to `/admin/settings?tab=requests` directly | Page loads with "Member Requests" tab active — `UserAddRequestsPanel` visible immediately, not "Users" tab | ⬜ Manual — paste URL, confirm correct tab; also test `?tab=cloud` and `?tab=unknown` (falls back to Users) |

**Related:** FR-319, FR-321, FR-323, FR-325, BR-113, BR-114, UC-096, UC-098, UC-099, UC-100 — manual verification suite; automated coverage for email graceful-skip and accept route deferred to a future test sprint.

---

## 9.54 — Roadmap Page: Epic Forecast Logic (TC-ROAD-01 to TC-ROAD-06)

| TC ID | Input / Scenario | Expected Output | How to verify |
|---|---|---|---|
| TC-ROAD-01 | Epic with `progress >= 100` passed to `forecastEpic()` | `forecastLabel: 'Complete'`, `confidence: 'high'`, `weeksRemaining: 0` | Unit test `forecastEpic()` with a mock EpicSummary where `progress=100` |
| TC-ROAD-02 | `avgThroughput = 0` passed to `forecastEpic()` | `forecastLabel: 'Insufficient data'`, `confidence: 'low'`, `sprintsRemaining: null` | Unit test with `avgThroughput=0` |
| TC-ROAD-03 | Epic with 3 remaining issues and `avgThroughput = 5` | `sprintsRemaining = 0.6`, `weeksRemaining = 2`, `forecastLabel: 'Within 2 weeks'`, `confidence: 'high'` | Unit test: `remaining=3, throughput=5` |
| TC-ROAD-04 | Epic with 12 remaining issues and `avgThroughput = 4` | `sprintsRemaining = 3.0`, `weeksRemaining = 6`, `forecastLabel: '~6 weeks'`, `confidence: 'medium'` | Unit test: `remaining=12, throughput=4` |
| TC-ROAD-05 | Epic with 40 remaining issues and `avgThroughput = 4` | `weeksRemaining = 20`, `forecastLabel: '~5 months'`, `confidence: 'low'` | Unit test: `remaining=40, throughput=4` |
| TC-ROAD-06 | `/roadmap` page loaded with no epic data in metrics | Empty state shown: "No epic data found"; no JS error | ⬜ Manual — upload a Jira export with no Epic Link/Epic Name column, navigate to /roadmap |

**Automated 2026-06-26:** TC-ROAD-01–05 implemented in `src/__tests__/roadmapForecast.test.ts` (mirrors `forecastEpic()` from `app/roadmap/page.tsx`, matching the project's existing convention for pure functions defined inside `'use client'` page files — see `trends.test.ts`). TC-ROAD-06 remains manual (full-page render/empty-state, not a pure function). One additional edge case added: `TC-ROAD-02b` — `remaining <= 0` with `progress < 100` (data inconsistency) also resolves to `Insufficient data` rather than crashing.

**Related:** FR-326, FR-327, UC-101, UJ-036, SCN-051

---

## 9.55 — Forecast Page: Status and Burn-Up (TC-FCAST-01 to TC-FCAST-05)

| TC ID | Input / Scenario | Expected Output | How to verify |
|---|---|---|---|
| TC-FCAST-01 | `done >= total` | Status `complete`; banner colour blue; confidence `high` | Automated — `src/__tests__/forecastEngine.test.ts` |
| TC-FCAST-02 | `avgThroughput = 0` | Status `insufficient_data`; confidence `low`; `confidenceReason` says confidence is "not available" | Automated |
| TC-FCAST-03 | `sprintsRemaining <= 6` | Status `on_track`; green banner | Automated |
| TC-FCAST-04 | `6 < sprintsRemaining <= 12` | Status `at_risk`; amber banner | Automated (previously a manual-only scenario with no automated coverage — closed 2026-06-27) |
| TC-FCAST-05 | BurnUpChart rendered with 0 sprint points | Empty SVG state; no JS error; no undefined reference | ⬜ Manual — upload data with no sprint history, visit /forecast |

**Related:** FR-328, FR-329, UC-102, UJ-037, SCN-052

---

## 9.55a — Forecast Engine: Confidence, Weakest Factor, Scope Trend (TC-FCAST-06 to TC-FCAST-13)

*(Added 2026-06-27, `FCAST-14`–`26` from TODO-List.md Section 18. `computeForecast()` had zero automated test coverage before this change.)*

| TC ID | Input / Scenario | Expected Output | How to verify |
|---|---|---|---|
| TC-FCAST-06 | More than 3 blocked items, regardless of `sprintsRemaining` | Status `off_track` | Automated — `src/__tests__/forecastEngine.test.ts` |
| TC-FCAST-07 | Two sprints with `completedCount` 4 and 6 | `avgThroughput = 5` | Automated |
| TC-FCAST-08 | Identical structural signals, Data Quality `Excellent` vs. `Critical` | `Critical` confidence band is never higher than `Excellent`'s; `confidenceReason` names the `Critical` band | Automated |
| TC-FCAST-09 | More than 3 blocked items | `weakestFactor.kind === 'blockers'` | Automated |
| TC-FCAST-10 | Mid-sprint added scope `>` `avgThroughput × 2`, no severe blockers | `weakestFactor.kind === 'scope'`; an adjustment mentions "added mid-sprint" | Automated |
| TC-FCAST-11 | Rich sprint data with `addedScopeCount`/`removedScopeCount`/`blockedCount` | `scopeTrend` contains one entry per sprint with the matching counts | Automated |
| TC-FCAST-12 | Only legacy 8-sprint-capped `metrics.sprint.sprints` data (no rich throughput) | `scopeTrend` is `[]` | Automated |
| TC-FCAST-13 | Zero issues entirely (`totalIssues === 0`) | Status `insufficient_data`; does not throw | Automated |

**Related:** FR-359, FR-360, FR-361, FR-362, FR-363, FR-364, Addendum J

---

## 9.56 — Retro Page: Form, Insights, Template Download (TC-RETRO-01 to TC-RETRO-07)

| TC ID | Input / Scenario | Expected Output | How to verify |
|---|---|---|---|
| TC-RETRO-01 | User visits `/retro` — no authentication | Redirected to `/login` (middleware) | ⬜ Manual — open `/retro` in incognito, confirm redirect |
| TC-RETRO-02 | Sprint Name field left empty | "Submit & Get Suggestions" button is disabled | ⬜ Manual — open form view, leave Sprint Name blank, confirm button disabled |
| TC-RETRO-03 | Sprint Name filled, goal met = "no", 1 blocker, 2 actions (1 missing owner, 1 missing due date) | `InsightPanel` "Do This Next" shows goal-not-met advice + blocker-count advice + ownership-gap warnings | ⬜ Manual — fill form matching these values, submit, verify InsightPanel |
| TC-RETRO-04 | Sprint Name filled, all action items have owner + due date, goal met = "yes" | "Do This Next" / "What to Watch" show no ownership-gap or blocker warnings | ⬜ Manual — fill complete form, submit |
| TC-RETRO-05 | User clicks "Download .xlsx →" on template card | Browser downloads `Retrospective_Template.xlsx` (2 sheets, 4 example rows); secondary "download as .csv instead" link still saves the original `Retrospective_Template.csv` | ⬜ Manual — click Download, open file, verify both sheets and the CSV fallback link |
| TC-RETRO-06 | User submits, then clicks "New Retrospective" | Form resets to `EMPTY_FORM`; view returns to menu | ⬜ Manual — submit, click New Retrospective, verify form is blank |
| TC-RETRO-07 | Entry list with 1 item — remove button not shown | No × button on the last remaining entry in any list | ⬜ Manual — check all three observation lists and action items list |

**Related:** FR-330, FR-331, FR-356, FR-357, UC-103, UC-104, UJ-038, SCN-053

---

## 9.56a — Retrospective File Parser: Upload, Grouping, Validation (TC-RETRO-08 to TC-RETRO-13)

| TC ID | Input / Scenario | Expected Output | How to verify |
|---|---|---|---|
| TC-RETRO-08 | CSV with a Sprint Name row followed by a blank-Sprint-Name row, then a second Sprint Name row | 2 `RetroRecord`s; second row's observations/actions merge into the first record | Automated — `src/__tests__/retroFileParser.test.ts` |
| TC-RETRO-08b | A real binary `.xlsx` workbook (not CSV text) with the same shape | Parses identically via `XLSX.read()` | Automated |
| TC-RETRO-09 | File missing the "Sprint Name" column entirely | `records: []`; warning naming the missing column | Automated |
| TC-RETRO-10 | A data row appears before any "Sprint Name" has been seen | Row skipped; logged as a `RetroDataCorrection` (severity `warning`) | Automated |
| TC-RETRO-11 | A row has a Sprint Name but no observations or action items | Record still created; warning naming the sprint and "no observations" | Automated |
| TC-RETRO-12 | Well-formed Markdown with `Sprint Name:` line, `##` headings, and bullet lists | 1 record; sections correctly mapped to `wentWell`/`didntGoWell`/`blockers`/`actions`; no warnings | Automated |
| TC-RETRO-13 | Markdown/plain text with no `Sprint Name:` line | `sprintName: 'Untitled Retrospective'`; warning advising the user to verify the preview | Automated |

**Related:** FR-355, FR-356a, UC-115

---

## 9.56b — Retrospective Insights Engine: Themes, Ownership Gaps, Repeated Blockers (TC-RETRO-14 to TC-RETRO-25)

| TC ID | Input / Scenario | Expected Output | How to verify |
|---|---|---|---|
| TC-RETRO-14 | "What Did Not Go Well" text mentions QA/regression/release | `themes` includes category `qa-release` | Automated — `src/__tests__/retroInsights.test.ts` |
| TC-RETRO-14b | "What Went Well" text mentions QA/testing (positive feedback) | `themes` is empty; `nextSprintSuggestions` never mentions the `qa-release` theme | Automated — regression test for the "retro report not useful" bug fix |
| TC-RETRO-14c | "What Did Not Go Well" text says "Sprint planning was too long" | `themes` includes category `process` | Automated |
| TC-RETRO-15 | Action item with no owner and no due date | `ownershipGaps` has 2 entries (missing owner, missing due date) | Automated |
| TC-RETRO-16 | Two action items with the same text differing only in case/whitespace | `duplicateActionItems` contains the normalized text | Automated |
| TC-RETRO-17 | Goal not met, 1 blocker, top theme detected | `nextSprintSuggestions` has 3 entries: re-plan advice, blocker-count advice, theme advice (citing the real example sentence) | Automated |
| TC-RETRO-18 | Records with 4, 2, and 0 non-empty context fields respectively | `confidence` is `high`, `medium`, `low` respectively | Automated |
| TC-RETRO-19 | A fully empty `RetroRecord` | Generates a safe insight with empty arrays and `confidence: 'low'`; does not throw | Automated |
| TC-RETRO-20 | The same blocker text (case/whitespace variant) appears in 2 of 3 records | `detectRepeatedBlockers()` flags it; all 3 records' insights carry the same `repeatedBlockers` list | Automated |
| TC-RETRO-21 | A non-repeated blocker | `suggestedBacklogItems` contains a `'task'` titled `Resolve blocker: "<text>"`, priority `high`, `description` starting "Task: Investigate and resolve..." with an "Acceptance criteria:" line | Automated |
| TC-RETRO-22 | The same blocker, now present in `repeatedBlockers` | No `'task'` for that text; instead a `'spike'` titled `Investigate root cause: "<text>"` | Automated |
| TC-RETRO-23 | A pain-point sentence matching a theme | `suggestedBacklogItems` contains a `'story'` titled `Improve: <ThemeLabel>` whose `description` starts "As a team, we want to address recurring..." and whose `evidence` quotes the triggering sentence | Automated |
| TC-RETRO-24 | `goalMet: 'no'` with a sprint goal set | `suggestedBacklogItems` contains a `'spike'` titled `Investigate why the sprint goal was not met` whose `evidence` quotes the sprint goal | Automated |
| TC-RETRO-25 | A fully empty `RetroRecord` | `suggestedBacklogItems` is `[]` — no fabricated suggestions | Automated |

**Related:** FR-356, FR-356b, UC-115, SCN-059

---

## 9.57 — ARCH-05 Phase 1: Jira Connection Admin Routes + API Adapter + Sync + Fallback Contract (TC-JIRA-01 to TC-JIRA-50)

*(Added 2026-06-20, JIRA-05/05b/06/06b/07/08/09 from TODO-List.md Section 19a. Tests in `src/__tests__/jiraConnections.test.ts`, `src/__tests__/gateway.test.ts`, `src/__tests__/jiraApiAdapter.test.ts`, `src/__tests__/jiraSync.test.ts`, `src/__tests__/latestMetricsStorage.test.ts`, and `src/__tests__/cloudRestoreHardening.test.ts`.)*

### GET / POST /api/admin/jira-connections

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-JIRA-01 | Unauthenticated `GET /api/admin/jira-connections` | HTTP 401 | ✅ Automated — `jiraConnections.test.ts` "TC-JIRA-01" |
| TC-JIRA-02 | Non-admin `GET /api/admin/jira-connections` | HTTP 403 | ✅ Automated — `jiraConnections.test.ts` "TC-JIRA-02" |
| TC-JIRA-03 | Admin lists connections | HTTP 200; `projectFilters`/`fieldMapping` deserialized from JSON; `hasGatewayToken` boolean present; no raw token field on the response | ✅ Automated — `jiraConnections.test.ts` "TC-JIRA-03" |
| TC-JIRA-04 | `POST` missing required fields | HTTP 400 mentioning "required" | ✅ Automated — `jiraConnections.test.ts` "TC-JIRA-04" |
| TC-JIRA-05 | `POST` with an invalid `deploymentType` | HTTP 400 | ✅ Automated — `jiraConnections.test.ts` "TC-JIRA-05" |
| TC-JIRA-06 | `POST` Cloud connection with no `authEmail` | HTTP 400 mentioning "email" | ✅ Automated — `jiraConnections.test.ts` "TC-JIRA-06" |
| TC-JIRA-07 | `POST` with a malformed `baseUrl` | HTTP 400 | ✅ Automated — `jiraConnections.test.ts` "TC-JIRA-07" |
| TC-JIRA-08 | `POST` valid payload | HTTP 201; `prisma.jiraConnection.create` called with correct fields; `safeAuditEvent` fired (`jira_connection_create`) | ✅ Automated — `jiraConnections.test.ts` "TC-JIRA-08" |

### POST /api/admin/jira-connections/[id]/test

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-JIRA-09 | Test a connection ID that doesn't exist | HTTP 404 | ✅ Automated — `jiraConnections.test.ts` "TC-JIRA-09" |
| TC-JIRA-10 | `GATEWAY_JIRA_API_TOKEN` not set in env | HTTP 409 mentioning the env var name | ✅ Automated — `jiraConnections.test.ts` "TC-JIRA-10" |
| TC-JIRA-11 | Cloud connection, gateway call succeeds | HTTP 200 `{ ok: true, account }`; `callExternal` called with `Authorization: Basic base64(email:token)`, `path: '/rest/api/3/myself'`, `baseUrlOverride` set to the connection's `baseUrl`; `lastSyncStatus` updated to `"success"` | ✅ Automated — `jiraConnections.test.ts` "TC-JIRA-11" |
| TC-JIRA-12 | Server/DC connection, gateway call succeeds | `callExternal` called with `Authorization: Bearer <token>`, `path: '/rest/api/2/myself'` | ✅ Automated — `jiraConnections.test.ts` "TC-JIRA-12" |
| TC-JIRA-13 | Gateway call fails (e.g. 401 from Jira) | HTTP 502 `{ ok: false, error, errorCategory }`; `lastSyncStatus` updated to `"failed"` with `lastSyncError` set | ✅ Automated — `jiraConnections.test.ts` "TC-JIRA-13" |

### Gateway: per-connection base URL override (JIRA-05b)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-GW-22 | `getProviderConfig('jira', { baseUrlOverride, extraAllowedHosts })` with no global `GATEWAY_JIRA_BASE_URL` set | `enabled: true`; `baseUrl` is the override value; `allowedHosts` includes both the override's hostname and the extra host | ✅ Automated — `gateway.test.ts` "TC-GW-22" |
| TC-GW-22b | `baseUrlOverride` given but no credential env vars set | `enabled: false` — credentials still gate enablement even with a valid override URL | ✅ Automated — `gateway.test.ts` "TC-GW-22b" |

### Gateway: non-env credential presence override (JIRA-05c)

*(Added 2026-06-20 after moving the Jira token to encrypted App Config — `getProviderConfig()`'s default env-var credential check would otherwise always report `enabled: false` for a token sourced from `getJiraApiToken()` instead of `process.env`.)*

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-GW-23 | `getProviderConfig('jira', { baseUrlOverride, credentialsPresentOverride: true })` with no credential env vars set at all | `enabled: true` — the override alone is sufficient | ✅ Automated — `gateway.test.ts` "TC-GW-23" |
| TC-GW-23b | `getProviderConfig('jira', { credentialsPresentOverride: false })` with valid `GATEWAY_JIRA_BASE_URL`/`GATEWAY_JIRA_API_TOKEN` env vars set | `enabled: false` — the override can also explicitly block enablement | ✅ Automated — `gateway.test.ts` "TC-GW-23b" |

### GET /api/admin/jira-connections/[id]/fields (JIRA-06b)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-JIRA-25 | Discover fields for a connection ID that doesn't exist | HTTP 404 | ✅ Automated — `jiraConnections.test.ts` "TC-JIRA-25" |
| TC-JIRA-26 | No Jira token configured (App Config + env both empty) | HTTP 409 mentioning "App Config" | ✅ Automated — `jiraConnections.test.ts` "TC-JIRA-26" |
| TC-JIRA-27 | Gateway call succeeds | HTTP 200 `{ ok: true, fields: [{id,name}, ...] }`; `callExternal` called with `path: '/rest/api/3/field'`, `baseUrlOverride` set to the connection's `baseUrl` | ✅ Automated — `jiraConnections.test.ts` "TC-JIRA-27". **Also verified live against the user's real Jira Cloud instance** — returned all 117 real fields, correctly surfacing "Story Points" (`customfield_10033`) and "Story point estimate" (`customfield_10016`) by name. |
| TC-JIRA-28 | Gateway call fails | HTTP 502 | ✅ Automated — `jiraConnections.test.ts` "TC-JIRA-28" |

### src/services/jira/apiAdapter.ts — normalize Jira API issues to the canonical shape (JIRA-06)

*(Added 2026-06-21. Tests in `src/__tests__/jiraApiAdapter.test.ts`.)*

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-JIRA-14 | `normalizeJiraIssue()` on a typical issue | All standard fields (Issue Type, Summary, Status, Project, Assignee, Reporter, Priority, Created/Updated Date) mapped to their canonical names | ✅ Automated — "TC-JIRA-14" |
| TC-JIRA-15 | Normalized output fed into `validateIssueData()` | `isValid: true`, no errors — proves the adapter's output is a drop-in replacement for `parseJiraFile()`'s output | ✅ Automated — "TC-JIRA-15" |
| TC-JIRA-16 | Issue with `assignee: null`, `priority: undefined` | Those canonical keys are omitted entirely, not set to `null`/`undefined` | ✅ Automated — "TC-JIRA-16" |
| TC-JIRA-17 | `labels` and `fixVersions` arrays | Joined into comma-separated strings (`Labels`, `Fix Version/s`) | ✅ Automated — "TC-JIRA-17" |
| TC-JIRA-18 | A custom field present in `fieldMapping` (e.g. Story Points → `customfield_10016`) | Resolved from `issue.fields[fieldId]` | ✅ Automated — "TC-JIRA-18" |
| TC-JIRA-19 | Story Points customfield value is a string (`"8"`) | Coerced to a number (`8`) | ✅ Automated — "TC-JIRA-19" |
| TC-JIRA-20 | Sprint customfield is an array of sprint objects (modern Cloud format) | Resolves to the **last** (most recent) sprint's `name` | ✅ Automated — "TC-JIRA-20" |
| TC-JIRA-21 | Sprint customfield is a legacy greenhopper string | `name=...` extracted via regex from the string | ✅ Automated — "TC-JIRA-21" |
| TC-JIRA-22 | Epic Link customfield is a plain string vs. an object with `key` | Both forms resolve to the epic's key | ✅ Automated — "TC-JIRA-22" |
| TC-JIRA-23 | `fieldMapping` tries to override a standard field (e.g. `Status`) | Standard-field extraction always wins; the mapping entry is ignored | ✅ Automated — "TC-JIRA-23" |
| TC-JIRA-24 | `normalizeJiraIssues()` on an array of 3 issues | Output array preserves input order | ✅ Automated — "TC-JIRA-24" |
| TC-JIRA-51 | `normalizeJiraIssue()` on an issue with `fields.parent.key` set (JIRA-11 fix) | `Parent Key` resolves to the parent's key — `fields.parent` is a standard fixed-path field, not a custom field, so it needs no `fieldMapping` entry | ✅ Automated — "TC-JIRA-51" |
| TC-JIRA-52 | `normalizeJiraIssue()` on an issue with no `fields.parent` | `Parent Key` is omitted from the output entirely, not set to `null`/`undefined` | ✅ Automated — "TC-JIRA-52" |

### src/services/jira/sync.ts — JQL building + pagination (JIRA-07)

*(Added 2026-06-21. Tests in `src/__tests__/jiraSync.test.ts`.)*

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-JIRA-29 | `buildSyncJql(['PROJ', 'TEAM2'])` | `"project in (PROJ,TEAM2) ORDER BY updated DESC"` | ✅ Automated — "TC-JIRA-29" |
| TC-JIRA-30 | `buildSyncJql([])` | `ok: false` — no project keys configured | ✅ Automated — "TC-JIRA-30" |
| TC-JIRA-31 | `buildSyncJql(['PROJ', 'x); DROP--'])` | The unsafe key is silently filtered out; JQL built from the remaining valid key only — never raw JQL injection | ✅ Automated — "TC-JIRA-31" |
| TC-JIRA-32 | `buildSyncJql(['1bad', '--also-bad'])` | `ok: false` — every key was unsafe | ✅ Automated — "TC-JIRA-32" |
| TC-JIRA-33 | `fetchAllJiraIssues()` with no project filters | Fails fast, `configError: true`, zero `callExternal` calls | ✅ Automated — "TC-JIRA-33" |
| TC-JIRA-34 | `fetchAllJiraIssues()` Cloud connection missing `authEmail` | Fails fast, `configError: true`, zero `callExternal` calls | ✅ Automated — "TC-JIRA-34" |
| TC-JIRA-35 | First page has no `nextPageToken` (Cloud) | Single `callExternal` call; all issues returned | ✅ Automated — "TC-JIRA-35" |
| TC-JIRA-36 | Two Cloud pages linked by `nextPageToken` | Two `callExternal` calls; the 2nd call's `query.nextPageToken` matches the 1st page's token; issues from both pages combined | ✅ Automated — "TC-JIRA-36" (this test caught a real bug — the query object was built but never passed to `callExternal()`) |
| TC-JIRA-37 | Server/DC pagination via `startAt`/`total` | Two calls; 2nd call's `query.startAt` equals the 1st page's issue count; `path: '/rest/api/2/search'`; `Authorization: Bearer <token>` | ✅ Automated — "TC-JIRA-37" |
| TC-JIRA-38 | Gateway fails on the 2nd page mid-pagination | `ok: false` with the Gateway's error message; `configError` falsy (a real upstream failure, not a setup problem) | ✅ Automated — "TC-JIRA-38" |
| TC-JIRA-39 | 11 pages of 100 issues each (1100 total) | Stops at the 1000-issue safety cap; `truncated: true` | ✅ Automated — "TC-JIRA-39" |
| TC-JIRA-53 | `fetchAllJiraIssues()`'s outgoing `fields=` query param (JIRA-11 fix) | Includes `'parent'` — without it, Jira's bulk search response omits `fields.parent` even though it exists on the issue, silently breaking every Sub-task/Epic-under-Initiative hierarchy link | ✅ Automated — "TC-JIRA-53". **Also verified live**: a direct raw `GET /rest/api/3/issue/AJ-28` probe against the user's real Jira instance confirmed `fields.parent.key: "AJ-27"` was present on the issue all along — it was the bulk search's missing `fields=parent` request that dropped it, not the issue data itself. |

### POST /api/admin/jira-connections/[id]/sync (JIRA-07)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-JIRA-40 | Unauthenticated | HTTP 401 | ✅ Automated — `jiraConnections.test.ts` "TC-JIRA-40" |
| TC-JIRA-41 | Non-admin | HTTP 403 | ✅ Automated — "TC-JIRA-41" |
| TC-JIRA-42 | Connection ID doesn't exist | HTTP 404 | ✅ Automated — "TC-JIRA-42" |
| TC-JIRA-43 | No Jira token configured | HTTP 409 | ✅ Automated — "TC-JIRA-43" |
| TC-JIRA-44 | `fetchAllJiraIssues` returns a real Gateway failure | HTTP 502; connection's `lastSyncStatus` updated to `"failed"` | ✅ Automated — "TC-JIRA-44" |
| TC-JIRA-44b | `fetchAllJiraIssues` returns a `configError` (e.g. no project keys) | HTTP 409, not 502 — distinguishes "never reached Jira" from "Jira/Gateway failed" | ✅ Automated — "TC-JIRA-44b" |
| TC-JIRA-45 | Fetched issues array is empty | `validateIssueData()` fails → HTTP 422; `ImportLog.create` never called (no partial commit) | ✅ Automated — "TC-JIRA-45" |
| TC-JIRA-46 | Successful sync with 2 issues | HTTP 200 `{ ok: true, totalIssues, doneIssues, healthScore }`; `writeLatestMetrics()` called; `ImportLog.create` called with `sourceType: "api"`, `jiraConnectionId`; connection updated to `lastSyncStatus: "success"` | ✅ Automated — "TC-JIRA-46". **Also verified end-to-end against the user's real Jira Cloud instance** (project `SAMPLEPROJ`, 7 real issues) — got `{ totalIssues: 7, doneIssues: 1, healthScore: 48 }`; confirmed the real `ImportLog` row and `data/latest-metrics.json` were both written correctly, then cleaned up. |

### src/services/metrics/latestMetricsStorage.ts — origin metadata (JIRA-08)

*(Added 2026-06-21. Tests in `src/__tests__/latestMetricsStorage.test.ts`.)*

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-JIRA-47 | `writeLatestMetrics(metrics, { source: 'jira-api', connectionName, connectionId })` | `readLatestMetrics()` returns the exact same `origin` object back | ✅ Automated — "TC-JIRA-47" |
| TC-JIRA-48 | `writeLatestMetrics(metrics)` with no `origin` argument | `readLatestMetrics().origin` is `null` — backward compatible | ✅ Automated — "TC-JIRA-48" |
| TC-JIRA-49 | `readLatestMetrics()` on a file written before origin metadata existed (no `origin` key in the JSON at all) | Loads successfully; `origin: null` | ✅ Automated — "TC-JIRA-49" |
| TC-JIRA-50 | `readLatestMetrics()` called repeatedly | Never calls `fs.writeFileSync` — confirms the reader cannot itself mutate or clobber the last-good snapshot | ✅ Automated — "TC-JIRA-50" |

### GET /api/metrics/latest + loadMetricsWithSource() — jira-api source surfacing (JIRA-08)

*(Added 2026-06-21. Tests in `src/__tests__/cloudRestoreHardening.test.ts`.)*

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-CS-13 | Latest snapshot's stored `origin.source` is `'jira-api'` | `/api/metrics/latest` returns `source: 'jira-api'` and `connectionName` from the origin, regardless of cloud-transport status | ✅ Automated — "TC-CS-13" |
| TC-CS-14 | Latest snapshot's stored `origin.source` is `'file'` (or absent) | `/api/metrics/latest` falls back to bucket/cache/server-local detection as before; `connectionName` is `undefined` | ✅ Automated — "TC-CS-14" |
| TC-CS-15 | `loadMetricsWithSource()` against a `jira-api`-sourced `/api/metrics/latest` response | Resulting `MetricsSourceInfo.source` is `'jira-api'` and `connectionName` is threaded through correctly | ✅ Automated — "TC-CS-15" |

**Live (non-mocked) verification, JIRA-08:** synced a real connection against `SAMPLEPROJ` (7 issues) — `DataSourceBadge` (newly mounted in `DashboardTopbar`, the first time this component was ever actually rendered anywhere) showed "Jira · 5m ago" with title `"Jira (Test) — last synced 7m ago"`. Then cleared the connection's project filters to force a sync failure (`409`) and confirmed `/api/metrics/latest`'s `savedAt`/`source`/`metrics` were unchanged byte-for-byte — the dashboard never lost the last-good Jira snapshot. Test data reverted afterward.

**Related:** `ARCH-05`, `JIRA-04`, `JIRA-05`, `JIRA-05b`, `JIRA-05c`, `JIRA-06`, `JIRA-06b`, `JIRA-07`, `JIRA-09`, `UC-110`, `FR-337`–`FR-340` — `jiraConnections.test.ts` (24 tests) + `gateway.test.ts` (27 total) + `jiraApiAdapter.test.ts` (11 tests) + `jiraSync.test.ts` (11 tests, new). Admin UI (JIRA-04) — see `UC-110` in `product/USE_CASES.md` and Addendum G in `product/SRS.md`. Jira API token is managed via Admin Settings → App Config (`src/lib/app-config.ts`), not a raw env var — see `JIRA-05c`. Suite: 623/66 passing.

## 9.58 — Admin-Configurable Issue Type Hierarchy (TC-IT-01 to TC-IT-17)

*(Added 2026-06-22, `ISSUETYPE-01` from TODO-List.md Section 19a. Replaces the hardcoded `IssueNodeType` union/`TYPE_MAP`/`LEAF_TYPES` from `JIRA-11`/`JIRA-12` with an admin-configurable registry. Tests in `src/__tests__/issueTypeHierarchy.test.ts`.)*

### Defaults (src/types/issueTypeHierarchy.ts)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-IT-01 | `DEFAULT_ISSUE_TYPES`' topmost (lowest-level) entry | Only `Product` is at the minimum level — the configured hierarchy root | ✅ Automated — "TC-IT-01" |
| TC-IT-02 | `DEFAULT_ISSUE_TYPES` level ordering | `Product < Project < Epic < Story < Sub-task` | ✅ Automated — "TC-IT-02" |
| TC-IT-03 | `buildIssueTypeId()` | Slugifies a label and appends `-2`, `-3`, ... to avoid a collision with an existing id | ✅ Automated — "TC-IT-03" |

### Storage service (src/services/settings/issueTypeHierarchy.service.ts)

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-IT-04 | `readIssueTypeHierarchy()` with no config file on disk | Falls back to `DEFAULT_ISSUE_TYPE_HIERARCHY` | ✅ Automated — "TC-IT-04" |
| TC-IT-05 | `writeIssueTypeHierarchy()` then `readIssueTypeHierarchy()` | Round-trips the exact config written | ✅ Automated — "TC-IT-05" |
| TC-IT-06 | Config file exists but `types` is an empty array (corrupt/invalid) | Falls back to defaults rather than serving an empty type list | ✅ Automated — "TC-IT-06" |

### GET / POST /api/admin/issue-type-hierarchy

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-IT-07 | Unauthenticated `GET` | HTTP 401 | ✅ Automated — "TC-IT-07" |
| TC-IT-08 | Non-admin `GET` | HTTP 200 — any logged-in user can read (the Explore page needs it for every user) | ✅ Automated — "TC-IT-08" |
| TC-IT-09 | Non-admin `POST` | HTTP 403 | ✅ Automated — "TC-IT-09" |
| TC-IT-10 | `POST` with an empty `types` array | HTTP 400 | ✅ Automated — "TC-IT-10" |
| TC-IT-11 | `POST` with two types sharing the same raw match name | HTTP 400, error names the duplicate | ✅ Automated — "TC-IT-11" |
| TC-IT-12 | `POST` that omits an existing built-in type | HTTP 400 — built-in types can be re-mapped but not deleted | ✅ Automated — "TC-IT-12" |
| TC-IT-13 | `POST` with all built-ins plus one new valid custom type | HTTP 200; the custom type is persisted and returned | ✅ Automated — "TC-IT-13" |

### Generalized hierarchy.service.ts with a custom multi-level config

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-IT-14 | A custom 2-level hierarchy ("Widget" root, "Gadget" one level down), Gadget has no explicit parent | Prefix inference correctly links Gadget to the Widget sharing its key prefix — generalized from the old hardcoded "must be Epic" rule | ✅ Automated — "TC-IT-14" |
| TC-IT-15 | The topmost level of a custom hierarchy with no parent | Never flagged as an orphan | ✅ Automated — "TC-IT-15" |
| TC-IT-16 | An issue type not present in the configured types at all | Never receives an inferred parent — unrecognized types are not guessed at | ✅ Automated — "TC-IT-16" |
| TC-IT-17 | `buildRelationGraph()` focus type resolution for a custom type | Resolves to the configured custom label, not the old hardcoded `TYPE_MAP` | ✅ Automated — "TC-IT-17" |

**Live (non-mocked) verification:** added a custom "Strategic Theme" type through the real Admin Settings → Issue Type Hierarchy screen, saved it, confirmed it persisted via a fresh page load and API fetch, then confirmed the real "Agile Jordan" Jira data (`AJ-26`/`AJ-27`/`AJ-28`) still resolved its types and hierarchy correctly through the now-dynamic config before cleaning up the test type.

**Related:** `ISSUETYPE-01`, `JIRA-11`, `JIRA-12`. Suite: 660/69 passing.

## 9.59 — "Sync Jira" Dashboard Button for Any Logged-In User (TC-JIRA-54 to TC-JIRA-60)

*(Added 2026-06-22, `JIRA-14` from TODO-List.md Section 19a. Tests in `src/__tests__/jiraDashboardSync.test.ts`.)*

### resolveActiveJiraConnection() — src/services/jira/connectionSyncRunner.ts

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-JIRA-54 | No `JiraConnection` rows exist | Returns `null` | ✅ Automated — "TC-JIRA-54" |
| TC-JIRA-55 | Multiple connections, some synced at different times, one never synced | Returns the connection with the most recent `lastSyncAt` | ✅ Automated — "TC-JIRA-55" |
| TC-JIRA-56 | Multiple connections, none has ever synced | Returns the most recently created connection | ✅ Automated — "TC-JIRA-56" |

### POST /api/jira/sync

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-JIRA-57 | Unauthenticated | HTTP 401 | ✅ Automated — "TC-JIRA-57" |
| TC-JIRA-58 | Logged in as a non-admin (`scrum_master`) role | HTTP 200 — no admin role check, by explicit product decision | ✅ Automated — "TC-JIRA-58" |
| TC-JIRA-59 | No Jira connection configured at all | HTTP 404 with a message pointing to Admin Settings | ✅ Automated — "TC-JIRA-59" |
| TC-JIRA-60 | Successful sync | Response includes `connectionName` identifying which connection was auto-resolved and used | ✅ Automated — "TC-JIRA-60" |

**Live (non-mocked) verification:** created a temporary `scrum_master`-role test account, logged in, confirmed the "Sync Jira" button (in `DashboardTopbar.tsx`'s right rail) is visible and clickable for a non-admin user, clicked it, and got a real successful sync of 27 issues from the real "Agile Jordan" Jira connection — then deleted the temporary test account. Also re-ran the existing 25-test `jiraConnections.test.ts` suite after refactoring the admin per-connection route to call the new shared `runJiraConnectionSync()` — all 25 passed unmodified, confirming the extraction didn't change existing admin behavior.

**Related:** `JIRA-14`, `JIRA-07`. Suite: 667/70 passing.

## 9.60 — Role-Based Delivery Coaching Insights (TC-RBC-01 to TC-RBC-09 + edge cases)

*(Added 2026-06-23, `RBC-01`–`RBC-20` from TODO-List.md Section 16. Tests in `src/__tests__/roleBasedCoaching.test.ts`. Pure interpretation layer over `DashboardMetrics` — no new domain calculations to test beyond confidence/severity derivation.)*

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-RBC-01a–g | Each of the 7 category generators called with the same fixture metrics | Each returns a non-null `RoleBasedCoachingInsight` with the matching `category` | ✅ Automated |
| TC-RBC-01h | `visibleCategoriesForRole()` for all 6 `AppRole` values plus `null` | Matches FR-346's exact mapping (Manager → 3 categories EM-first, Admin → all 7, User/unknown → Team Lead) | ✅ Automated |
| TC-RBC-02 | Same metrics run through Scrum Master and Product Owner generators | `focusAreas`/`recommendedActions` differ between categories | ✅ Automated |
| TC-RBC-03 | Every generated category's `evidence[]` | Every `detail` string contains a real injected numeric value (regex digit check) | ✅ Automated |
| TC-RBC-04 | 5 blocked items in fixture metrics | Scrum Master `weakPoints` includes the literal count "5" | ✅ Automated |
| TC-RBC-05 | `ceremonyAdvice` for two different categories from the same metrics | `dailyStandup` is non-empty when triggers fire; identical (by deep equality) across categories | ✅ Automated |
| TC-RBC-06 | Sprint fixture with `carryoverCount: 4` | Product Owner `preventionAdvice` is non-empty | ✅ Automated |
| TC-RBC-07 | All 7 categories' `nextSprintSuggestions` | Non-empty, each suggestion >10 characters (content-bearing, not a stub) | ✅ Automated |
| TC-RBC-08 | Same fixture run with `dataQuality.band` Excellent vs Critical | Critical run's `confidence.score` is strictly lower (validates the ×0.5 downgrade multiplier) | ✅ Automated |
| TC-RBC-09 | All `MetricConfidenceMap` sample sizes zero | `confidence.band === 'N/A'`, `confidence.score === 0`, `reason` contains no `%` (no fabricated number) | ✅ Automated |
| — | Zero-issue fixture run through `generateAllCoachingInsights()` for `admin` | Does not throw | ✅ Automated |
| — | Empty `throughput.sprint.sprints` array | Product Owner generator does not throw | ✅ Automated |
| — | Confidence inputs at exactly 80/60/40 | Bands resolve to High/Medium/Low respectively (boundary check) | ✅ Automated |
| — | `metrics.relations` undefined | Scrum Master generator does not throw (defensive `?? []` via `getRelations()`) | ✅ Automated |

**Related:** `RBC-01`–`RBC-20`. Suite: 689/71 passing.

## 9.61 — Coaching Insights Redesign & Encouragement Enhancements (TC-RBC-10 to TC-RBC-13)

*(Added 2026-06-26, `RBC-21`–`RBC-26` from TODO-List.md Section 16. Tests in `src/__tests__/coachingTrend.test.ts` and `src/__tests__/coachingEvidenceLink.test.ts`. Presentation redesign of §9.60's page; only the two new derived-data helpers (severity trend, evidence routing) get dedicated unit tests — the UI components themselves are covered by the unchanged §9.60 generator tests plus manual verification.)*

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-RBC-10 | `computeSeverityTrend('medium', 'critical')`, `computeSeverityTrend('low', 'high')` | Returns `'improved'` (lower urgency rank than before) | ✅ Automated |
| TC-RBC-11 | `computeSeverityTrend('critical', 'medium')`, `computeSeverityTrend('high', 'low')` | Returns `'worsened'` (higher urgency rank than before) | ✅ Automated |
| TC-RBC-12 | `computeSeverityTrend('critical', 'critical')`, `computeSeverityTrend('low', 'low')` | Returns `'same'` | ✅ Automated |
| TC-RBC-13 | `resolveEvidenceRoute()` called with known metric-family keys (`flow.*`, `throughput.kanban.*`, `throughput.sprint.*`, `relations.*`, `dataQuality.*`, `capacity[].*`) and an unknown key | Known keys resolve to their mapped `/dashboard/*` route; unknown key resolves to `null` | ✅ Automated |

**Related:** `RBC-21`–`RBC-26`, `FR-353`, `FR-354`. Suite: 694/73 passing.

---

## 9.62 — Multi-Tenant Phase 1: `scopedRepository` Tenant-Isolation Guarantee (TC-ORG-01 to TC-ORG-12)

*(Added 2026-06-27, `ORG-04`/`ORG-05`/`ORG-08` from TODO-List.md Section 20a. Tests in `src/__tests__/scopedRepository.test.ts`, covering `src/server/tenancy/scopedRepository.ts`. **Not on `main`** — this module and its tests exist only on `feature/org-phase1-tenant-isolation`, unmerged; listed here per CLAUDE.md §55's documentation-as-part-of-Definition-of-Done so test coverage isn't invisible just because the branch hasn't merged. Excluded from `main`'s suite totals elsewhere in this document until merged. Every Prisma call is mocked; these are pure contract tests against `scopedRepository`'s own injection logic, not the database.)*

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-ORG-01 | `repo.user.findMany({ where: { isActive: true } })` | Calls `prisma.user.findMany` with `organizationId` merged into `where` | ✅ Automated (feature branch) |
| TC-ORG-02 | `repo.importLog.findMany({ where: { organizationId: ORG_B } })` called by a repo scoped to `ORG_A` | Caller-supplied `organizationId` is overwritten, not merged — `where` ends up `{ organizationId: ORG_A }` only | ✅ Automated (feature branch) |
| TC-ORG-03 | `repo.user.findUnique({ where: { id: 'user-1' } })` | Rewritten to call `prisma.user.findFirst` (not `findUnique`) with `id` and `organizationId` both in `where` | ✅ Automated (feature branch) |
| TC-ORG-04 | `repo.importLog.create({ data: { userId, status } })` | `organizationId` injected into `data` | ✅ Automated (feature branch) |
| TC-ORG-05 | `repo.user.create({ data: { ...,  organizationId: ORG_B } })` called by a repo scoped to `ORG_A` | Caller-supplied `organizationId` in `data` is overwritten with `ORG_A` | ✅ Automated (feature branch) |
| TC-ORG-06 | `repo.jiraConnection.update({ where: { id: 'conn-1' }, data: {...} })` | `organizationId` injected into `where`; `data` passed through unchanged | ✅ Automated (feature branch) |
| TC-ORG-07 | `repo.dashboardSnapshot.delete({ where: { id: 'snap-1' } })` | `organizationId` injected into `where` — proves org A cannot delete org B's row by ID alone | ✅ Automated (feature branch) |
| TC-ORG-08 | `repo.user.count()` (no arguments) | Calls `prisma.user.count` with `{ where: { organizationId: ORG_A } }` | ✅ Automated (feature branch) |
| TC-ORG-09 | `repo.notification.createMany({ data: [item1, item2 (with organizationId: ORG_B)] })` | `organizationId` injected/overwritten on **every** array item, not just the first | ✅ Automated (feature branch) |
| TC-ORG-10 | Two `scopedRepository` instances created for `ORG_A` and `ORG_B`, each calling `userAddRequest.findMany()` | Each call is independently scoped to its own org — no shared mutable state leaks between instances | ✅ Automated (feature branch) |
| TC-ORG-11 | `repo.jiraConnection.findMany()` (no arguments) | Still injects `{ where: { organizationId: ORG_A } }` even with nothing to merge into | ✅ Automated (feature branch) |
| TC-ORG-12 | `repo.importLog.updateMany({ where: { status: 'failed' }, data: {...} })` | `organizationId` injected into `where`; `data` passed through unchanged | ✅ Automated (feature branch) |

**Related:** `ORG-04`, `ORG-05`, `ORG-08`. Suite on `feature/org-phase1-tenant-isolation`: 748/78 passing (12 of those tests are this section). `main`'s suite (736/77) does not include these until the branch merges.

## 9.63 — EP-012 Email Verification (TC-EV-01 to TC-EV-10)

*(Added 2026-07-04. Tests in `src/__tests__/emailVerification.test.ts`. `prisma` and `safeAuditEvent` are mocked; `POST /api/auth/verify-email` is invoked directly as a real Next.js route handler against a hand-built `Request`, not simulated inline, so the test exercises the actual route code.)*

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-EV-01 | `generateVerificationToken()` | Returns a 64-character lowercase hex string | ✅ Automated |
| TC-EV-02 | `generateVerificationToken()` called twice | Two calls never produce the same token | ✅ Automated |
| TC-EV-03 | `EMAIL_VERIFICATION_TTL_HOURS` | Is a positive number (24) | ✅ Automated |
| TC-EV-04 | `buildVerificationEmail('Ali', 'ali@test.com', 'abc123token', 'https://app.example.com')` | Both `text` and `html` contain `https://app.example.com/verify-email?token=abc123token` | ✅ Automated |
| TC-EV-05 | `buildVerificationEmail(...)` content | Mentions the recipient's name and states the link expires in 24 hours | ✅ Automated |
| TC-EV-06 | `POST /api/auth/verify-email` with a valid, unexpired token | 200, `{ ok: true, alreadyVerified: false }`; `prisma.user.update` called with `emailVerified: true` and both token fields set to `null` | ✅ Automated |
| TC-EV-07 | `POST /api/auth/verify-email` with a token whose `emailVerificationExpires` is in the past | 400, error message matches `/expired/i`; `prisma.user.update` NOT called | ✅ Automated |
| TC-EV-08 | `POST /api/auth/verify-email` with a token matching no user | 400, error message matches `/invalid\|already been used/i` | ✅ Automated |
| TC-EV-09 | `POST /api/auth/verify-email` for a user already `emailVerified: true` | 200, `{ alreadyVerified: true }`; `prisma.user.update` NOT called (idempotent) | ✅ Automated |
| TC-EV-10 | `POST /api/auth/verify-email` with no `token` in the body | 400, error message matches `/required/i` | ✅ Automated |

**Related:** SRS Addendum K (FR-365–FR-367). Manually verified end-to-end beyond these mocked tests: registered a real user against the dev Postgres database, confirmed the token/expiry were persisted, called the live endpoint, confirmed `emailVerified` flipped and the token was cleared, and confirmed replaying the same (now-cleared) token is rejected. Suite total: 800/83 passing (10 of those tests are this section).

## 9.64 — Request-Derived Email Links + EP-013 Forgot/Reset Password (TC-URL-01 to TC-URL-06, TC-PR-01 to TC-PR-12)

*(Added 2026-07-04. `resolveRequestOrigin` tests in `src/__tests__/url.test.ts`; forgot/reset-password tests in `src/__tests__/passwordReset.test.ts`. `prisma`, `system-error-logger`, and `sendEmail` are mocked — `buildPasswordResetEmail` deliberately is NOT mocked, so its real content is exercised too. `POST /api/auth/forgot-password` and `POST /api/auth/reset-password` are invoked as real route handlers against hand-built `Request` objects with a `.nextUrl` property attached, matching the `NextRequest` shape `resolveRequestOrigin` expects.)*

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-URL-01 | `resolveRequestOrigin` with no forwarded headers, `Host: localhost:3000` | Returns `http://localhost:3000` | ✅ Automated |
| TC-URL-02 | `resolveRequestOrigin` with `X-Forwarded-Proto: https` and `X-Forwarded-Host: delivery-clarity.onrender.com`, but a different raw `Host` | Forwarded headers win — returns `https://delivery-clarity.onrender.com` | ✅ Automated |
| TC-URL-03 | `resolveRequestOrigin` with `X-Forwarded-Proto` but no `X-Forwarded-Host` | Falls back to the plain `Host` header | ✅ Automated |
| TC-URL-04 | `resolveRequestOrigin` with no forwarded headers at all, HTTPS `nextUrl` | Falls back to `nextUrl.protocol` — returns `https://example.com` | ✅ Automated |
| TC-URL-05 | `resolveRequestOrigin` with a comma-separated `X-Forwarded-Host: "deliveryclarity.app, internal-lb"` | Takes only the first value | ✅ Automated |
| TC-URL-06 | `normalizeAppUrl('deliveryclarity.app/')` / `normalizeAppUrl(undefined)` | Adds `https://` and strips trailing slash; falls back to `http://localhost:3000` when undefined (regression check — unchanged) | ✅ Automated |
| TC-PR-01 | `PASSWORD_RESET_TTL_HOURS` | Equals `1` (shorter than `EMAIL_VERIFICATION_TTL_HOURS = 24`) | ✅ Automated |
| TC-PR-02 | `buildPasswordResetEmail('Ali', 'reset-token-abc', 'https://app.example.com')` | Both `text` and `html` contain `https://app.example.com/reset-password?token=reset-token-abc` | ✅ Automated |
| TC-PR-03 | `buildPasswordResetEmail(...)` content | Mentions the recipient's name and states the link expires in 1 hour | ✅ Automated |
| TC-PR-04 | `POST /api/auth/forgot-password` for an existing, active user | 200; `prisma.user.update` called (token+expiry stored); `sendEmail` called | ✅ Automated |
| TC-PR-05 | `POST /api/auth/forgot-password` for a non-existent email vs. an existing one | Both return byte-identical `{ ok: true, message: ... }` responses — no enumeration | ✅ Automated |
| TC-PR-06 | `POST /api/auth/forgot-password` for a deactivated (`isActive: false`) account | 200 generic response, but `sendEmail` is NOT called | ✅ Automated |
| TC-PR-07 | `POST /api/auth/forgot-password` with an invalid email format | 400, error matches `/valid email/i` | ✅ Automated |
| TC-PR-08 | `POST /api/auth/forgot-password`, 6th request from the same IP within an hour | 429, error matches `/too many/i` | ✅ Automated |
| TC-PR-09 | `POST /api/auth/reset-password` with a valid, unexpired token | 200; `prisma.user.update` sets a new `passwordHash`, clears both token fields to `null`, sets `mustChangePassword: false` | ✅ Automated |
| TC-PR-10 | `POST /api/auth/reset-password` with an expired token | 400, error matches `/expired/i`; `prisma.user.update` NOT called | ✅ Automated |
| TC-PR-11 | `POST /api/auth/reset-password` with a token matching no user | 400, error matches `/invalid\|already been used/i` | ✅ Automated |
| TC-PR-12 | `POST /api/auth/reset-password` with a weak new password | 400 before even querying for the token (`prisma.user.findUnique` NOT called) | ✅ Automated |

**Related:** SRS Addendum L (FR-368–FR-372). Manually verified end-to-end beyond these mocked tests, against the real dev database and a running dev server: registered a user, requested a password reset, confirmed the token and 1-hour expiry were persisted, called the live reset endpoint, confirmed the *old* password now fails `POST /api/auth/login` and the *new* one succeeds, confirmed replaying the same reset token is rejected, and screenshotted `/login` (new "Forgot password?" link), `/forgot-password`, and `/reset-password`. Suite total: 818/85 passing (18 of those tests are this section).

## 9.65 — EP-016 Super Admin Protection (extends admin users API tests)

*(Added 2026-07-04. Tests added to the existing `src/__tests__/adminUsers.test.ts` rather than a new file, since they extend the same route's test coverage. One pre-existing test — "admin users API updates role and active state" — required adding a `prisma.user.findUnique` mock since `PATCH` now performs that lookup before updating, for any target.)*

| ID | Test | Expected | Status |
|----|------|----------|--------|
| — | Another admin attempts to change a super-admin's role via `PATCH /api/admin/users` | 403, `"Super admin accounts can only be modified by themselves."`; `prisma.user.update` NOT called | ✅ Automated |
| — | The super-admin modifies their own account (`session.userId` equals the target id) | 200, update succeeds normally, cloud sync still triggered | ✅ Automated |
| — | `PATCH /api/admin/users` targeting a nonexistent user id | 404, `"User not found."` | ✅ Automated |
| — | Another admin attempts to delete a super-admin via `DELETE /api/admin/users` | 403, `"Super admin accounts cannot be deleted."`; `prisma.user.delete` NOT called | ✅ Automated |

**Related:** SRS Addendum M (FR-373–FR-375). Manually verified end-to-end beyond these mocked tests, against the real dev database with a running dev server and two real logged-in sessions: created the actual requested super-admin account (Ali Abu Ras, `admin@deliveryclarity.app`), created a throwaway second admin account, confirmed that account gets 403 on role-change/deactivate/delete attempts against the super-admin, confirmed the super-admin can still edit their own account and completes the standard `mustChangePassword` first-login flow, screenshotted Admin → User Management showing the "Super Admin" badge and locked-down row, then deleted the throwaway admin. Suite total: 822/85 passing (4 net-new tests; a 5th test — the `findUnique` mock fix — is a correction to a pre-existing test, not new coverage).

## 9.66 — EP-017 Per-User Local-Only Data Storage Mode

*(Added 2026-07-04. New files `jiraParser.test.ts`, `localImportHistory.test.ts`, `localUpload.test.ts`, `profileApi.test.ts`; two tests appended to the existing `auth.test.ts`.)*

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-PARSE-01 | `parseJiraFile` parses a Node `Buffer` (existing server upload path) | Issues parsed correctly, unchanged from pre-EP-017 behavior | ✅ Automated — `jiraParser.test.ts` |
| TC-PARSE-02 | `parseJiraFile` parses an `ArrayBuffer` identically to the equivalent `Buffer` (local-mode browser path) | Same `issues`/`headers`/`sheetName` from both input types | ✅ Automated — `jiraParser.test.ts` |
| TC-LIH-01–09 | `localImportHistory.ts`: empty-list default, add/list/prepend-order, 20-item cap, remove-one, clear-all, corrupt-JSON and malformed-entry resilience, quota-exceeded reporting | All pass without throwing; malformed data is dropped, never crashes the caller | ✅ Automated — `localImportHistory.test.ts` |
| TC-LU-01–06 | `localUpload.ts`: extension parsing, unsupported-extension rejection, oversize rejection, `fetch` never called for any local-mode path, output shape matches `POST /api/upload`'s `{metrics, warnings, columnMapping}`, validation failures surfaced instead of saved | All pass; `global.fetch` mock confirms zero network calls | ✅ Automated — `localUpload.test.ts` |
| TC-PROF-01–05 | `/api/profile`: `GET` returns `dataStorageMode` (defaulting invalid/unset to `"cloud"`), `PATCH` accepts/persists `"local"`, `PATCH` rejects an invalid value (400), `PATCH` updates the session, `PATCH` without the field leaves it untouched | All pass | ✅ Automated — `profileApi.test.ts` |
| TC-A-06/07 | Login session picks up `dataStorageMode` from the user record, defaulting to `"cloud"` when unset | `session.dataStorageMode` matches expected value in both cases | ✅ Automated — `auth.test.ts` |

**Related:** SRS Addendum N (FR-376–FR-380). Manually verified the server-side half end-to-end against the real dev database with a running dev server: created a throwaway test user directly via Prisma, logged in through the real `POST /api/auth/login`, confirmed `GET /api/auth/me` and `GET /api/profile` both defaulted `dataStorageMode` to `"cloud"`, `PATCH /api/profile` with `dataStorageMode: "local"` returned 200 and flipped it, `GET /api/auth/me` immediately reflected `"local"` from the session (no re-login needed), an invalid value returned 400, and the `User` row and a `profile_storage_mode_change` audit event were both confirmed directly in the database — then deleted the throwaway user and all related rows. **Not yet driven through an actual browser** (no browser-automation tool available in this session) — recommended before shipping: switch a test account to "This device only" in `/profile` in a real browser, upload a sample export, confirm via DevTools Network tab that no `POST /api/upload` request fires, confirm the dashboard renders and the import appears in local history, then confirm as an admin that no new `ImportLog` row exists for that upload. Suite total: 848/89 passing (22 net-new tests).

## 9.67 — Saved SMTP "From Address" Persistence Fix

*(Added 2026-07-04, backfilled alongside §9.68 after the gap was noticed — this section should have been written at the time the fix shipped. New files `appConfig.test.ts`, `appConfigRoute.test.ts`.)*

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-CFG-01 | An explicitly-saved cloud "from" address is not overridden by `SMTP_*` env vars | `getAppConfig()` returns the saved `from`, not the env value | ✅ Automated — `appConfig.test.ts` |
| TC-CFG-02 | An explicitly-saved cloud Jira token is not overridden by an env token | `getAppConfig()` returns the saved token | ✅ Automated — `appConfig.test.ts` |
| TC-CFG-03 | Env vars still bootstrap SMTP config when the cloud config has nothing saved yet | `getAppConfig()` returns the env-derived config | ✅ Automated — `appConfig.test.ts` |
| TC-CFGR-01 | `PUT /api/admin/app-config` with a blank password field passes the currently-effective password, not `undefined` | `saveSmtpSettings` called with the effective password; `dbSaveError` absent | ✅ Automated — `appConfigRoute.test.ts` |
| TC-CFGR-02 | A genuine DB save failure is surfaced as `dbSaveError` instead of a silent `{ ok: true }` | Response includes `dbSaveError` with the real message | ✅ Automated — `appConfigRoute.test.ts` |

**Related:** SRS changelog v4.21.0 (no dedicated FR/Addendum — a bug fix, not a new requirement). Manually verified end-to-end against the real dev database with a throwaway admin account: confirmed 0 rows existed in `SmtpSettings` before the fix despite prior save attempts, then confirmed `PUT` with a blank password field correctly created the row with the intended "From address" and `GET` reflected `source: "cloud"` instead of falling back to `"env"`. Suite total: 853/91 passing (5 net-new tests).

## 9.68 — EP-018 Settings Hub, Role-Gated Nav, Registration Defaults

*(Added 2026-07-04. New file `register.test.ts`. The tabbed Settings UI itself — `app/profile/page.tsx` and `src/components/settings/*` — has no automated coverage; this repo has no component-testing infrastructure (no jsdom/RTL in `jest.config.js`), same limitation as every other UI-only change in this project.)*

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-REG-01 | Self-registration creates the user with `dataStorageMode: "local"` and `role: "user"` | `prisma.user.create` called with both fields set | ✅ Automated — `register.test.ts` |

**Related:** SRS Addendum O (FR-381–FR-385). Not manually verified through a real browser session in this pass (no browser-automation tool available) — recommended before shipping: log in as each role and confirm the Settings side menu (Profile/Storage/Security) renders and behaves correctly, confirm "My Settings" appears correctly in the account dropdown without visually colliding with the admin-only "Settings" link, confirm the Create User and bulk role-change dropdowns in Admin → User Management no longer offer "User", and confirm a freshly self-registered account lands on "This device only" in its own Settings → Storage tab. Suite total: 854/92 passing (1 net-new test).

## 9.69 — Main Nav Role Filtering + "App Storage" Rename (extends EP-018)

*(Added 2026-07-04, immediate follow-up to §9.68 from a user screenshot. New file `navGroupsForRole.test.ts`.)*

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-NAV-01 | Admin sees the Administration nav group | `getNavGroupsForRole('admin')` includes it | ✅ Automated — `navGroupsForRole.test.ts` |
| TC-NAV-02 | Every non-admin role never sees the Administration group | Excluded for scrum_master/product_owner/manager/c_level/user | ✅ Automated — `navGroupsForRole.test.ts` |
| TC-NAV-03 | Every non-admin role (and null/undefined) never sees the Developer link | Excluded from the Reference group's items | ✅ Automated — `navGroupsForRole.test.ts` |
| TC-NAV-04 | Admin sees the Developer link | Present in the Reference group's items | ✅ Automated — `navGroupsForRole.test.ts` |
| TC-NAV-05 | c_level does not see Flow Health / Sprint & Kanban / Work Explorer | Excluded, matching `allowedRoutePrefixesForRole('c_level')` | ✅ Automated — `navGroupsForRole.test.ts` |
| TC-NAV-06 | A group left with zero items after filtering is dropped, never rendered empty | Every returned group has `items.length > 0` | ✅ Automated — `navGroupsForRole.test.ts` |
| TC-NAV-07 | Admin retains every item — nothing over-filtered for the most privileged role | Filtered item count equals the full source config's item count | ✅ Automated — `navGroupsForRole.test.ts` |

**Related:** SRS Addendum O, FR-386–387. `getNavGroupsForRole()`'s filtering logic is tested directly since this repo has no component-testing infrastructure to render `AppShell.tsx`/`DashboardTopbar.tsx` and click through the menu. Not manually verified through a real browser session (no browser-automation tool available) — recommended before shipping: log in as a non-admin role and confirm the Administration group and Developer link are genuinely absent from both the top nav and mobile nav, and confirm the Storage tab now says "App storage" rather than "Cloud storage." Suite total: 861/93 passing (7 net-new tests).

## 9.70 — Header/Account Menu Identity Cache (no flash on route change)

*(Added 2026-07-05, immediate follow-up to §9.69, revised same day per §9.71 below. `src/lib/currentRole.ts`/`currentRole.test.ts` (role-only cache) were superseded by `src/lib/currentUser.ts`/`currentUser.test.ts` (full identity cache) rather than left as a second, narrower cache alongside the new one — see §9.71.)*

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-USER-01 | `getCachedUser()`/`getCachedRole()` return `null` before any fetch has resolved | `null` | ✅ Automated — `currentUser.test.ts` |
| TC-USER-02 | `fetchCurrentUser()` resolves and populates the cache for subsequent synchronous reads | Cached value matches the fetched user | ✅ Automated — `currentUser.test.ts` |
| TC-USER-03 | Concurrent calls while a fetch is in flight share the same request | `fetch` called exactly once | ✅ Automated — `currentUser.test.ts` |
| TC-USER-04 | A failed fetch resolves to the last known cached value rather than throwing | Previous cached user preserved | ✅ Automated — `currentUser.test.ts` |
| TC-USER-05 | `clearCachedUser()` resets the cache so a subsequent read reflects logout | `getCachedUser()` returns `null` | ✅ Automated — `currentUser.test.ts` |

**Related:** SRS v4.23.1–4.23.2. Not manually verified through a real browser session (no browser-automation tool available) — recommended before shipping: navigate between several pages that each use `AppShell` (e.g. `/`, `/profile`, `/help`) and confirm neither the header nav nor the account menu/username visibly flickers on each navigation. Suite total: 866/94 passing (5 net-new tests, replacing the 4 from the original `currentRole.test.ts`).

## 9.71 — Account Menu ("My Settings") No Longer Flashes "Sign In"

*(Added 2026-07-05, same-day follow-up to §9.70 from a screenshot showing `UserMenu.tsx` doing the exact thing §9.70 just fixed for the nav. No new test file — covered by `currentUser.test.ts` above, since `UserMenu.tsx` now consumes the same cache `AppShell`/`DashboardTopbar` do.)*

**Related:** SRS v4.23.2 (extends 4.23.1). Considered wiring the cached role into `NotificationBell`'s existing `role` prop to remove its own separate `/api/auth/me` fetch too, but that component only reads `roleProp` once at mount with no effect to re-sync if the prop changes afterward — passing a value that starts `null` and resolves later would leave its admin-only badge permanently stuck unresolved, a worse regression than the one being fixed. Deliberately left untouched; flagged here rather than silently skipped. Not manually verified through a real browser session (no browser-automation tool available) — recommended before shipping: log in, navigate between several pages, and confirm the account menu's username never flashes to "Sign in."

## 9.72 — EP-020: Workspace-Scoped Live Dashboard Data + Jira Connection Resolution (P0 security fix)

*(Added 2026-07-05. From a large merged request; two research passes established most of it was already correct — see SRS v4.24.0 for the full before/after. This section covers only the genuine bug: `data/latest-metrics.json` was a single file shared by the whole server process, and `resolveActiveJiraConnection()` was an unfiltered `findMany()` — both fixed to scope by workspace/user.)*

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-JIRA-51 | Two different scope keys (`ws:team-a`, `user:solo-b`) never read each other's metrics; a brand-new scope with nothing written returns `null` | Each scope's file is independent; new scope is empty, never falls back to another's data | ✅ Automated — `latestMetricsStorage.test.ts` |
| TC-JIRA-52 | `writeLatestMetrics`/`readLatestMetrics` reject a malformed scope key (e.g. path-traversal-shaped input) | Throws rather than silently writing/reading an arbitrary path | ✅ Automated — `latestMetricsStorage.test.ts` |
| TC-JIRA-61 | `resolveActiveJiraConnection({ userId, workspaceId })` queries only connections the caller created or that belong to their workspace | `findMany` called with `where: { OR: [{ createdByUserId }, { workspaceId }] }`, never unfiltered | ✅ Automated — `jiraDashboardSync.test.ts` |
| TC-ISO-07 | `GET /api/metrics/latest` resolves and reads the caller's own workspace scope key | `readLatestMetrics` called with `ws:<caller's workspace id>`, never another workspace's key | ✅ Automated — `workspaceIsolation.test.ts` |
| TC-ISO-08 | `GET /api/metrics/latest` returns `available: false` for a user with no scoped file yet | Never falls back to another user's data | ✅ Automated — `workspaceIsolation.test.ts` |
| TC-CS-10 | Workspace/user-scoped metrics files under `data/metrics/` are individually included in backup bundles | Each scoped file appears in the manifest under its own `metrics/<name>` key | ✅ Automated — `cloudRestoreHardening.test.ts` (rewritten; previously tested the now-removed single `latest-metrics.json` entry) |
| TC-CS-10b | Two different workspaces' metrics files never collide or overwrite each other inside one backup bundle | Both files present with their own distinct content | ✅ Automated — `cloudRestoreHardening.test.ts` |

**Manual end-to-end verification (real dev database, throwaway accounts):** created two cloud-mode users (`ep020-verify-a@example.com`, `ep020-verify-b@example.com`), each with their own workspace and entitlement. Before any upload, both correctly showed `available: false`. Uploaded `public/samples/sample-jira-export.csv` as user A via the real `POST /api/upload` route — confirmed `data/metrics/` contained exactly one file (`ws_<A's workspace id>.json`), user A's `GET /api/metrics/latest` returned the uploaded data, and **user B's still returned `available: false`** — no leak. Then uploaded as user B and confirmed both users now had independent, correct data (two separate files, neither overwritten), i.e. isolation holds in both directions. Both accounts, their workspaces, entitlements, and the two scoped metrics files were deleted after verification.

**Not covered by this fix (documented in SRS v4.24.0, tracked as separate follow-on work):** password show/hide toggles, a Cancel button on the cloud-storage "Change provider" form, and a manual admin tool to reset a non-`deliveryclarity.app` user's workspace data.

**Relationship to the separate, larger multi-tenant redesign:** `product/MULTI_TENANT_ORG_DESIGN.md` describes a planned `Organization`-level model and a `scopedStorage()` helper (§3a, Phase 8) intended to eventually cover this same class of problem; that work is partially built on an unmerged branch (`feature/org-phase1-tenant-isolation`) and does not yet touch `latestMetricsStorage.ts`. This fix intentionally reuses the current, already-in-production `Workspace` model (the same one `ImportLog`/`DashboardSnapshot`/`JiraConnection` already scope by) rather than waiting on that larger redesign, since the leak needed closing now. Reconciling the two is flagged as follow-up work for whenever that branch merges.

## 9.73 — EP-021: Password/Secret Show-Hide Toggle Everywhere

*(Added 2026-07-05. Third follow-on branch from the same large merged request as §9.72 — this section covers §4 of that request, password field visibility.)*

New shared `src/components/ui/PasswordInput.tsx` — drop-in replacement for `<input type="password">`, toggling `type="password"`/`"text"` via a trailing icon button. Applied at all 14 confirmed locations:

| Area | File |
|---|---|
| Login | `app/login/page.tsx` |
| Register | `app/register/page.tsx` |
| Reset password (2 fields) | `app/reset-password/page.tsx` |
| Forced change-password (3 fields) | `app/change-password/page.tsx` |
| Voluntary change-password (3 fields) | `src/components/settings/SecurityTab.tsx` |
| Admin SMTP password | `src/components/admin/AppConfigPanel.tsx` (via its shared `Field` helper) |
| Jira API token | `src/components/admin/JiraConnectionsPanel.tsx` |
| S3 Access Key ID / Secret Access Key | `app/admin/settings/page.tsx` |
| Azure connection string | `app/admin/settings/page.tsx` |
| Admin "Add User" temp password | `app/admin/settings/page.tsx` |

**Not included:** GCP's Service Account JSON field is a `<textarea>` (already unmasked by default) — a different problem than a password-toggle, tracked as a separate smaller follow-up rather than silently bundled in here.

**Automated coverage:** none. This repo has no component-testing infrastructure (`jest.config.js`: `testEnvironment: 'node'`, `.test.ts` only, no jsdom/React Testing Library) — the same pre-existing limitation already documented for EP-017/018's settings-tab UI. Full suite remains 872/94 passing (unchanged — no tests added or broken by this change); `npx tsc --noEmit` clean.

**Manual verification performed:** started the dev server and curled `/login` (Tailwind-styled form) and `/register` (SCSS-Module-styled form) — the two different styling systems this component has to work correctly under. Both returned HTTP 200 with the eye icon (`0153-eye-open.svg`) and `aria-label="Show password"` present in the server-rendered HTML, confirming the component mounts and renders without error under both styling conventions.

**Not verified:** the actual click-to-reveal interaction in a live browser — no browser-automation tool was available this session. **Recommended before shipping:** manually click the eye icon on at least one Tailwind field (e.g. `/login`) and one SCSS-Module field (e.g. `/register`) and confirm the password becomes visible as plain text and the icon/aria-label swap to "Hide password"; also spot-check one of the admin secret fields (Jira token or S3 keys) since those sit inside more complex parent layouts.

## 9.74 — EP-022: Cancel Button on Cloud Storage "Change Provider"

*(Added 2026-07-05. Fourth follow-on branch from the same large merged request as §9.72/§9.73 — this section covers §5, the Change Provider Cancel button.)*

`CloudStorageSettings` (`app/admin/settings/page.tsx`) gained a `formSnapshot` state — captured directly from the fetched settings on initial load, and refreshed after every successful save — plus a Cancel button next to Save/Test/Upload:

| Behavior | Expected |
|---|---|
| Click Cancel with no unsaved changes | Immediately reverts (no-op since nothing differs) and exits edit mode |
| Click Cancel with unsaved changes (different provider selected or a credential field typed) | Confirmation dialog appears (`ConfirmDeleteDialog`, `danger={false}`, "Discard unsaved changes?") before anything is reverted |
| Confirming the discard | Provider selection and all credential fields (S3/Azure/GCP) reset to `formSnapshot`; status message cleared; edit mode closes, returning to the locked view showing the previously-saved provider |
| Cancel itself | Never calls `handleSave`, never calls `pushToCloud`/persists anything — purely a client-side state revert |
| After a successful Save | The snapshot updates to the just-saved state, so a subsequent Cancel reverts to that, not the original page-load state |

**Automated coverage:** none (same no-component-testing-infrastructure limitation as §9.73). Full suite unchanged at 872/94 passing; `npx tsc --noEmit` clean.

**Attempted but inconclusive verification:** tried the same curl-smoke-check technique used for §9.73 (create a throwaway admin, log in, curl the page) but `CloudStorageSettings` only renders its real markup after a client-side data fetch resolves, and the tab itself is selected via a `?tab=cloud` query param read client-side — so the pre-hydration HTML curl receives never contains this component's actual content, unlike the standalone `/login`/`/register` pages used for §9.73. This was not swept under the rug — full typecheck and test suite were run instead, and the existing `isLocked`/`editMode`/`savedProvider` state machine this change plugs into was reviewed carefully to confirm Cancel's revert logic is consistent with it.

**Recommended manual QA before shipping:** as an admin, go to Admin Settings → Cloud Storage, click "Change provider," change the provider dropdown and/or type a credential, click Cancel — confirm a confirmation prompt appears, confirm it, and verify (a) the form returns to showing the previously-saved provider as locked/active, (b) reloading the page confirms nothing was actually persisted.

## 9.75 — EP-023: Manual Admin Reset for Non-`deliveryclarity.app` Users

*(Added 2026-07-05. Fifth and final follow-on branch from the same large merged request as §9.72–§9.74 — this section covers §6, resetting external users' data. Manual-only per the user's explicit choice when asked how this should run.)*

New `src/services/settings/userReset.service.ts` — `previewUserReset(userId)` (dry-run) and `resetUserData(userId, actorId)` (actual delete + one `user_data_reset` audit event). Both refuse, server-side, any target whose email ends in `@deliveryclarity.app` (case-insensitive).

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-RESET-01 | `previewUserReset` on an internal `@deliveryclarity.app` account | `blocked: true`, no count queries issued | ✅ Automated — `userReset.test.ts` |
| TC-RESET-02 | `previewUserReset` on an eligible external user | Correct `importLogs`/`dashboardSnapshots`/`jiraConnections`/`hasScopedMetricsFile` counts | ✅ Automated |
| TC-RESET-03 | `previewUserReset` on an unknown user id | Throws `User not found.` | ✅ Automated |
| TC-RESET-04 | `resetUserData` on an internal account | `success: false`, clear error, **zero** `deleteMany` calls issued | ✅ Automated |
| TC-RESET-05 | `resetUserData` on an external user with data | Deletes the rows, deletes the scoped metrics file, logs `user_data_reset` audit event with counts | ✅ Automated |
| TC-RESET-06 | `resetUserData` never modifies the `User` row itself | Only `importLog`/`dashboardSnapshot`/`jiraConnection` deletes are called | ✅ Automated |
| TC-RESET-07 | `resetUserData` when no scoped metrics file exists | Succeeds without calling `fs.unlinkSync` | ✅ Automated |
| TC-RESET-08 | `listExternalUsersEligibleForReset` query shape | `where.NOT.email.endsWith === '@deliveryclarity.app'` | ✅ Automated |
| TC-RESET-09 | Scoped-file check prefers the user's workspace scope key | Checks `metrics/ws_<workspaceId>.json`, not a bare user key, when a workspace exists | ✅ Automated |
| TC-RESET-10 | Internal-domain check is case-insensitive | `Someone@DeliveryClarity.App` is still blocked | ✅ Automated |

**API:** `GET`/`POST /api/admin/users/:id/reset-preview` and `/reset`, admin-only (401/403 otherwise). Reset returns 409 with the blocked reason for internal accounts.

**UI (`app/admin/users/page.tsx`):** per-row "Reset data" button — hidden entirely for `@deliveryclarity.app` accounts — opens a dry-run preview modal before a confirm step. A header-level "Reset external users' data" button opens a bulk flow: select users (only non-internal accounts are ever listed), preview all selected at once, then a single confirmation step listing every selected user's counts before executing sequentially.

**Manual end-to-end verification (real dev database, throwaway accounts):** created a throwaway admin, an external user (`ep023-verify-external@example.com`) seeded with 1 `ImportLog` + 1 `DashboardSnapshot`, and an internal user (`ep023-verify-internal@deliveryclarity.app`). Confirmed:
- Preview and reset on the internal account both returned a clear refusal (reset: HTTP 409) — no rows touched.
- Preview on the external account correctly reported 1 import log, 1 snapshot, 0 Jira connections.
- Reset on the external account returned `{ importLogsDeleted: 1, snapshotsDeleted: 1, jiraConnectionsDeleted: 0 }`; confirmed directly against the database afterward that both rows were gone, the audit event was logged with the exact expected description, and the account itself was untouched (`isActive: true`, `role: 'user'`, login still worked).

All three throwaway accounts (and their workspaces/data) were deleted after verification. Full suite 882/95 passing, typecheck clean.

## 9.76 — EP-025: Members Directory Restricted to the Super-Admin Account

*(Added 2026-07-05, P1 security fix. From a screenshot showing a regular member's session listing another user's email through the Members directory.)*

New `SessionData.isSuperAdmin` (set at login, returned by `GET /api/auth/me`, carried by `CurrentUser`/`getCachedIsSuperAdmin()`). Distinct from `role: 'admin'` — a regular admin does **not** pass this check.

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-MD-09 | `GET /api/members` as a regular admin (`role: 'admin'`, `isSuperAdmin: false`) | 403, `prisma.user.findMany` never called | ✅ Automated — `members.test.ts` |
| TC-MD-10 | `GET /api/members` as the super-admin | 200, real member data returned | ✅ Automated |
| TC-NAV-07 (corrected) | `getNavGroupsForRole('admin', true)` — super-admin sees every item | All source items visible | ✅ Automated — `navGroupsForRole.test.ts` (previously asserted this for `role: 'admin'` alone, which is no longer sufficient) |
| TC-NAV-08 | `getNavGroupsForRole('admin')` (no `isSuperAdmin`) | `members` item absent | ✅ Automated |
| TC-NAV-09 | Every role with `isSuperAdmin: false` | `members` item absent for all of them | ✅ Automated |
| TC-NAV-10 | `getNavGroupsForRole('user', true)` | `members` item present regardless of role | ✅ Automated |

**API/page:** `GET /api/members` returns 403 for anyone without `isSuperAdmin`. `/members` page redirects to `/dashboard` both from its own `GET /api/auth/me` check and defensively if the members fetch itself returns 403 (defense in depth — the API is the real boundary, the page check is just UX).

**Manual end-to-end verification (real dev database, throwaway accounts):** created a throwaway regular admin (`role: 'admin'`, `isSuperAdmin: false`) and a throwaway super-admin (`role: 'admin'`, `isSuperAdmin: true`). Confirmed `GET /api/auth/me` correctly reports `isSuperAdmin: false` for the regular admin; confirmed `GET /api/members` returns 403 (`"Super admin access required."`) for the regular admin and 200 with real member data for the super-admin. Both throwaway accounts deleted after verification.

**Not verified:** the Members nav link actually disappearing in a rendered browser — the nav dropdown's item list isn't present in the server-rendered HTML (client-side dropdown state), so curl-based smoke checks can't confirm it visually; no browser-automation tool was available this session. The underlying filtering logic (`getNavGroupsForRole`) is fully covered by TC-NAV-07–10 above. **Recommended manual QA:** log in as a regular admin and confirm the "Members" link is absent from both the top nav and mobile nav; log in as the super-admin and confirm it's present and the page loads.

## 9.77 — EP-024: Per-User "Bring Your Own Cloud" Storage

*(Added 2026-07-05. From screenshots of the per-user Storage tab and the admin's Cloud Storage Provider picker, with the explicit requirement that every cloud-mode user get their own bucket, never a shared one.)*

New `UserStorageProvider` Prisma model (additive migration `20260705062735_add_user_storage_provider`) and `src/services/storage/userStorageProvider.service.ts`, reusing the existing provider classes/factory (`src/services/storage/{providers,storageProvider}.ts`) and encryption helper (`src/lib/secret-field.ts`) unchanged.

| ID | Test | Expected | Status |
|----|------|----------|--------|
| TC-UBYOC-01–03 | Save rejects config missing required fields per provider (S3 bucket; Azure containerName; GCP bucket + projectId) | `{ ok: false, error }`, no DB write | ✅ Automated — `userStorageProvider.test.ts` |
| TC-UBYOC-04 | Save requires credentials for a brand-new provider (no existing row) | Rejected, no DB write | ✅ Automated |
| TC-UBYOC-05 | Save encrypts credentials via `encryptSecret()`, never stores them in the non-secret `configJson` column | `credentialsEnc` goes through the encryption function; `configJson` never contains a secret value | ✅ Automated |
| TC-UBYOC-06 | Save always resets `verified: false`, even overwriting an already-verified provider | `verified: false, verifiedAt: null` after every save | ✅ Automated |
| TC-UBYOC-07 | Blank credential fields on an *unchanged* provider type keep the existing encrypted blob | Existing `credentialsEnc` preserved | ✅ Automated |
| TC-UBYOC-08 | Blank credentials when *switching* provider type is rejected | Never silently reuses the old provider's (wrong-shaped) credentials | ✅ Automated |
| TC-UBYOC-09/10 | `getUserStorageProviderSafe` never exposes `credentialsEnc` or decrypted values; returns `null` when nothing configured | Safe shape only | ✅ Automated |
| TC-UBYOC-11 | `testUserStorageProvider` sets `verified: true` only on a real provider `.test()` success | — | ✅ Automated |
| TC-UBYOC-12/13 | Test failure (provider-reported error, or a thrown exception) sets `verified: false` with the error recorded | — | ✅ Automated |
| TC-UBYOC-14 | Test with nothing configured yet returns a clear error | — | ✅ Automated |
| TC-UBYOC-15 | Delete removes the row | — | ✅ Automated |
| TC-UBYOC-16 | `getUserStorageProviderStatus` returns `'none'`/`'unverified'`/`'verified'` correctly | — | ✅ Automated |
| TC-UBYOC-17/18 | `getVerifiedUserStorageProviderInstance` returns `null` unless `verified: true`; builds a real instance only when verified | Never calls `createProvider` for an unverified row | ✅ Automated |

**Upload pipeline (`app/api/upload/route.ts`, `merge/route.ts`):** no provider configured → unaffected (App storage, unchanged); saved-but-unverified → `409` with a message pointing at Settings → Storage; verified → existing local scoped-file write unchanged, plus a non-blocking push to the user's own bucket under a fixed key (`delivery-clarity-metrics.json`). `GET /api/metrics/latest` attempts one restore from the user's own bucket when the local scoped file is missing, before returning `available: false`.

**Existing tests updated to avoid a real DB dependency:** `uploadUserId.test.ts`, `workspaceIsolation.test.ts`, and `cloudRestoreHardening.test.ts` now mock `@/services/storage/userStorageProvider.service` — without this, the new upload/metrics-route calls would have silently fallen through to a real (unmocked) database connection in those test files rather than failing loudly, since the calls are wrapped in `.catch(() => null)` for production resilience.

**Manual end-to-end verification (real dev database and dev server, throwaway user in cloud mode):**
1. Baseline: `GET /api/profile/storage-provider` → `config: null`; upload succeeds (`200`) with nothing configured — App storage path unaffected.
2. Saved an S3 config with fake credentials (`PUT /api/profile/storage-provider`) → confirmed `verified: false` in the response; the very next upload attempt returned `409` with the expected "not yet verified" message.
3. `POST /api/profile/storage-provider/test` against the fake bucket → failed with a real, specific AWS error ("The AWS Access Key Id you provided does not exist in our records"), `verified` stayed `false`, `lastError` recorded.
4. `DELETE /api/profile/storage-provider` → the next upload attempt no longer returned the storage-provider `409` — it reached a *different*, unrelated, pre-existing check instead (trial entitlement already consumed, `403`), proving the block was specifically lifted rather than the upload just failing for some other reason.

Throwaway user, workspace, entitlement, and scoped metrics file all deleted after verification.

**Not verified:** an actually-successful `Test connection` against a real S3/Azure/GCP bucket (no real cloud credentials were available this session) — covered instead by TC-UBYOC-11/18's mocked-success unit tests, which exercise the exact same code path (`createProvider()` → `.test()` → `verified: true`) that a real success would. **Recommended manual QA before relying on this in production:** configure a real bucket you control, confirm "Test connection" succeeds, upload a file, and confirm the object actually appears in that bucket under the expected key.

Full suite 905/96 passing, typecheck clean.

## 9.78 — Home/Upload Page Redesign (Dark Theme Consistency)

*(Added 2026-07-05. Visual-only pass on `app/page.tsx` — no state, handler, or existing shared component was changed, so no new automated tests were needed; the existing full suite already covers the untouched upload/merge/sample-data logic.)*

New `app/page.module.scss` using only existing dark Theme D tokens (`--dc-bg/s1/s2/s3`, `--dc-accent`, `--dc-p1/p2/p3`, `--dc-bdr`) — no new colour values introduced. Every inline `style={{...}}` initially written was moved into proper module classes once the local ESLint inline-style rule flagged them (none were genuinely dynamic/data-driven, so none qualified for the CSS-custom-property exception).

**Automated checks:** `npx tsc --noEmit` clean; `npx eslint app/page.tsx --max-warnings=0` clean; `npx stylelint app/page.module.scss --max-warnings=0` clean; full suite unchanged at 905/96 passing (zero logic touched).

**Manual verification (real dev server, throwaway account):** the page requires an authenticated session — confirmed anonymous requests still redirect to `/login` (pre-existing, unchanged). Logged in as a throwaway user and fetched the page: confirmed the new copy ("Drag it in, we'll take it from here," "Try a sample first," "Merge multiple files," "See everything it does") and the new `page_page__*` module class all render correctly. Not independently re-verified: the drop-zone icon's hover/float animation and drag-and-drop interaction visually (no browser-automation tool available this session) — the component logic itself (file handlers, drag/drop event wiring) is unchanged from the working, previously-shipped version.

## 9.79 — Home/Upload Page Redesign Round 2 (Real Logo + Always-Dark Hero + Energy)

*(Added 2026-07-05. Follow-up to §9.78 after user feedback on a screenshot: wrong logo, page should look like `/login`/`/register`, add a bouncing arrow, make the page feel more energetic. Visual-only pass again — no state, handler, or existing shared component was changed.)*

**Root cause check before changing anything:** the §9.78 screenshot rendered light/blue instead of dark because the app's live theme was set to light and §9.78's classes correctly used theme-reactive `--dc-*` tokens — not a bug, but not what "look like login/register" needs either, since those two pages are intentionally always-dark regardless of the site theme toggle.

**Logo fix:** `AppShell.tsx` and `DashboardTopbar.tsx` (confirmed via `ps`/`grep` as the only two live, actually-rendered top bars in the app) both hardcoded a placeholder lightning-bolt path in a blue box instead of the real brand mark at `public/logo/delivery-clarity-logo-icon.svg`. A third component with the same bug, `DCTopbar.tsx`, was checked and confirmed dead code (not imported anywhere) and intentionally left untouched. Both live components now render the real icon via `next/image`.

**Hero background:** `app/page.tsx` now renders the existing `AnimatedDataBackground` canvas (already used on `/login`/`/register`, already respects `prefers-reduced-motion`) plus a vignette, matching the login page's `.wrapper`/`.bg`/`.vignette` pattern. `app/page.module.scss` colors were converted from theme-reactive `var(--dc-*, fallback)` to fixed literal dark hex values (the same values that were already the fallbacks) so the hero no longer changes with the site theme toggle — consistent with, not a deviation from, `/login`/`/register`'s existing behavior.

**Energy touches:** drop-zone icon animation replaced with a more pronounced `arrowBounce` (travels down ~11px, springs back, continuous loop) per "arrow follows down and goes up from the bottom"; added `badgeGlow` (pulsing glow on the top pill) and `dropzonePulse` (breathing box-shadow on the drop zone). All three new animations are gated by the existing `prefers-reduced-motion` media query, same as before.

**Automated checks:** `npx tsc --noEmit` clean. `npx eslint` on every changed file: `app/page.tsx` clean; `AppShell.tsx`/`DashboardTopbar.tsx` each showed pre-existing warnings confirmed via `git diff` to predate this change (unrelated dropdown status-dot inline styles) and left untouched. `npx stylelint` on all four changed `.module.scss` files clean. Full suite unchanged at 905/96 passing.

**Manual verification (real dev server, throwaway account):** confirmed via server-rendered HTML that the old lightning-bolt SVG path string is gone (`grep -c` returns 0), the real logo path (`/logo/delivery-clarity-logo-icon.svg`) is now referenced, the `<canvas>` for `AnimatedDataBackground` mounts, and the new `page_dropIcon__*`/`page_page__*` module classes are applied. Not independently re-verified: the actual visual motion (arrow bounce, glow pulses, canvas wave animation) in a live browser — no browser-automation tool was available this session. The `AnimatedDataBackground` component and its animation approach are already proven in production on `/login`/`/register`, but a quick visual pass by the user is still worth doing.

## 9.80 — Home Page Full-Bleed Hero + Banner Reposition, and Landing Page Rebuild

*(Added 2026-07-05. Third round of feedback on the same pages: full-width hero with no visible margins, the stored-data banner moved near the footer, and a full pass on `/landing`, which had accumulated real inline-style technical debt on top of feeling visually flat.)*

**Full-bleed hero (`app/page.tsx`/`.module.scss`):** `.page` now uses the standard CSS full-bleed breakout — `inline-size:100vw` with `margin-inline:calc(50% - 50vw)` to escape the shared `AppShell.module.scss` `.main` container's `max-width:80rem` centering, and `margin-block` set to the exact negative of `.main`'s own padding formula (`calc(-1 * (var(--header-height, 52px) + var(--space-6, 24px)))` / `calc(-1 * var(--space-6, 24px))`) so the hero's top and bottom edges land exactly at the header's bottom edge and the footer's top edge, with no guessed pixel values. Rounded corners were removed since the section is now edge-to-edge. This is intentionally coupled to `.main`'s implementation — documented in a comment — since only this one page currently needs a full-bleed treatment.

**Banner reposition:** the "stored local data found" banner (and its "data cleared" counterpart) moved from the very top of the page to immediately before the closing of the content section, right above the footer, and its copy was condensed from a two-line `<strong>` + `<br/>` paragraph into one short sentence. Purely a JSX/copy relocation — the `storedDataFound`/`clearSuccess` state and the `Clear Local Data` confirm flow are unchanged.

**Landing page rebuild (`app/landing/page.tsx` + new `app/landing/page.module.scss`):** replaced dozens of hardcoded `style={{...}}` objects (one on nearly every element: colors, border-radius, font sizes) with token-driven SCSS module classes, and replaced the `FeatureCard`'s `onMouseEnter`/`onMouseLeave` DOM-style-mutation hover effect with real CSS `:hover`/`:focus-visible` (now keyboard-accessible, which the old implementation was not). Added the same `AnimatedDataBackground` + vignette hero treatment used on `/`, `/login`, `/register` — as a bounded rounded card here, not full-bleed, since this page keeps its top nav and multiple sections below. The two genuinely per-item data-driven colors (`FEATURES[].color`, `STATS[].color`) were kept as CSS custom properties (`--feature-color`, `--stat-color`) per the CLAUDE.md §14.2 exception rather than forced into static classes, each with the required "DYNAMIC CSS VARIABLE" comment.

**ESLint exception-rule limitation found:** this repo's `react/forbid-dom-props` rule (`.eslintrc.json`) blanket-forbids the `style` prop with no logic distinguishing the documented `--`-only custom-property exception from a real violation — verified by running `npx eslint --max-warnings=0` against an existing, already-accepted exception elsewhere in the codebase (`app/admin/audit/page.tsx`), which produces the identical warning. This is the same known limitation CLAUDE.md §60.1 already documents at the codebase level (1,524 pre-existing warnings, 0 errors, tracked as accepted debt) — the 2 warnings on this change's new `--feature-color`/`--stat-color` usages are that same already-tolerated kind, not a new category of violation.

**Automated checks:** `npx tsc --noEmit` clean. `npx eslint app/page.tsx` clean; `npx eslint app/landing/page.tsx` clean apart from the 2 documented custom-property warnings explained above. `npx stylelint` clean on `app/page.module.scss` and `app/landing/page.module.scss` (one real fix applied along the way: `margin-inline-start`/`margin-inline-end` and `margin-block-start`/`margin-block-end` longhand pairs were flagged by `declaration-block-no-redundant-longhand-properties` and consolidated into `margin-inline`/`margin-block` shorthand). Full suite unaffected, 905/96 passing — no state, handler, or route logic touched in any of the three fixes.

**Manual verification (real dev server, throwaway account):** confirmed via the compiled CSS bundle (`/_next/static/css/app/page.css`) that the `100vw`/`50vw` full-bleed rules for `/`'s hero are present and shipped. Confirmed via `/landing`'s server-rendered HTML that the old inline `style="background: var(--dc-s2..."` markup is completely gone, the new `page_hero__*`/`page_heroBadge__*`/`page_featureCard__*` module classes render, the `AnimatedDataBackground` `<canvas>` mounts, and each feature card carries its own `--feature-color` value in its rendered `style` attribute. The stored-data banner's new position could not be triggered via `curl` (it depends on browser `localStorage`, which a server-side request has none of) — verified instead by code review that the relocation didn't change the `storedDataFound`/`clearSuccess` conditions themselves, only their position in the JSX. Not independently re-verified: the actual full-bleed edge alignment, the landing hero's animation, and feature-card hover/focus lift in a live browser — no browser-automation tool was available this session.

## 9.81 — Landing Page Redesign (Premium SaaS Rebuild)

*(Added 2026-07-05. Full rebuild of `/landing` per a detailed 16-section design brief. No image/video attachments actually reached this session — only the brief's text, which was thorough enough to implement directly.)*

**Scope decisions made up front:** no `framer-motion`/`lucide-react`/`@heroicons` installed (checked `package.json` directly) — all motion is CSS animations/transitions plus a small local `useReveal()` `IntersectionObserver` hook in `app/landing/page.tsx`, and icons continue to use the project's existing `SvgIcon`/icon-registry system. The brief's requested nav items (Analytics/Delivery/Planning/Data/Reference) already exist as real nav groups in `src/components/dc-shell/navigation.ts` shared across ~28 pages — nothing to change there. Deliberately skipped adding scroll-triggered "glassy" blur to `AppShell`'s shared header, since that component is shared app-wide and reskinning it for one page's aesthetic is disproportionate scope.

**Hero:** 5 decorative floating mini-cards (Sprint Health / Release Readiness / Data Quality / Team Capacity / Risk Signals — `hidden lg:flex`, never shown on mobile/tablet), a two-tone orange→blue `background-clip: text` gradient on the headline's highlighted phrase, and a compact translucent "dashboard glimpse" panel (3 illustrative progress rows) below the CTAs.

**Stats strip:** each stat now has its own icon and a colored top border (`--stat-color`).

**How it works:** a connecting line between the 3 step cards (desktop only) plus left-to-right scroll-reveal stagger.

**Feature grid:** hover state now glows using each card's own `--feature-color` in addition to the existing lift/shadow.

**Two new sections:** a business-value section ("Turn Jira exports into delivery decisions", 4 cards matching the brief's exact statements) and a dashboard-preview section — a dark, grid-textured panel with 6 CSS-only mock widgets (conic-gradient completion-rate ring, cycle time, blocked items, a release-confidence pill, and two progress bars), explicitly labeled "Illustrative example" so it reads as a preview, not live data.

**CTA:** rewritten with the brief's exact copy and a third button, "Try Sample Dataset," routing to `/?sample=1`. `app/page.tsx` gained a small `useEffect` that reads the new `sample` search param and auto-invokes the pre-existing, unchanged `handleSampleData()`; `handleFile`/`handleSampleData` were wrapped in `useCallback` so the effect's dependency array is exhaustive rather than suppressed.

**Accessibility/motion:** the `useReveal()` hook resolves immediately to "revealed" under `prefers-reduced-motion: reduce`, and every new animation has a matching reduced-motion override. Added an explicit `:focus-visible` style to the new `.btnOutline` class (additive polish — the rest of the app relies on unstyled native browser focus rings, confirmed via `grep` that no global `outline: none` reset exists anywhere, so this isn't fixing a prior regression).

**Automated checks:** `npx tsc --noEmit` clean. `npx eslint` clean on both files apart from 7 documented, already-established-pattern CSS-custom-property warnings (same tolerated category as §9.80). `npx stylelint` clean — one real fix applied: a manually-added `-webkit-background-clip` was rejected by `property-no-vendor-prefix`; removed it and confirmed via the compiled CSS bundle that the project's existing autoprefixer (`postcss.config.js`) adds the vendor prefix automatically at build time. Full suite unaffected, 905/96 passing. `npx next build` succeeds; `/landing` prerenders statically at 6.32 kB.

**Manual verification (real dev server, throwaway account):** confirmed via server-rendered HTML that both new sections, all 5 floating cards, the hero glimpse, all 6 preview tiles, and the new CTA copy all render; confirmed the gradient-headline module class is present. The `?sample=1` auto-trigger could not be end-to-end verified this session — it depends on a client-side effect that only runs after hydration, which a plain `curl` request never executes (confirmed via a direct database check: hitting `/?sample=1` with `curl` produced zero new `ImportLog` rows) — the `handleSampleData()` function it calls is pre-existing and already exercised by the home page's own "Try a sample first" button, so the addition is small and low-risk, but the wiring itself needs a real browser to confirm end-to-end. Also not independently verified: the actual visual motion (floating-card drift, scroll-reveal stagger, hover glow, gradient text) in a live browser — no browser-automation tool was available this session.

## 9.82 — Landing Page Rebuild (GSAP ScrollTrigger, Dark-Hero-Then-Light Direction)

*(Added 2026-07-05. Full replacement of the §9.81 landing page from a new mockup: full-screen dark hero only at the top, then clean light/white SaaS sections. Installed `gsap` (new dependency, zero transitive deps) since the brief required GSAP ScrollTrigger and the requested animations — pinned scroll-scrub sections, batched reveals, SVG path drawing — aren't practical with CSS/IntersectionObserver alone.)*

**Global nav requirement (explicitly emphasized in the brief):** `app/landing/page.tsx` still wraps everything in the same `<AppShell showNav>` every other page uses. `AppShell.tsx`/`.module.scss` were not touched. Verified via server-rendered HTML: the shared nav's 5 groups (Analytics/Delivery/Planning/Data/Reference) render unchanged, no duplicate or landing-specific nav element exists (`grep -c 'role="banner"'` → 0 beyond the one real header), and the logo-icon fix from an earlier round is still in place.

**New structure:** 8 page-local components under `app/landing/components/` (`LandingHero`, `ProductFlowSection`, `HowItWorksSection`, `MetricsStrip`, `FeatureUniverse`, `DashboardPreview`, `BusinessValueSection`, `FinalCTA`) plus `ScrollProgressRail`, composed by a thin `app/landing/page.tsx`. Shared plumbing: `app/landing/hooks/useGsapContext.ts` (registers `ScrollTrigger` once; wraps `gsap.context()` with `.revert()` cleanup on unmount; skips entirely under `prefers-reduced-motion`), `app/landing/hooks/useCountUp.ts` (count-up numbers/rings), `app/landing/lib/cssVars.ts` (shared `CSSVariableProperties` type for the §14.2 exception, used everywhere a per-item color/delay/width is data-driven).

**Hero:** reuses `AnimatedDataBackground` (same canvas already on `/`, `/login`, `/register`) for the "animated gradient waves" requirement, a GSAP entrance timeline (badge → headline → subhead → CTAs → trust chips → dashboard mockup with blur-to-sharp → floating cards), 5 floating stat cards with real values, and a scroll-scrubbed exit as the hero leaves view.

**Product Flow, How It Works, Dashboard Preview:** each has a `gsap.matchMedia()` desktop/mobile split — pinned and scrubbed on desktop (`ScrollTrigger` `pin:true, scrub`), plain reveal-on-enter below the relevant breakpoint. Product Flow draws its connector lines via the `getTotalLength()`/`stroke-dashoffset` technique; Dashboard Preview draws 2 line charts and an area chart the same way, and grows 8 bars via `scaleY`. Feature Universe uses plain CSS `position:sticky` for its left column and `ScrollTrigger.batch()` for the 12-card reveal.

**ScrollProgressRail:** visible only at `≥1280px`; tracks the active section via one `ScrollTrigger` per section `id` (`onToggle`); scrolls via native `scrollIntoView` (falls back to `behavior:'auto'` under reduced motion). It registers the GSAP plugin defensively itself (`ensureGsapPlugins()`, exported from the shared hook) since it manages its own `ScrollTrigger` instances outside `useGsapContext` and can't assume another component's effect ran first.

**Issues caught and fixed before finishing:**
1. The hero initially shipped with no actual moving-wave background — just a static CSS grid + vignette. Caught by checking the rendered HTML for a `<canvas>` element and finding none; fixed by reusing `AnimatedDataBackground`.
2. A work-item-flow bar's height and a data-quality ring's fill were set via plain (non-`--`) inline `style` properties — real CLAUDE.md §14 violations. Fixed by routing them through `--bar-height`/`--ring-percent` custom properties.
3. A decorative icon's color used a raw `style={{color}}` prop, and an upload icon used a Tailwind `text-blue-600` color utility (§15 also prohibits this) — fixed with a `--decision-color` custom property and a plain `.uploadIcon` class respectively.

**Route verification:** all 4 CTA destinations checked directly against the filesystem before wiring — `/` (`app/page.tsx`), `/dashboard` (`app/dashboard/page.tsx`), `/developer` (`app/developer/page.tsx`), `/?sample=1` (existing §9.81 wiring, unchanged). No invented routes.

**Automated checks:** `npx tsc --noEmit` clean across the whole project. `npx eslint app/landing/` clean apart from 10 documented, intentional CSS-custom-property warnings (colors/delays/widths/ring-percentages, each genuinely per-item data-driven, each with a "DYNAMIC CSS VARIABLE" comment). `npx stylelint` clean — one real fix: `fill:url(#id)` needed quoting per `function-url-quotes`. Full suite unaffected, 905/96 passing. `npx next build` succeeds; `/landing` is now 54.1 kB / 166 kB first load (up from 6.32 kB, expected given GSAP + 9 new components); no new build warnings or errors.

**Manual verification (real dev server, throwaway account):** confirmed via server-rendered HTML that all 8 section `id`s are present (`landing-hero`, `product-flow`, `how-it-works`, `metrics-strip`, `feature-universe`, `dashboard-preview`, `business-value`, `final-cta`), the scroll rail renders, all required headline/section copy is present, all 12 feature cards render, and — after the fix above — exactly one `<canvas>` mounts in the hero. Not independently verified: the actual GSAP animation behavior (pinning, scrubbing, timeline sequencing, chart-drawing) in a live browser — no browser-automation tool was available this session; correctness of the timelines and ScrollTrigger configuration was checked via static code review and a clean production build only, not by watching them run.

## 9.83 — Landing Page Polish Round 2 (Overlap Fix, Replay Animations, Layout Fixes)

*(Added 2026-07-06. Follow-up to §9.82 from live screenshots of the rendered page — 5 concrete visual bugs, one of them a critical layout collision.)*

**1. Critical: hero/rail overlap.** The `ScrollProgressRail`'s "01 Hero" label rendered directly on top of the hero's "Sprint Health 86%" floating card — both sit in the same top-left region. Fixed at the source rather than by repositioning 5 floating cards to dodge it: `ScrollProgressRail.tsx` now computes `overHero = activeId === LANDING_SECTIONS[0].id` and applies a `.railHidden` class (`opacity:0; pointer-events:none`) whenever true, so the rail is invisible (and unclickable) for the entire duration the hero is the active section, appearing only once scrolled into the light sections below.

**2. Hero gradient-text hardening.** The gradient-clip "measurable delivery confidence" span was previously the exact element GSAP animated `opacity`/`y` on. Some browsers show a visible box artifact when a `background-clip:text` element's own inline style is actively tweened mid-transition — plausibly what a screenshot showed as a highlighted-looking box behind that phrase. Restructured so the GSAP-animated ref is now an outer wrapper `<span>`, with the gradient-clip `.highlight` styling isolated to a separate, never-animated inner `<span>` — removes the interaction regardless of whether it was the true cause or a mid-animation capture.

**3. Button shape/alignment.** `.btnOutline` (Final CTA) and `.btnSecondary` (Hero) used `border-radius:10px` against the primary button's fully-rounded pill shape — visually mismatched. Both changed to `border-radius:100px`. Added explicit `align-items:center` to both `.actions` flex rows (previously default `stretch`, which could misalign an icon+text button against a text-only sibling).

**4. Dashboard Preview / Metrics Strip only animated once.** Both used `once:true` or default `toggleActions`, so scrolling away and back never replayed the count-up numbers or chart-drawing. `useCountUp` now resets to 0 and re-tweens every time `active` toggles true (not just the first time); both sections' `ScrollTrigger`s now use `onEnter`/`onEnterBack`/`onLeave`/`onLeaveBack` plus `toggleActions: 'restart none restart none'`, so everything replays on every pass in both scroll directions. The chart-drawing timeline's `.to()` line-draw tweens were changed to `.fromTo()` with each path's `getTotalLength()` as an explicit start state — restarting a `.to()` that already reached 0 is a visual no-op the second time around.

**5. Feature Universe too long.** A short sticky title beside a tall 3-column/4-row grid left a large, unbalanced empty gap. Widened `.layout` to `78rem` and added a 4-column breakpoint at `1536px` (3 rows instead of 4 on very wide screens); kept 3 columns from `1280–1535px` so cards don't get too narrow at common desktop widths.

**6. Product Flow engine shape.** Replaced the plain flat-outlined circle with a layered treatment: a pulsing radial-gradient glow, a slow-spinning (24s) dashed ring, and a small icon chip above the title.

**Automated checks:** `npx tsc --noEmit` clean. `npx eslint app/landing/` clean — same 10 already-documented CSS-custom-property warnings, no new ones. `npx stylelint` clean. Full suite unaffected, 905/96 passing. `npx next build` succeeds, no new warnings/errors.

**Manual verification (real dev server, throwaway account):** confirmed via server-rendered HTML and the compiled CSS bundle that the rail's `railHidden` class, both pill-shaped button classes (`border-radius:100px` present in compiled CSS), the new engine wrapper/glow/ring classes, and the `1536px` feature-grid breakpoint are all present and shipped. Not independently verified: the actual visual result of any of these six fixes in a live browser — no browser-automation tool was available this session.

## 9.84 — Landing Page Order & Section-Rhythm Pass (Promo-Referenced)

*(Added 2026-07-06. Checked `/promo` as an explicit design reference; applied its narrative-order and section-rhythm lessons to `/landing` while keeping blue/white as the light-section palette per direct instruction.)*

**`/promo` findings:** it's a fully dark page throughout (`--p-bg:#0d1017`), not "dark hero then light" — not a color reference for `/landing`. Its structure was the useful reference: a simple `IntersectionObserver`-based `Reveal` component (no animation library) and a deliberate narrative order — hook → problem → capability depth → value framing → concrete steps → stats as proof → trust → final ask — with alternating `.section`/`.sectionAlt` background shades giving each section a visible seam.

**Reorder:** `Hero → ProductFlow → HowItWorks → MetricsStrip → FeatureUniverse → DashboardPreview → BusinessValue → FinalCTA` (previous) → `Hero → ProductFlow → HowItWorks → FeatureUniverse → BusinessValue → MetricsStrip → DashboardPreview → FinalCTA` (new) — moves the two strongest proof points (stats, dashboard preview) from the middle to right before the final CTA, where they build momentum instead of being buried. Updated both `app/landing/page.tsx`'s composition order and `ScrollProgressRail.tsx`'s `LANDING_SECTIONS` array (order and `0X` label prefixes) — these two must stay in sync.

**Section-rhythm fix:** replaced the previous inconsistent `#fff`/`#f8fafc` mix (every section was near-identical, no visual seam at all) with a strict alternation of white (`#fff`) and light blue (`#eff6ff`): `ProductFlowSection` blue → `HowItWorksSection` white → `FeatureUniverse` blue → `BusinessValueSection` white → `MetricsStrip` blue → `DashboardPreview` white (outer only; inner dark panel untouched) → `FinalCTA` white. Two deliberate exceptions to strict alternation, both justified: `BusinessValueSection` stayed white rather than blue because its 4 cards already carry their own pastel tints (blue/green/orange/purple) — a blue section behind a blue-tinted first card would blend together; `FinalCTA` stayed white rather than blue because its own card already carries a blue→orange gradient that would flatten into a same-blue backdrop — mirrors `/promo`'s own final CTA section similarly breaking from its alternation pattern. Added `border-block-end: 1px solid rgba(15,23,42,0.06)` to each section (mirroring `/promo`'s `border-block` treatment) so sections read as distinct panels rather than one continuous scroll.

**Scope decision:** did not replace the existing GSAP scroll-reveal system with `/promo`'s simpler `IntersectionObserver` `Reveal` component — the GSAP entrance animations were already working correctly after §9.83's fixes, and the more concrete, verifiable reading of "fix the transition" (section background rhythm) was addressed instead. Noted explicitly in case a future request clarifies a different intent.

**Automated checks:** `npx tsc --noEmit` clean. `npx eslint app/landing/` clean — same 10 already-documented warnings, unchanged. `npx stylelint` clean. Full suite unaffected, 905/96 passing. `npx next build` succeeds, no new warnings/errors.

**Manual verification (real dev server on port 3001, to avoid disturbing the user's own dev server already running on port 3000):** confirmed via server-rendered HTML that all 8 sections now appear in the intended new order (verified by comparing each section `id`'s byte offset in the response body — `landing-hero` < `product-flow` < `how-it-works` < `feature-universe` < `business-value` < `metrics-strip` < `dashboard-preview` < `final-cta`); confirmed via the compiled CSS bundle that both `#eff6ff` and `#fff` are present. Not independently verified: the actual visual rhythm/seam appearance in a live browser — no browser-automation tool was available this session.

## 9.85 — Scroll Rail Removed, Product Flow / How It Works Transitions Simplified; Login Page Redesign Pushed

*(Added 2026-07-06. From a live screenshot of `ScrollProgressRail` plus an explicit "remove it, don't just adjust it" instruction, and a separate request to bring `/landing`'s transitions closer to `/promo` while keeping the confirmed blue/white palette.)*

**Merge-safety note:** before this session's changes, the user had independently pushed two commits directly to `main` (`c32ee91`, `885caa0`) removing the hero's floating cards and making dashboard-preview charts visible by default — both touching files this session was also editing. Rebased the pending branch onto those commits (not a blind merge) so neither set of changes was lost; verified both coexist correctly in the file contents directly (not just via git log) before proceeding.

**Clarifying question asked and answered:** this message's "match `/promo`" instruction conflicted with the prior message's explicit "keep blue/white" instruction. Asked which was intended rather than guessing a third full color change; confirmed: keep blue/white, adopt `/promo`'s simpler *motion* only, not its dark palette.

**1. Scroll rail removed entirely.** Deleted `ScrollProgressRail.tsx`/`.module.scss` and its usage in `app/landing/page.tsx`. `ensureGsapPlugins` in `useGsapContext.ts` (previously exported solely for the rail's standalone `ScrollTrigger` usage) was un-exported back to a private helper now that nothing external calls it.

**2. Product Flow / How It Works transitions simplified.** These were the two remaining components still using `gsap.matchMedia()` to pin-and-scrub the section on desktop with a separate mobile fallback — the same class of complexity that had already caused the hero/dashboard-preview bugs the user fixed themselves. Both now use a single, unconditional one-shot reveal-on-scroll: the existing timeline gets a plain `scrollTrigger: { trigger, start: 'top 75%' }` with no `pin`, no `scrub`, and no breakpoint branching — matching `/promo`'s simpler `Reveal` pattern. Each file's now-unused `ScrollTrigger` import was removed.

**3. Pushed the user's own pending login/feedback edits.** Reviewed both diffs before pushing (not authored this session): `app/login/page.tsx`/`.module.scss` converts the login page from a white Tailwind-utility card to the same dark glass-card treatment as the registration page (plus explicit input/label `id` associations); `FeedbackButton.tsx` hides the floating feedback button on auth-flow pages. Both were complete and coherent, not partial — verified clean and pushed per explicit instruction, no content changes made.

**Automated checks:** `npx tsc --noEmit` clean. `npx eslint app/landing/` clean — same 10 already-documented warnings, count unchanged. `npx stylelint` clean. Full suite unaffected, 905/96 passing (one transient single-suite failure seen once during verification, gone on immediate re-run — not reproducible, not investigated further). `npx next build` succeeds; `/landing` is now 54 kB (down slightly, reflecting the removed rail component). `npx eslint`/`npx stylelint` on the login-page files also clean.

**Manual verification (real dev server on an alternate port, to avoid disturbing the user's own concurrent dev server on port 3000):** confirmed via server-rendered HTML that the rail is completely absent (zero matches for its component name or aria-label), all 8 sections still render, and the login page's new dark-card module class is present. Not independently verified: the actual visual/motion result of the simplified reveal-on-scroll timelines in a live browser — no browser-automation tool was available this session.

## 9.86 — Button Alignment Fix + Dashboard/Hero/Steps Chart Animations Reinstated

*(Added 2026-07-06. From 4 screenshots: misaligned Final CTA buttons, dashboard-preview charts static since the user's own §9.85-era `885caa0` simplification with an explicit "animate each time you land on this section" request, the same request for the hero's own mini dashboard mockup, and a request to design a proper animation for the How It Works steps.)*

**1. Button alignment — root cause, not just a tweak.** `.btn-primary` (global, `app/globals.scss`) inherits `line-height:1; white-space:nowrap;` from its shared `.btn-base`. The page-local `.btnOutline` (Final CTA) and `.btnSecondary` (Hero) declared neither, so their rendered height depended on the browser/Tailwind-preflight default line-height (taller than `1`) — both used identical `px-7 py-3 text-sm` Tailwind padding classes, but that alone wasn't enough to guarantee pixel parity. Fixed by giving both classes their own explicit `padding: 12px 28px; font-size: 14px; line-height: 1; white-space: nowrap;` directly in SCSS, matching `.btn-primary`'s resolved box model exactly instead of relying on Tailwind's utility/component layer ordering to happen to agree. Removed the now-redundant Tailwind padding classes from both buttons' JSX.

**2. Dashboard Preview animation reinstated (not just re-added — made to replay).** Restored the `useState`/`useGsapContext`/`ScrollTrigger` scaffolding removed by the user's own earlier simplification commit (KPI count-up, ring fills, 2 line-chart draws, 1 area-chart draw+fill, 8-bar grow) using the same proven `onEnter`/`onEnterBack`/`onLeave`/`onLeaveBack` + `toggleActions:'restart none restart none'` pattern from §9.83/§9.84, so it now replays every time the section scrolls into view in either direction — directly satisfying "each time," not a revert to static-by-default.

**3. Hero mockup charts animated (previously fully static).** The hero's embedded dashboard mockup (sparkline `<polyline>` + 7 bars) had no animation at all before this fix. Added the same `getTotalLength()`/`strokeDasharray`/`strokeDashoffset` draw technique plus `scaleY` bar growth on its own `ScrollTrigger` (`start:'top 60%'`, `toggleActions:'restart none restart none'`), kept separate from the hero's existing mount-only entrance timeline (badge/headline/CTAs/chips) so only the mockup's charts replay on repeat visits to the hero, not the whole entrance sequence.

**4. How It Works steps animation re-paced and made to replay.** The existing per-step "spotlight" sequence (scale/glow per card, files bounce, progress-bar fill, insight rows fade in) was functionally present but used the default `toggleActions` (plays once, ever). Re-paced timing for clearer breathing room between steps and added `toggleActions:'restart none restart none'` so the full sequence now replays on every scroll-into-view pass, consistent with the other three fixes.

**Automated checks:** `npx tsc --noEmit` clean. `npx eslint app/landing/` clean — same 10 already-documented warnings, count unchanged. `npx stylelint` clean. Full suite unaffected, 905/96 passing. `npx next build` succeeds; `/landing` now 54.4 kB / 167 kB first load (up slightly, reflecting reinstated/new animation code), no new warnings or errors.

**Manual verification (real dev server on an alternate port, avoiding the user's own concurrent dev server on port 3000):** hit one transient "can't reach database server" error seeding a throwaway account — resolved immediately on retry (a Neon connection blip, not a code issue). Confirmed via server-rendered HTML that all 8 sections and both button classes render; confirmed via the compiled CSS bundle that the new `line-height`/`28px` padding values are present and shipped. Not independently verified: the actual visual result of any of these four fixes in a live browser (button alignment, chart replay behavior in both directions, step-animation pacing) — no browser-automation tool was available this session.

## 9.87 — Product Flow Engine Replaced with a 3-Gear Mesh (Adapted from User-Provided Reference)

*(Added 2026-07-06. User shared a standalone reference HTML file — a hand-tuned 3-gear mesh with data dots on a CSS `offset-path` — asking for it to be adapted to this project's conventions and used to replace `ProductFlowSection`'s plain circle "engine.")*

**Adaptations made (not a verbatim copy):**
1. **Colors** — reference used arbitrary yellow gears on a dark page background; this section is light blue, so gears were recolored to the section's actual blue/orange brand palette (`#2563eb`/`#93c5fd` gears, `#f97316` contact glow, `#eff6ff` gear "hole" matching the section background).
2. **Logical properties** — all `left`/`top`/`width`/`height` converted to `inset-inline-start`/`inset-block-start`/`inline-size`/`block-size` per CLAUDE.md §20/§27.
3. **Geometry preserved exactly, scaled uniformly** — the reference's hand-tuned 560×340 gear positions/sizes and the SVG `offset-path` string were kept completely unchanged (recalculating each value at a smaller scale risked breaking the mesh alignment); wrapped in an outer container sized via `calc(560px * var(--gear-scale))` with the original-size stage scaled inside it via `transform: scale(var(--gear-scale))` (`0.5` desktop, `0.42` mobile) — the same technique the reference itself used for its own mobile breakpoint.
4. **Robust dot staggering** — replaced the reference's fragile `:nth-child(6)`/`:nth-child(7)` selectors (coupled to exact sibling position among a mixed gear/contact/dot list) with explicit `.gearDataDot2`/`.gearDataDot3` modifier classes.
5. **Reduced motion** — added a `prefers-reduced-motion: reduce` override disabling all 4 animations (the reference had none), per CLAUDE.md §26.6.
6. **Accessibility** — the whole gear stage is `aria-hidden="true"` (decorative; the two text cards carry the actual meaning), replacing the reference's own `aria-label` on a non-interactive `<div>` with no role.

**Replaces, not just adds:** this fully supersedes both the previous glowing-circle-with-spinning-ring engine treatment (§9.83) and the separate blue/orange SVG connector lines between the two cards — the gear stage's own continuous data-dot path now visually connects the cards through the engine in one piece. The old circle's GSAP `boxShadow` pulse tween was removed since the gears' own perpetual CSS contact-pulse now provides that feel without JS. The existing GSAP reveal timeline (file-icon stagger, decision-item stagger) was kept, with a new fade/scale-in step added for the gear stage's entrance — the gears' internal rotation/data-flow animations run continuously regardless of this reveal.

**Automated checks:** `npx tsc --noEmit` clean. `npx eslint app/landing/` clean — same 10 already-documented warnings (the new gear colors are fixed decorative constants, not per-item data, so none use the §14.2 exception). `npx stylelint` clean. Full suite unaffected, 905/96 passing. `npx next build` succeeds; `/landing` unchanged at 54.4 kB / 167 kB first load.

**Manual verification (real dev server):** confirmed via server-rendered HTML that all 5 new gear-related module classes render and the old engine/line classes are completely gone (0 matches); confirmed via the compiled CSS bundle that `offset-path` and all 4 new `@keyframes` names shipped. Not independently verified: whether the gears visually mesh at their tuned contact points, whether the data dot appears to travel correctly end to end, or `offset-path` rendering (well-supported in Chrome/Edge/Safari, only recently in Firefox — a purely decorative `aria-hidden` element, so a static-dot fallback in unsupported browsers is acceptable, matching the reference's own approach) — no browser-automation tool was available this session.

## 9.88 — Feedback Submissions Notify support@deliveryclarity.app by Email

*(Added 2026-07-06. User request: "feedback when added to the system send an email to support@deliveryclarity.app".)*

**TC-FB-01:** A valid feedback submission still creates a `Feedback` row and returns `{ok:true}` (unchanged behavior, verifying the new email step doesn't disturb the existing write path).

**TC-FB-02:** The notification email is sent to the configured recipient, defaulting to `support@deliveryclarity.app` (overridable via `FEEDBACK_NOTIFICATION_TO`).

**TC-FB-03:** The notification's subject and text body reflect the submitted category, impact level, and message.

**TC-FB-04:** A failed/rejected `sendEmail` call does not fail the feedback submission — the response is still `200 {ok:true}` and the `Feedback` row is still created, per the best-effort design.

**TC-FB-05:** When the submitter is logged in and checked `canContact: true`, their email address is included in the notification body.

**TC-FB-06:** When `canContact` is `false` (even if logged in), the submitter's email is omitted from the notification and replaced with "(not provided)" — matching the existing `Feedback.userEmail` visibility rule.

**TC-FB-07:** An invalid submission (message too short) is rejected with `400` before the `Feedback` row is created or any email is attempted — validation happens first.

**TC-FB-08:** `buildFeedbackNotificationEmail`'s HTML body HTML-escapes user-supplied message content (`<script>`, `&`, `"`) to prevent HTML injection into the notification email.

**TC-FB-09:** The email subject includes both the category and impact level.

**TC-FB-10:** The plain-text body includes the raw (unescaped) message, since it's plain text rather than HTML.

**Automated checks:** All 10 tests pass (`src/__tests__/feedbackNotification.test.ts`). `npx tsc --noEmit`, `npx eslint`, `npx stylelint`, `npx next build` all clean. Full suite 97 suites / 915 tests passing (up from 96/905), no regressions.

**Manual verification:** started the dev server and confirmed `POST /api/feedback` is live and correctly wired by submitting an intentionally-invalid payload (`message: "hi"`), which correctly returned `400` before reaching the email-sending code. **Deliberately did not** perform a live successful-submission test in this environment: `.env` has real Gmail SMTP credentials configured, and `getAppConfig()` checks the database `SmtpSettings` table before falling back to env vars — a real successful submission here would send an actual email to `support@deliveryclarity.app`. Recommended before production reliance: one real end-to-end submission against a non-production recipient to visually confirm the delivered email's formatting.

## 9.89 — P0 Audit Follow-Ups: Email Escaping, Super-Admin Reset Protection, Admin Audit Logging, Site SEO

*(Added 2026-07-07. Follow-up work from a 7-area P0 repo audit — see SRS.md Addendum N. Three contained fixes plus a site-wide SEO pass, done together per explicit instruction: "fix them all, and make the html is seo friendly.")*

**Email escaping (FR-389):**

**TC-EMAIL-ESC-01:** `buildWelcomeEmail`'s HTML body HTML-escapes a `name`/`email` containing `<`, `>`, `&`, `"` characters.

**TC-EMAIL-ESC-02:** `buildVerificationEmail`'s HTML body HTML-escapes `name`/`email` the same way.

**TC-EMAIL-ESC-03:** `buildPasswordResetEmail`'s HTML body HTML-escapes `name` the same way.

**TC-EMAIL-ESC-04:** `buildDemoRequestEmail` and `buildFeedbackNotificationEmail` still escape correctly after their local `esc()` definitions were replaced with the shared `escapeHtml()` — no behavior change, verified by the existing TC-FB-08/TC-DR tests still passing unmodified.

**Super-admin reset protection (FR-390) — 3 new tests in `userReset.test.ts`:**

**TC-RESET-11:** `previewUserReset` blocks the super-admin account even when its email is not on the internal `@deliveryclarity.app` domain.

**TC-RESET-12:** `resetUserData` refuses the super-admin account and deletes nothing.

**TC-RESET-13:** `listExternalUsersEligibleForReset` queries with `isSuperAdmin: false`, excluding the super-admin from the eligible list itself, not just at reset-time.

**Admin audit logging (FR-391):** no new automated tests added — each of the ~10 routes touched already had passing route-level tests (retention settings, thresholds, issue-type hierarchy, orphan rules, cleanup, storage, storage sync, auto-restore, Jira connection sync) covering their functional behavior; the added `safeAuditEvent`/`auditEvent` calls are fire-and-forget side effects consistent with the existing pattern used elsewhere (e.g. `admin_user_create` in `adminUsers.test.ts`, which does not assert on the audit call directly either). Manually verified by reading each route's diff and confirming the call sites match the existing established `safeAuditEvent({ userId, eventType, eventDescription })` shape.

**SEO (FR-392):**

**TC-SEO-01:** `public/robots.txt` allows crawling of `/`, `/landing`, `/promo`, `/help`, `/glossary`, `/privacy`, `/terms` and disallows the ~25 authenticated app-surface prefixes (`/dashboard/`, `/admin/`, `/developer/`, `/api/`, etc.) — verified by reading the file content directly (no automated test framework covers static file content in this repo).

**TC-SEO-02:** `public/sitemap.xml` lists exactly the 7 public pages referenced in `robots.txt`'s `Sitemap:` directive.

**TC-SEO-03:** `/landing`, `/privacy`, `/terms`, `/help`, `/glossary` each render a page-specific `<title>` and meta description via a new sibling `layout.tsx`, distinct from the generic root-layout default — verified via `npx next build`'s output confirming each route compiles as a distinct static page; not verified via a live browser fetch (no browser-automation tool available this session).

**TC-SEO-04:** `/login`, `/register`, `/forgot-password`, `/reset-password`, `/verify-email`, `/change-password` each carry `robots: { index: false, follow: true }` metadata.

**TC-SEO-05:** Root layout (`app/layout.tsx`) emits two `<script type="application/ld+json">` blocks (Organization, WebSite) with static, non-user-controlled content — confirmed via `npx next build` succeeding and via direct code review that no interpolated value originates from user input, request data, or an external source.

**Automated checks:** 3 new tests (`userReset.test.ts` TC-RESET-11–13). Full suite 97 suites / 918 tests passing (up from 97/915), no regressions. `npx tsc --noEmit` clean. `npx eslint` clean on all touched files. `npx stylelint` surfaced 2 **pre-existing** warnings in `app/privacy/page.module.scss`/`app/terms/page.module.scss` (vendor-prefixed `-webkit-backdrop-filter`), confirmed via `git status` to be unrelated to this change (those `.scss` files were not touched — only new sibling `layout.tsx` files were added) and left as-is per the Progressive Technical-Debt Rule rather than expanding scope. `npx next build` clean, all routes compile.

**Known deviation from the initial approach:** a dynamic `app/robots.ts`/`app/sitemap.ts` implementation (reading `NEXT_PUBLIC_APP_URL` at request time instead of a hardcoded domain) was attempted first and reverted after hitting a Next.js 14.2.5 webpack loader bug specific to this machine's project path containing an apostrophe (`Ali's MacBook Pro`) — confirmed via `npx next build` failing with "Module parse failed: Unexpected token" inside `next-metadata-route-loader.js`, tracing to the raw file path being embedded unescaped in a generated single-quoted JS string. This is an environment-specific Next.js bug, not a defect in the route code (both files had valid default exports). Reverted to static `public/robots.txt`/`public/sitemap.xml` with the production domain (`deliveryclarity.app`) written directly, which builds cleanly regardless of local path quirks.

**Manual verification:** not performed — no browser-automation tool available this session to visually confirm rendered `<title>` tags, Open Graph previews, or that `robots.txt`/`sitemap.xml` are served correctly at their expected URLs in a running instance. Recommended before relying on this in production: fetch `/robots.txt` and `/sitemap.xml` directly against the deployed instance, and spot-check `/landing`'s rendered `<title>` in a browser's tab/view-source.

## 9.90 — P0 CSV / Spreadsheet Formula-Injection Guard

*(Added 2026-07-07. Security fix for CLAUDE.md §38.5: outbound CSV/XLSX exports must not allow Jira-sourced or uploaded-spreadsheet metadata to become executable spreadsheet formulas.)*

**TC-SEC-CSV-01:** `sanitizeSpreadsheetCell()` prefixes strings whose first character is `=`, `+`, `-`, `@`, tab, or carriage return with a leading apostrophe.

**TC-SEC-CSV-02:** Strings that contain trigger characters away from position zero are left unchanged.

**TC-SEC-CSV-03:** Non-string values (`number`, `boolean`, `null`, `undefined`, `Date`) are returned unchanged, not stringified.

**TC-SEC-CSV-04:** Empty strings are left unchanged.

**TC-SEC-CSV-05:** Strings with leading whitespace before a trigger (for example `" =SUM(A1)"`) are left unchanged because the trigger is not the actual first character.

**TC-SEC-CSV-06:** The object-row and matrix helpers sanitize nested spreadsheet cell values without changing other cells.

**TC-SEC-CSV-07:** `buildSafeCsv()` sanitizes before CSV quoting/escaping.

**TC-EX-09:** Work Item Explorer XLSX and raw CSV exports neutralize malicious-looking Jira `summary`, `reason`, and generated insight values.

**TC-EX-10:** Work Item Explorer exports leave benign `summary` and `reason` values unchanged.

**TC-X-14:** Smart Excel export neutralizes malicious-looking Jira fields across its shared `makeWs()` path, including Risks & Blockers and Raw Data Reference sheets.

**TC-X-14b:** Smart Excel export leaves benign Jira fields unchanged.

**TC-SEC-CSV-08:** Legacy `exportToExcelBasic()` neutralizes malicious-looking Jira `summary`, `assignee`, `sprint`, and `reason` values in the generated workbook.

**TC-SEC-CSV-09:** Legacy `exportToExcelBasic()` leaves benign Jira fields unchanged.

**TC-SEC-CSV-10:** `exportImportLogsWorkbook()` neutralizes malicious-looking uploaded metadata such as file name, sheet name, headers, warnings, validation errors, and count-breakdown labels.

**TC-SEC-CSV-11:** `exportImportLogsWorkbook()` leaves benign uploaded metadata unchanged.

**TC-SEC-CSV-12:** The retro `.xlsx` template still produces the expected static workbook cells unchanged while routing through the shared spreadsheet safety guard.

## 9.91 — Login/Register Cross-Redirect Notices + Auto-Scroll to Errors

*(Added 2026-07-08. User request: don't dead-end a user who tries to log in without an account or register with an existing one; put errors inside the relevant card and scroll to them.)*

**TC-AUTH-UX-01:** `/login`'s existing `USER_NOT_FOUND` redirect now appends `&notice=create_account` in addition to the pre-existing `?email=`.

**TC-AUTH-UX-02:** `/register` now detects `data.code === 'ALREADY_REGISTERED'` from `POST /api/auth/register` and redirects to `/login?email=<email>&notice=welcome_back` instead of only showing an inline error.

**TC-AUTH-UX-03:** `/login` reads `notice=welcome_back` from the URL and renders a green "Welcome back!" banner (distinct from the existing red error/orange warning banners) instead of a red error, with the email field pre-filled from the `email` param.

**TC-AUTH-UX-04:** `/register` reads `notice=create_account` from the URL and renders an informational banner ("No Delivery Clarity account exists for that email yet — create one below") when arriving from `/login`'s not-found redirect.

**TC-AUTH-UX-05:** On `/login`, `/register`, `/forgot-password`, `/reset-password`, and `/change-password`, the error/notice container has a ref, and a `useEffect` calls `scrollIntoView({behavior:'smooth', block:'center'})` whenever an error or notice becomes visible.

**TC-AUTH-UX-06:** On `/reset-password`, the "missing token" banner and the generic error-state banner are mutually exclusive (guarded so only one renders at a time) so a single shared ref never has to attach to two simultaneously-rendered elements.

**Automated checks:** No new automated tests — this repo has no component-testing infrastructure (no jsdom/RTL in `jest.config.js`); the underlying `USER_NOT_FOUND`/`ALREADY_REGISTERED` API behavior was already covered by existing `loginRoute.test.ts`/`register.test.ts` and is unchanged by this fix. `npx tsc --noEmit` clean. `npx eslint` clean on all 5 touched pages. `npx stylelint` clean on `login/page.module.scss` and `register/page.module.scss`. Full suite 104 suites / 947 tests passing, no regressions. `npx next build` clean.

**Manual verification:** not performed — no browser-automation tool available this session to confirm the redirect round-trip, banner styling, or scroll behavior visually. Recommended before relying on this: manually try logging in with an unregistered email and registering with an already-registered one, confirming both round-trips and banner text/color.

## 9.92 — Persona Preview Switcher (Soft Launch)

*(Added 2026-07-08. User request: a dashboard dropdown to preview each professional persona's view, super-admin-controlled visibility, explaining what differs between views, with an "All" option.)*

**TC-PP-01:** `readPersonaPreviewSettings()` defaults to `enabled: false` when no settings file exists yet.

**TC-PP-02:** `writePersonaPreviewSettings()` persists, and a subsequent `readPersonaPreviewSettings()` reflects the write (cache invalidated correctly).

**TC-PP-03:** `GET /api/admin/persona-preview` returns 401 for an unauthenticated request.

**TC-PP-04:** `GET /api/admin/persona-preview` returns the current settings for any logged-in user, including a non-admin — this is a read, not a mutation, so it isn't restricted to admins.

**TC-PP-05:** `POST /api/admin/persona-preview` returns 403 for a logged-in regular admin (`isSuperAdmin: false`), and does not change the stored setting.

**TC-PP-06:** `POST /api/admin/persona-preview` succeeds for the super-admin, persists the change, and logs an `admin_persona_preview_toggled` audit event.

**TC-PP-07:** `POST /api/admin/persona-preview` returns 400 when `enabled` is not a boolean.

**TC-PP-08:** `POST /api/admin/persona-preview` returns 401 for an unauthenticated request.

**TC-PP-09:** `PERSONA_FOCUS` (the config mapping each persona to its highlighted dashboard pages) has a non-empty summary and at least one focus area for every persona in `PERSONAS` — a config-completeness check per CLAUDE.md §45.2.

**Also updated:** `adminSettingsConsole.test.ts`'s exact `ADMIN_TABS` id-list assertion, to include the new `personaPreview` tab — an intentional, expected update, not a regression.

**Automated checks:** 9 new tests in `personaPreview.test.ts`, all passing. Full suite 105 suites / 956 tests passing (up from 104/947), no regressions. `npx tsc --noEmit` clean. `npx eslint` clean on all touched/new files. `npx stylelint` clean on the new SCSS module. `npx next build` clean.

**Manual verification:** not performed — no browser-automation tool available this session to confirm the dropdown renders correctly, the trigger button's active state, the admin toggle's live effect across a page reload, or that a regular admin genuinely cannot see the Persona Preview tab in Admin Settings. Recommended before relying on this: enable the switch as the super-admin, confirm it appears for a non-admin user, select a persona, confirm the focus links navigate correctly, then disable it and confirm it disappears everywhere.

## 9.93 — Fix: Persona Preview Tab Was Unreachable (AdminNavSidebar Has Its Own Hardcoded List)

*(Added 2026-07-08. User reported "I cant find it" for the §9.92 Persona Preview tab, testing as the super-admin on both local and production.)*

**Root cause:** §9.92 added the tab to `ADMIN_TABS`/`settingsNavItems`, passed to `AdminConsoleLayout`'s `navItems` prop — which is never rendered in `AdminConsoleLayout.tsx` (a pre-existing dead prop, confirmed true for every other tab too, not a regression). The real settings sub-navigation the user actually clicks comes from a separate, independently-hardcoded `SETTINGS_SUB_ITEMS` list in `AdminNavSidebar.tsx`, which §9.92 didn't know existed.

**TC-PP-10:** `AdminNavSidebar` appends a "Persona Preview" link (`/admin/settings?tab=personaPreview`) to the settings sub-navigation only when the current user's `isSuperAdmin` is true (fetched via the existing `getCachedIsSuperAdmin`/`fetchCurrentUser` pattern, seeded from cache then refreshed) — not shown to a regular admin.

**Automated checks:** No new automated tests — `AdminNavSidebar` has no existing test file and this repo has no component-testing infrastructure. `npx tsc --noEmit` clean, `npx eslint` clean, full suite unchanged at 105 suites / 956 tests (this fix only touched one file, no test-relevant logic), `npx next build` clean.

**Manual verification:** not performed. Recommended: the super-admin re-checks `/admin/settings` after this fix deploys and confirms the "Persona Preview" link now appears in the left sidebar under Settings.

## 9.94 — P0 Cross-Account Local Data Leak

*(Added 2026-07-08. User report: "both users in the app access to each other data... must data be private for each one" — confirmed real, two independent root causes, both fixed. See SRS.md Addendum O.)*

**TC-DATAISO-01:** `setStoredOwner`/`getStoredOwner` (shared `src/lib/localDataOwnership.ts` utility) round-trip a tag correctly.

**TC-DATAISO-02:** `isOwnedByCurrentUser` returns `true` only when the stored tag matches the live, server-verified session's `userId`.

**TC-DATAISO-03:** `isOwnedByCurrentUser` returns `false` when the stored tag belongs to a different account than who's currently logged in.

**TC-DATAISO-04:** `isOwnedByCurrentUser` returns `false` for untagged data (e.g. cached before this fix shipped) — treated as untrusted, not "no owner recorded, assume it's fine".

**TC-DATAISO-05:** `isOwnedByCurrentUser` returns `false` when nobody is logged in.

**TC-DATAISO-06:** `loadMetricsWithSource()` refuses a different account's cached `dc_metrics_v2` and clears both the metrics and owner-tag keys rather than returning the data.

**TC-DATAISO-07:** `loadMetricsWithSource()` still correctly serves the *same* account's own cached metrics across a logout/login cycle (same-user continuity is preserved, not broken by this fix).

**TC-DATAISO-08:** A metrics response from `GET /api/metrics/latest` gets tagged in `localStorage` with its real owning `userId` (returned in the API response specifically to support this).

**TC-DATAISO-09:** `clearMetrics()` removes the owner-tag key along with the metrics/source keys.

**TC-DATAISO-10:** `listLocalImportsForCurrentUser()` refuses a different account's cached local import history (filenames, health scores) and clears it.

**TC-DATAISO-11:** `listLocalImportsForCurrentUser()` still serves the same account's own local import history.

**TC-DATAISO-12:** `GET /api/imports` returns `401` for an unauthenticated request — the previous unscoped, unauthenticated file-fallback path no longer exists; the Prisma query is never reached.

**TC-DATAISO-13:** `GET /api/imports` scopes its Prisma query to `where: { userId: session.userId, ... }` for a regular (non-admin, non-`all=true`) request.

**Also fixed:** 1 pre-existing test (`cloudRestoreHardening.test.ts` TC-CS-12) was asserting the old, insecure "always trust local storage regardless of who's logged in" behavior — updated to tag the cache with the test session's own `userId` and mock `GET /api/auth/me`, so it now correctly asserts same-account continuity rather than reverting this fix. Also added `export {};` to both `cloudRestoreHardening.test.ts` and the new `crossAccountDataIsolation.test.ts` to resolve a pre-existing TypeScript "duplicate function implementation" conflict between two same-named, non-exported helpers at global script scope — a `tsc` prerequisite for adding the new test file, unrelated to this fix's actual logic.

**Automated checks:** 13 new tests in `crossAccountDataIsolation.test.ts`, all passing. Full suite 106 suites / 969 tests passing (up from 105/956), no regressions. `npx tsc --noEmit` clean. `npx eslint` clean on all touched/new files. `npx next build` clean.

**Manual verification:** not performed — no browser-automation tool available this session. **This is the most important manual check to run before considering this closed**: log in as account A, upload data, sign out (or don't — either should work), log in as account B on the *same browser*, and confirm the dashboard shows no data belonging to account A.

## 9.95 — Email Verification No Longer Blocks Sign-In

*(Added 2026-07-08. Product decision reversing same-day commit f3ba44a's login-gating behavior — see SRS.md Addendum P.)*

**TC-EMVERIFY-01:** `POST /api/auth/login` succeeds (200) for an account with `emailVerified: false` — no 403/`EMAIL_NOT_VERIFIED` block.

**TC-EMVERIFY-02:** The session created on that login carries `emailVerified: false`, so the UI can react to it (`loginRoute.test.ts` TC-LOGIN-02, updated).

**TC-EMVERIFY-03:** `GET /api/profile` returns `emailVerified` in the profile payload.

**TC-EMVERIFY-04:** `PATCH /api/profile` ignores an `emailVerified` field sent in the request body — the update `data` object is built field-by-field, not spread from the body, so a user cannot self-verify.

**TC-EMVERIFY-05:** A real (non-replay) `POST /api/auth/verify-email` sends a thank-you email to the user (`buildVerificationThankYouEmail`).

**TC-EMVERIFY-06:** Replaying an already-verified token (the route's existing `alreadyVerified` short-circuit) does not resend the thank-you email.

**TC-EMVERIFY-07:** A failed thank-you-email send does not turn a successful verification into an error response — the route still returns `{ok:true}`.

**TC-EMVERIFY-08:** `GET /api/auth/me` re-fetches `emailVerified` from the DB (piggybacking on the existing EP-010 suspension check) and syncs + saves the session when it differs from the stored value — so verifying via a token link (no session involved) clears the reminder on the next page load without requiring a fresh login.

**TC-EMVERIFY-09:** `GET /api/auth/me` does not call `session.save()` again when the DB value already matches the session (no redundant writes).

**Not covered by automated tests (no component-testing infrastructure in this repo):** `EmailVerificationBanner` rendering/hiding logic (hidden on auth-flow pages, hidden when verified, session-scoped dismiss via `sessionStorage`); the "!" badge appearing/disappearing on `UserMenu`'s and `ProfileTab`'s avatars; the verification link working correctly when opened in a different browser than the one the account is logged into (confirmed by code review — `POST /api/auth/verify-email` is purely token-based with no session dependency, and `/verify-email` is not in `middleware.ts`'s `PROTECTED` list — but not exercised end-to-end).

**Automated checks:** Updated `loginRoute.test.ts` (TC-LOGIN-02). 2 new tests in `profileApi.test.ts` (TC-PROF-06/07). 3 new tests in `emailVerification.test.ts` (TC-EV-11–13). 2 new tests in `sessionHardening.test.ts` (TC-SH-06/07). Full suite 106 suites / 976 tests passing (up from 106/969), no regressions. `npx tsc --noEmit` clean. `npx eslint` clean on all touched files. `npx stylelint` clean on the new `EmailVerificationBanner.module.scss`. `npx next build` clean.

**Manual verification:** not performed — no browser-automation tool available this session. Recommended before relying on this: (1) register a new account, confirm you can sign in immediately without verifying; (2) confirm the reminder banner and avatar badge appear; (3) click the verification link from a *different* browser than the one you're logged in on, confirm it succeeds; (4) return to the logged-in browser, navigate to a new page, confirm the banner/badge clear without needing to log out and back in; (5) confirm the thank-you email arrives; (6) specifically check the banner's visual stacking on `/dashboard`, `/admin`, and `/developer` (fixed-position topbar pages) for any overlap.

## 9.96 — Free Color Picker for Issue Type Hierarchy

*(Added 2026-07-08. User request: "make a free color palette to select any color not just specific colors" on Admin Settings → Issue Type Hierarchy. See SRS.md Addendum Q.)*

**TC-ITCOLOR-01:** `deriveColorSet(hex)` preserves the exact chosen color unchanged as `color`.

**TC-ITCOLOR-02:** `deriveColorSet(hex)` returns valid 7-character `#rrggbb` hex strings for `bg` and `border`.

**TC-ITCOLOR-03:** For a representative saturated color, the derived `bg` is lighter than `border`, which is lighter than the original `color` — preserving the presets' "vivid color + pale background + soft border" visual pattern.

**TC-ITCOLOR-04:** Works correctly for a fully-saturated primary color (`#ff0000`).

**TC-ITCOLOR-05:** A near-gray input (`#808080`, ~0% saturation) still produces a valid, non-degenerate tint rather than a flat/colorless background (saturation floored at 40% for tint generation).

**TC-ITCOLOR-06:** Two different hues produce visibly distinct `bg`/`border` values.

**Not covered by automated tests (no component-testing infrastructure in this repo):** the `<input type="color">` swatch's rendering/interaction, the CSS-custom-property `--swatch-color` binding actually painting the swatch preview correctly, and the presets remaining unaffected alongside the new picker.

**Automated checks:** 6 new tests in `issueTypeColorPicker.test.ts`, all passing. Full suite 107 suites / 982 tests passing (up from 106/976), no regressions. `npx tsc --noEmit` clean. `npx eslint` clean on the new `src/lib/colorSwatch.ts` and test file; the component file's warning count went from 4 (pre-existing) to 5 — the 5th is the new, correctly-documented CLAUDE.md §14.2 CSS-variable exception, not new raw-style debt (confirmed via `git stash` comparison against `main`). `npx stylelint` clean on the new SCSS module. `npx next build` clean.

**Manual verification:** not performed — no browser-automation tool available this session. Recommended before relying on this: open Admin Settings → Issue Type Hierarchy, click the new color swatch next to the presets, pick an arbitrary color, and confirm the type's icon/label color, background, and border all update to a coordinated, readable combination; save and reload to confirm it persists.

## 9.97 — P0 Fix: Upload Success but Dashboard Shows Stale Cached Data

*(Added 2026-07-08. User report: "I cant upload new jira sheet, once return success and open dashboard return the already cashed one." See SRS.md Addendum R for the full root-cause analysis.)*

**TC-PENDPUSH-01:** `markPendingPush()` creates valid cache metadata (with `pendingPush: true`) even when no cache-metadata file exists yet — previously a silent no-op in that case.

**TC-PENDPUSH-02:** `markPendingPush()` preserves existing `provider`/`key`/`contentHash` fields while flipping `pendingPush` to `true`.

**TC-PENDPUSH-03:** `pushToCloud()` marks `pendingPush: true` in the cache file *before* the (slow, network-bound) upload resolves — verified with a deliberately delayed mock upload, proving the flag is set for the entire in-flight duration, not just before/after.

**TC-PENDPUSH-04:** `pushToCloud()` clears `pendingPush` and records the new backup key on success.

**TC-PENDPUSH-05:** `pushToCloud()` does not touch the pending flag at all when storage mode is `local` (nothing to push, so no race is possible).

**TC-PENDPUSH-06:** `pushToCloud()`'s existing on-failure retry-marker behavior (re-marking `pendingPush: true` when the upload throws) is unchanged.

**TC-PENDPUSH-07:** `getCacheMeta()` — the function other code reads for sync status — reflects `pendingPush: true` mid-push and `false` after completion, confirming the guard is visible to callers, not just internal state.

**Also fixed:** 1 pre-existing test (`uploadUserId.test.ts`) whose `cloudSync` mock didn't include the newly-imported `markPendingPush`, causing a `TypeError` (500 response) the moment the real upload route tried to call it — added `markPendingPush: jest.fn()` to the mock. This is exactly the kind of failure a missing mock function produces, distinguishing it from a logic regression.

**Automated checks:** 7 new tests in `cloudSyncPendingPush.test.ts`, all passing. Full suite 108 suites / 989 tests passing (up from 107/982), no regressions. `npx tsc --noEmit` clean. `npx eslint` clean on all touched files. `npx next build` clean.

**Manual verification:** not performed — no browser-automation tool or live cloud-storage credentials available this session. Recommended before relying on this: with a real S3/Azure/GCP bucket configured, upload a Jira export in cloud/App storage mode and immediately check the dashboard reflects the new data (not the previous upload); repeat a few times, since the original bug was timing-dependent and wouldn't reproduce on every attempt.

## 9.98 — P0 Fix: Dashboard Router Cache Kept Showing Data From a Previous Upload

*(Added 2026-07-08. Same-day follow-up user report: "when upload new excel sheet, the defualt data loaded all of the time... when upload new sheet need to be presented and do analysis for it." A distinct root cause from §9.97/Addendum R — reproduces on every re-upload within a multi-minute window, independent of cloud storage being configured at all. See SRS.md Addendum S.)*

**Root cause:** none of the 16 `/dashboard/*` pages export `dynamic = 'force-dynamic'`, so Next.js 14.2's App Router treated them as static segments and reused an already-mounted page instance (Router Cache) for up to 5 minutes on any soft navigation back to a previously visited dashboard route — including the redirect immediately after an upload. The page's mount-only `useEffect` (which calls `loadMetricsWithSource()`) never reran, so whatever data had loaded the first time kept showing regardless of what was just uploaded.

**Fix:** `next.config.js` — `experimental.staleTimes: { dynamic: 0, static: 0 }`, disabling the client Router Cache for both tiers so every dashboard navigation always fetches fresh and remounts.

**Automated checks:** `npx tsc --noEmit` clean. Full suite 108 suites / 989 tests passing, unchanged — this is Next.js client-side navigation-cache behavior with no unit-testable surface (Jest doesn't execute the App Router's browser-side Router Cache). `npx eslint` clean, no new warnings. `npx next build` clean, all 16 dashboard routes still prerender successfully.

**Manual verification:** not performed — no browser-automation tool available this session. Recommended before relying on this: upload a Jira export, view a dashboard page, upload a second (different) export within ~1 minute, and confirm the dashboard now shows the second file's data rather than the first.

## 9.99 — P0 Fix: Uploaded Sheet Vanished Immediately After Saving (Ownership-Tag Race)

*(Added 2026-07-08. Urgent same-day user report: "I cant presne [present] the sheet that i uploaded and presented in the app." Diagnosed by querying the live dev database read-only: both real accounts are in local storage mode (EP-017) and `ImportLog` had only one row from an unrelated admin sample-data test — confirming the bug lived entirely client-side. A regression introduced by this same day's cross-account data-leak fix. See SRS.md Addendum T.)*

**Root cause:** `saveMetrics()` wrote fresh upload data to `localStorage` and called `tagCurrentOwner()` to tag it with the current account — but that tag write was fire-and-forget (never awaited). Every upload flow in `app/page.tsx` called `saveMetrics(...)` and immediately `router.push()`'d to the dashboard. The dashboard's `loadMetricsWithSource()` calls `isOwnedByCurrentUser()`, an independent async ownership check, to decide whether to trust the cached data. If that check's own `/api/auth/me` request resolved before the tag-write's request had landed, it found no tag yet, concluded the just-saved data was unverified, and discarded it (`clearMetrics()`) — the exact "untrusted data" safety branch added by the earlier cross-account-leak fix, now misfiring against the *same* account's own fresh upload. The dashboard then found nothing and redirected back to `/`.

**Fix:** `tagCurrentOwner()` (`src/lib/localDataOwnership.ts`) and `saveMetrics()` (`src/lib/storage.ts`) are now both `async` and properly chained — `saveMetrics()` does not resolve until the ownership tag is actually written. All four call sites (`app/page.tsx`'s `handleFile`/`handleProceed`/`handleMerge`, `app/snapshots/page.tsx`'s `handleLoad`) now `await saveMetrics(...)` before navigating.

**TC-DATAISO-14:** `saveMetrics()` does not resolve until the ownership tag is written to `localStorage`, verified under an artificially delayed `/api/auth/me` mock (20ms).

**TC-DATAISO-15:** a freshly saved upload survives an *immediate* subsequent `loadMetricsWithSource()` call — the exact end-to-end scenario that was broken — verified under the same delayed-mock conditions.

**Regression-proven:** both tests were re-run against the pre-fix (fire-and-forget) code by temporarily reverting the fix; both failed, with TC-DATAISO-15 reproducing the reported symptom exactly (`result.metrics` was `null` instead of the just-saved data).

**Automated checks:** 2 new tests, full suite 108 suites / 991 tests passing (up from 108/989). `npx tsc --noEmit` clean. `npx eslint` clean on all touched files. `npx next build` clean.

**Manual verification:** not performed — no browser-automation tool available this session. The live dev database was queried read-only only to diagnose account/storage-mode configuration, not for an end-to-end write test. Recommended before relying on this: as a local-storage-mode account, upload a Jira export and confirm the dashboard shows it immediately without bouncing back to the upload screen; repeat a few times on a throttled/slow network connection, since the original bug was latency-dependent.
